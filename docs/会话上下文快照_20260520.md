# kube-agent 会话上下文快照 — 2026-05-20

> **生成日期**: 2026-05-20 08:42
>
> **对应 commit**: `fdd8c42`
>
> **当前分支**: master（GitLab + GitHub 已双推）
>
> **生成原因**: 哥哥准备出差（5月21日），需要保存完整上下文以便新 session 快速恢复

---

## 一、Session 速查卡

| 项目 | 状态 |
|------|------|
| 后端服务 | PID 54970，端口 8500，BUILD SUCCESS |
| 前端服务 | 前端 Vue 热更新运行（端口 3001） |
| Git 状态 | clean（无未提交改动） |
| 最新 commit | `fdd8c42` Review Log 追加 |
| 109 个 Tool | 全部注册，运行中 |
| LLM 连通性 | 有效（http://124.74.245.75:3000） |
| 会话测试 | ses_XDceQ_YYxib2VQU285cW0Q（zhaotiandi/100002） |

---

## 二、本次 Session 已完成的全部工作

### M3.2 阶段产出（3 项修复 + 1 份文档）

| # | 任务 | 状态 | Commit | 说明 |
|:---:|------|:---:|:---:|:---|
| 1 | 脏数据泄漏根治 | 完成 | `1ee576e` | fallback 路径改 Jackson 序列化 |
| 2 | Graph 模式数据不一致修复 | 完成 | `cc04cf1` | tool_call 节点 orgId 透传修复 |
| 3 | P4 Tool 清理审计 | 完成 | — | 100/100 Tool 合规 |
| 4 | 文档清理 + 里程碑全景图 | 完成 | `5683a8d` | 21 完成+20 未完成+4 阶段计划 |

### 文档清单

- `docs/v3.1/项目里程碑全景图_20260519.md` — 21 完成+20 未完成+4 阶段详细计划（32KB）
- `docs/REVIEW_LOG.md` — M3.2 审计日志已追加

---

## 三、已确认技术决策

| # | 决策项 | 结论 |
|:---:|------|------|
| 1 | 脏数据根因 | fallback 路径的 `StringBuilder.append(data)` → Jackson 序列化 |
| 2 | Graph 29→0 根因 | `resolveOrgId()` 桶式搜索 → state 透传 orgId |
| 3 | P4 实际已完成 | P3.1 时自然完成 |
| 4 | 未来推进顺序 | 按蓝图阶段推进，先冲阶段一 ReAct 引擎 |

---

## 四、ReAct 专家会诊结论（恢复后优先执行！）

### 会诊结论：手写 ReAct（复用框架 StateGraph，手写循环引擎）

| 调研项 | 结论 |
|------|------|
| 框架 ReactAgent | 存在，但 outputKey 写入 AssistantMessage（被污染），不满足条件边解析需求 |
| 版本兼容性 | 框架内部 Spring AI 1.1.2 vs 项目 1.1.6，有运行时风险 |
| 推荐方案 | **手写 ReAct 循环**（已有 SSE/ThreadLocal/超时/截断全部验证过） |
| 入口分流 | AtlasBrain 新增 `DELEGATE_REACT` ActionType，诊断类意图触发 |
| 工具执行 | 复用现有 ToolRegistry + ReActToolExecutor 适配器（109 个零改动） |
| 工期 | MVP 7 人天，完整版 18 人天 |

### 分批实施（已规划，待执行）

| 批次 | 内容 | 工期 | 产出文件 |
|------|------|:---:|:---|
| **第一批** | 核心循环 | 2 天 | ReActEngine, ReActMemory, ReActPromptBuilder, BrainDecision(DELEGATE_REACT), AtlasBrain |
| **第二批** | 工具执行层 | 2 天 | ReActToolExecutor, ReActParser, AtlasOrchestrator(接入) |
| **第三批** | 安全+SSE | 2 天 | ReActBudgetManager, ReActResultSynthesizer, SSE 流式改造 |
| **第四批** | E2E 验证+调优 | 1 天 | 诊断类查询 E2E, Prompt 调优, Polish 集成 |

---

## 五、已知 Bug / 风险清单

| # | 问题 | 严重度 | 备注 |
|:---:|------|:---:|:---|
| 1 | Caffeine SessionStore 重启清空 | 低 | 长期需 Redis |
| 2 | 流式润色 polishStream 未接入 SSE | 中 | polishSync 已运行 |
| 3 | Graph outputKey 被 ReAct 污染 | 中 | 框架 AgentLlmNode 字节码已确认 |
| 4 | Spring AI 1.1.2/1.1.6 版本漂移 | 低~中 | 运行时兼容风险 |
| 5 | LLM 超大 Observation 截断 | 中 | 3000 字符 + "截断/不完整"标记 |

---

## 六、环境速查

| 项 | 值 |
|------|------|
| 项目路径 | `/home/guojin/kube-agent` |
| 前端路径 | `/mnt/f/gitProject/kube-agent-vue` |
| 后端端口 | 8500 |
| kube-manager | localhost:8100 |
| LLM Endpoint | http://124.74.245.75:3000 |
| LLM Model | moonshotai/kimi-k2.6 |
| Java | GraalVM 17（Windows Maven 调用） |
| Maven | E:pache-maven-3.8.8（通过 /mnt/e/ 映射） |
| WSL 代理 | 127.0.0.1:10792 |
| WSL sudo | P@ssw0rd@1 |
| 测试账号 | zhaotiandi / ninePwd! / orgId=100002 |
| 关键备份路径 | `/mnt/h/Hermes中重要文件/` |

---

## 七、下次恢复后「立即执行」清单

恢复 session 后，按优先级依次执行（哥哥已拍板按阶段推进）：

### 立即优先：ReAct MVP 第一批（核心循环）

1. **确认 Claude Code 状态** — 问好测试
2. **新建 `react/` 包结构** + `ReActEngine.java`
3. **新建 `ReActMemory.java`** — Thought/Action/Observation 管理
4. **新建 `ReActPromptBuilder.java`** — System Prompt 动态构建
5. **扩建 `BrainDecision.java`** — 新增 `DELEGATE_REACT` ActionType
6. **扩建 `AtlasBrain.java`** — 诊断类意图路由 ReAct

> 参考：`spring-ai-manual-react` skill + ReAct 集成设计文档

---

## 八、文档索引

| 文档 | 路径 |
|------|------|
| 里程碑全景图 | `docs/v3.1/项目里程碑全景图_20260519.md` |
| REVIEW_LOG | `docs/REVIEW_LOG.md` |
| ReAct 集成设计 | `docs/v3.1/REACT_INTEGRATION_DESIGN.md` |
| 踩坑技能 | `skill_view(name='kube-agent-pitfalls')` |
| 架构决策记录 | `docs/v3.1/ARCHITECTURE_DECISIONS.md` |

---

> **Atlas v3.1 — 出差平安，回来后妹妹接着冲 ReAct！**
