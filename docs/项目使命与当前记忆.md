# kube-agent 项目使命与当前记忆

> 最后更新：2026-07-07
> 这个文件只保存当前仍会指导开发和学习的记忆。历史波次细节请从 Git 历史和 `codex-memory/kube-agent/current` 查找。

## 终极目标

`kube-agent` 不只是一个生产级 Agent 项目，也是一个 Agent 开发学习项目。目标是让项目拥有者通过真实工程实践，从 Agent 初学者成长为能设计、审计、实现顶级 Agent 的开发者。

一期目标不是“能对话的 MVP”，而是在 `kube-manager` / `vue-kube-manager` 基座能力之上交付顶级 Agent Core：

- 安全边界清晰。
- Tool / MCP / kube-manager 外部出口可治理。
- Graph / ReAct / Plan 编排可解释、可测试、可恢复。
- Memory/RAG、Eval、Observability、Audit 具备证据链。
- 多 Agent / 多专家审查先以证据和读模型落地，再逐步考虑运行时 handoff。
- 代码、测试、文档、恢复记忆都带有中文教学价值。

用户已明确：NIM / HPC / Slurm / BCM 推迟到二期，只是暂停这些专家域插件，不降低一期对顶级 Agent Core 的要求。

## 当前 Git 分支规则

- `master`：主分支，也是当前远端默认分支，用于保存已经整理、可作为主线恢复点的成果。
- `develop`：开发分支，也是后续继续推进后端 Agent Core、中文注释和证据链切片的默认工作分支。
- 历史 `codex/...` 分支已经收敛删除；旧恢复快照中出现这些分支名只代表当时状态，不再作为新的工作入口。
- 每个有效切片仍要在 `develop` 上完成测试、文档和恢复记忆，确认后再同步到 `master`，避免重新膨胀出许多临时分支。

## 当前主线状态

| 范围 | 状态 | 记忆 |
|---|---|---|
| 后端主线 | 当前线程负责 | 继续推进 Java/Spring 后端、中文注释、架构学习、文档治理和恢复记忆。 |
| 前端工作台 | 官方 `vue-kube-manager` 已接入 reviewed fixture 只读页 | M5.85-49 已在 `F:\gitProject\vue-kube-manager` 的 `develop` 分支新增 `/system/agent-reviewed-fixture`，通过独立 `/agent-api` 代理读取 kube-agent 绑定规范；旧 `kube-agent-vue` 工作区仍可能有历史脏改，本轮未触碰。 |
| kube-manager 对接 | 已通过 opt-in 8100 READ + 真实 Agent 执行链 + 人审包数据面 smoke | 当前机器 `localhost:8100` 已完成登录型只读验证；smoke 默认跳过，可传 token/orgId，也可用 username/password 在测试进程内登录并通过 `/api/{orgId}/user` 反查可信 orgId。业务链路目前覆盖 6 条 GET/READ/no-HITL 链路：节点列表、节点剩余资源、Dashboard 部署/镜像/EasyFlow 统计和 EasyFlow 列表；M5.85-38 已验证同一批链路能通过 `SafeToolExecutor` 真实执行，并用代表性 `AtlasToolCallback -> SafeToolExecutor` 链路证明模型 ToolCallback 入口不绕过安全边界；M5.85-46 已把真实 READ smoke 的 redacted audit 接到 replay/eval/candidate discovery/reviewed fixture human review package；写操作仍需 HITL、审计、幂等、release evidence。 |
| Eval trace evidence | 已具备可人审包 + 人审字段门禁 + 官方 Vue 只读工作台入口 | M5.85-39 已让 workbench catalog patch review 返回 `reviewedFixtureReadiness`；M5.85-41 已把 reviewed fixture manifest 行级质量门收紧为 `qualityGateStatus` / `failedQualityGates`；M5.85-42 已把当前 trace set 的质量门状态、失败门名、matching/ready/rework 计数投影到 catalog patch review；M5.85-43 新增手动 reviewed fixture candidate preview；M5.85-44 新增自动 candidate workbench；M5.85-45 新增 human review package；M5.85-46 确认真实 `SafeToolExecutor` READ 审计能产出 `READY_FOR_HUMAN_GIT_REVIEW_PACKAGE`；M5.85-47 新增 human review gate；M5.85-48 新增 `GET /api/agent/observability/eval/workbench/reviewed-fixture-vue-binding-spec`；M5.85-49 已在官方 `vue-kube-manager` 实现 `/system/agent-reviewed-fixture` 只读页面、独立 Agent API client、`/agent-api` 代理、单测和文档。它仍是 admin-only/read-only/render-only，不上传 fixture、不写 catalog、不执行 Tool/MCP/LLM/RAG/kube-manager、不写 audit/memory、不授予 CI/release 权力；gate 通过只代表人工 Git 提交可继续，不代表运行时可写。 |
| 文档存放 | 工作区内 `codex-memory` 优先 | 历史 `H:\codex重要文件` 不再作为默认写入路径。 |
| Git 权限 | 用户已授权 | 每个有意义切片完成后提交并推送。 |

## 最新完成切片：M5.85-49 Reviewed fixture Vue workbench

本切片把 M5.85-48 的后端绑定规范接到官方 `vue-kube-manager`，新增 `/system/agent-reviewed-fixture` 只读工作台。目标是让管理员能从真实后端契约查看 reviewed fixture 工作流的组件、字段、阶段、安全策略和禁用动作，而不是在浏览器里创造 fixture、上传 JSON 或写 catalog。

已完成：

- 在 `F:\gitProject\vue-kube-manager` 新增 `src/api/agent-observability.js`：使用独立 `VUE_APP_AGENT_BASE_API || '/agent-api'`，只 GET `reviewed-fixture-vue-binding-spec`，按 kube-agent `ApiResponse(success/data/message)` 解包，可选透传当前输入的 `X-Session-Id`，不复用 kube-manager `X-Token`，不持久化会话、密码或 token。
- 新增 `src/views/agent/reviewed-fixture/index.vue`：渲染 summary metrics、binding status、safety evidence、component specs、field bindings、workflow stages、disabled action bindings、implementation checklist 和只读 raw JSON。
- 在 `src/router/index.js` 的 System 下新增 `/system/agent-reviewed-fixture`，并修正同文件既有 `k8s-dashboard` meta 缩进；后端仍是最终 admin-only 权限边界。
- 在 `vue.config.js`、`.env.development`、`.env.staging` 增加独立 `/agent-api` 代理和 `VUE_APP_AGENT_BACKEND_API=http://localhost:8300`，避免和 kube-manager `8100` 的 `/api` 代理混用。
- 新增 `tests/unit/api/agent-observability.spec.js`，覆盖 session header、ApiResponse 解包、失败消息脱敏传播和只读 endpoint 常量。
- 更新 `vue-kube-manager` 的 `README.md`、`README-zh.md`，并同步本仓 `codex-memory` 恢复记忆。

验证：

- `npx eslint src\api\agent-observability.js src\views\agent\reviewed-fixture\index.vue src\router\index.js` 通过。
- `$env:VUE_APP_TITLE='Kube'; $env:VUE_APP_BILLING_MODEL='true'; npm run test:unit -- --runTestsByPath tests/unit/api/agent-observability.spec.js --runInBand` 通过，4 个测试全部通过；Jest 提示 `.claude/worktrees/.../package.json` 命名碰撞，但不影响结果。
- `npm run build:stage` 成功，仅输出既有 `src/permission.js` unused `whiteList` eslint-loader warning。
- `npm run lint` 仍因既有无关问题失败：`src/permission.js` unused `whiteList`，以及 `src/views/ai4s/course-preview/index.vue` 两处模板解析错误。
- `git diff --check` 通过，仅 Windows LF/CRLF 提示；真实测试密码字面量扫描未命中新改前端文件、测试或文档。

安全边界：

- 本切片是 frontend read-only rendering only；没有新增后端运行时代码。
- 不上传 fixture、不创建 fixture 文件、不写 `eval-trace-sets.json`、不写 catalog、不执行 Tool/MCP/LLM/RAG/kube-manager、不触发 HITL、不写 audit/memory、不启用 CI blocking、不授予 release authority、不升级依赖、不打开二期 NIM/HPC/Slurm/BCM 权力。
- 页面只有刷新读取动作；所有 runtime action 都以禁用动作证据呈现，而不是按钮或 click handler。

下一步计划：

- 用真实 kube-agent admin session 通过 `/agent-api` 做端到端联调，确认工作台能展示 live backend 数据。
- 扩展 `vue-kube-manager` 只读页面到 candidate workbench、human review package、human review gate 和 readiness detail。
- 或准备首个真实 reviewed fixture：只能在 human review gate digest 校验后通过人工 Git review 入仓，再重跑 manifest/catalog patch review。

## 上一完成切片：M5.85-48 Reviewed fixture Vue binding spec

本切片没有直接改 `vue-kube-manager`，而是先让后端发布 reviewed fixture 工作流的前端绑定规范：`GET /api/agent/observability/eval/workbench/reviewed-fixture-vue-binding-spec`。它把 workbench capabilities、overview、自动候选预检、人审包、人审门禁、manifest readiness、catalog patch review、禁用 runtime actions 和前端测试夹具组织成一个 `agent-reviewed-trace-fixture-vue-binding-spec.v1` 只读契约，避免未来前端靠猜字段拼页面。

已完成：

- 新增 `AgentReviewedTraceFixtureVueBindingSpecService` 和 `AgentReviewedTraceFixtureVueBindingSpecResponse`，发布 `VUE_BINDING_SPEC_READY`、组件规格、字段绑定、工作流阶段、状态渲染规则、禁用动作绑定、mock 测试夹具、endpointMap、bindingPolicy、安全和隐私证明。
- 新增 `GET /api/agent/observability/eval/workbench/reviewed-fixture-vue-binding-spec`，通过 Controller admin guard、source-contract、Controller 测试和 Spring Security MockMvc 测试保护。
- workbench capabilities 新增 `workbench-reviewed-fixture-vue-binding-spec`，recommendedWorkflow 在 `workbench-overview` 后优先指向该绑定规范；overview nextActions 新增 `open-reviewed-fixture-vue-binding-spec`。
- 新增 `AgentReviewedTraceFixtureVueBindingSpecServiceTest`，覆盖只读组件、字段路径、禁用动作、mock fixtures、源 capability/overview 嵌入、敏感信息不外泄，以及源码中不得出现 Tool/MCP/kube-manager/HTTP client/执行器调用。

验证：

- 聚焦测试、完整 `mvn -q test`、`mvn -q "-DskipTests" package` 均已通过。
- `git diff --check` 通过，仅保留 Windows LF/CRLF 提示。
- 真实测试密码字面量扫描无源码、文档或恢复记忆命中。

安全边界：

- 这是 admin-only / read-only / binding-spec-only / vue-workbench-only。
- `runtimeControlAllowed=false`、`runtimeButtonsAllowed=false`、`fixtureUploadAccepted=false`、`createsFixtureFile=false`、`catalogMutationAllowed=false`、`runtimeCatalogWrite=false`、`toolExecution=false`、`mcpToolCall=false`、`kubeManagerCalls=false`、`auditWrite=false`、`memoryWrite=false`、`hitlInvocation=false`、`ciBlockingEnabled=false`、`releaseAuthority=false`。
- 该规范可以指导前端隐藏或禁用上传 fixture、写 catalog、开启 CI、批准 release、调用 MCP tools/call、调用 kube-manager 写接口、触发 HITL、运行 LLM eval 或 retrieval/vector runtime 的按钮；但它本身不执行这些动作。

下一步计划：

- 直接在 `vue-kube-manager` 按该 binding spec 实现 reviewed fixture 只读工作台，先用 mock fixtures 覆盖 gate success、gate rework、无候选和 runtime buttons absent。
- 或准备首个真实 reviewed fixture：只能通过 human review gate 得到最终 digest 后，由人工 Git review 提交真实 JSON，再重跑 manifest/catalog patch review。

## 上一完成切片：M5.85-47 Reviewed fixture human review gate

本切片把 M5.85-45 的人审包继续推进到“人工字段可机器校验”的只读门禁：管理员提交 `selectedCandidateTraceId/sourceCommitSha/reviewer/reviewTimestamp/candidateEvidenceDigest/evidenceDigest` 后，后端重新读取当前 human review package，确认候选 trace 没有被调用方替换，确认人审字段格式有效，并按稳定规则重新计算最终 `sha256:` 摘要。通过时返回 `READY_FOR_MANUAL_GIT_FIXTURE_COMMIT` 和 `readyForFixtureCommit=true`，但这只表示人工 Git 提交流程可以继续，运行时仍不能创建 fixture 或写 catalog。

已完成：

- 新增 `AgentReviewedTraceFixtureHumanReviewGateRequest`、`AgentReviewedTraceFixtureHumanReviewGateService`、`AgentReviewedTraceFixtureHumanReviewGateResponse`，发布 `agent-reviewed-trace-fixture-human-review-gate.v1`。
- 新增 `POST /api/agent/observability/eval/workbench/trace-sets/{traceSetId}/reviewed-fixture-human-review-gate?limit={limit}`，通过 Controller admin guard、source-contract、Controller 测试和 Spring Security MockMvc 测试保护。
- gate 校验 `sourceCommitSha` 必须是完整 40 位 Git SHA、`reviewer` 不能像 token/password/secret、`reviewTimestamp` 必须是 ISO-8601 Instant、`candidateEvidenceDigest` 必须匹配当前 package draft、`evidenceDigest` 必须匹配后端按固定字段重新计算的最终 sha256。
- human review package 的 `endpointMap`、`reviewChecklist` 和 `nextActions` 已指向新 gate；workbench capabilities 新增 `workbench-reviewed-fixture-human-review-gate`，capabilityCount 变为 20；overview nextActions 新增 `validate-reviewed-fixture-human-review-gate`，trace set 行新增 `reviewedFixtureHumanReviewGatePath` 和 workflow stage。
- 新增 `AgentReviewedTraceFixtureHumanReviewGateServiceTest`，覆盖成功门禁、package 未就绪 fail-closed、恶意/错配 trace 不回显敏感输入、未知 trace set 404 语义。

安全边界：

- `readyForFixtureCommit=true` 只代表“人工 Git 提交可以继续”，`runtimeFixtureCommitAllowed=false`、`createsFixtureFile=false`、`fixtureUploadAccepted=false`、`runtimeCatalogWrite=false`、`qualityGateStatusGrantedNow=false` 仍然关闭。
- 请求体不会成为新的 trace evidence；`selectedCandidateTraceId` 只能和当前自动候选做一致性比对，不能替换 redacted audit discovery 的证据来源。
- 不创建 `src/main/resources/observability/reviewed-trace-fixtures/*.json`，不写 `eval-trace-sets.json`，不执行 Tool/MCP/LLM/RAG/kube-manager，不写 HITL/audit/memory，不启用 CI blocking、release authority、依赖升级或 Phase 2 NIM/HPC/Slurm/BCM 能力。

本轮验证：

- 聚焦测试：`mvn -q "-Dtest=AgentReviewedTraceFixtureHumanReviewGateServiceTest,AgentReviewedTraceFixtureHumanReviewPackageServiceTest,AgentEvalWorkbenchCapabilitiesServiceTest,AgentEvalWorkbenchOverviewServiceTest,ObservabilityControllerTest,ObservabilityControllerSecurityContractTest,AgentSecurityConfigWebMvcTest" test`。
- 全量测试：`mvn -q test`。
- 打包校验：`mvn -q "-DskipTests" package`。
- `git diff --check` 通过，仅有 Windows LF/CRLF 提示。
- 敏感字面量扫描：针对真实测试密码字面量执行 `rg`，源码、文档和恢复记忆无命中；真实密码未落盘。

下一步计划：

- 如果要提交首个真实 reviewed fixture，先由人工 Git review 用最新 gate 返回的 `expectedEvidenceDigest` 补齐最终字段，再提交真实 JSON 文件并重跑 manifest/catalog patch review；仍不能由运行时自动写入。
- 或切到 `vue-kube-manager` 只读渲染 auto candidate workbench、human review package、human review gate、readiness、checklist、nextActions 和 failedQualityGates，前端仍不能触发 runtime action。

## 上一完成切片：M5.85-46 Real READ smoke to reviewed fixture human review package

本切片把真实 kube-manager 只读链路和 Eval trace evidence 主线接起来。`KubeManagerReadOnlySmokeTest` 在 opt-in 模式下已经能通过登录型身份解析可信 orgId、让 6 条 READ Tool 经 `SafeToolExecutor` 访问真实 `localhost:8100` 并留下 redacted audit；M5.85-46 继续把这些审计事件送入 replay/eval/candidate discovery/reviewed fixture human review package，证明真实只读链路可以产出可人工 Git review 的候选包。

已完成：

- 更新 `KubeManagerReadOnlySmokeTest`：`SafeToolExecutor` READ smoke 使用 W3C-compatible `trc_` traceId，保证后续 candidate discovery 可以接受真实 smoke trace。
- 新增 `assertRealReadSmokeCanProduceReviewedFixtureHumanReviewPackage(...)`：用真实 READ 审计构造 `AgentReplayTimelineService`、`AgentEvalReportService`、`AgentEvalTraceSetCandidateDiscoveryService`、candidate workbench 和 human review package。
- 断言 `phase1-core-golden` 能得到 `READY_FOR_HUMAN_GIT_REVIEW_PACKAGE`，recommended candidate count 等于 6 条 READ Tool，且 package 仍保持 `readyForFixtureCommit=false`。
- 断言 package 不创建 fixture、不写 catalog、不授予 release，不再次执行 Tool 或调用 kube-manager，隐私证明仍为 redacted-only。
- 已更新 README、路线图、项目使命记忆和恢复记忆。

安全边界：

- 真实 8100 调用只发生在显式 opt-in smoke 中；默认测试仍不会访问外部服务。
- 唯一允许的 POST 仍是 `/api/login` 认证 bootstrap；业务链路仍只允许 6 条已审阅 GET/READ/no-HITL Tool。
- 人审包由 redacted audit 读模型生成，不创建 `src/main/resources/observability/reviewed-trace-fixtures/*.json`，不写 `eval-trace-sets.json`，不把 candidate 直接变成 `qualityGateStatus=PASS`。
- 密码和临时 token 只存在于测试进程环境/内存，不写源码、文档、恢复记忆或 Git。
- 不启用上传、catalog runtime write、MCP `tools/call`、kube-manager 写、retrieval/vector runtime、CI blocking 或 release 权力。

本轮验证：

- 默认 smoke：`mvn -q "-Dtest=KubeManagerReadOnlySmokeTest" test`。
- 登录型真实 smoke：临时环境变量注入测试账号，执行 `mvn -q "-Dtest=KubeManagerReadOnlySmokeTest" test`，连接 `http://localhost:8100`，可信 orgId 解析为 `100002`，6 条 `SafeToolExecutor` READ 链路和 reviewed fixture human review package 数据面断言通过。

下一步计划：

- 继续保守推进：如果要提交首个真实 reviewed fixture，必须先由人工 Git review 补齐 `sourceCommitSha/reviewer/reviewTimestamp/evidenceDigest` 并计算最终 sha256 digest。
- 或转向 `vue-kube-manager` 只读渲染自动候选、人审包、readiness、failedQualityGates、checklist 和 nextActions，前端仍不能触发 runtime action。

## 上一完成切片：M5.85-45 Reviewed fixture human review package

本切片承接 M5.85-44 的自动 candidate workbench，但仍不创建真实 fixture、不写 `eval-trace-sets.json`。它把自动发现的首个 redacted trace 候选、candidate fixture draft、人工 Git review 必填字段、复核清单、建议文件名和 manifest 质量门预期组织成一个只读人审包，方便后续准备首个真实 `qualityGateStatus=PASS` fixture。

已完成：

- 新增 `AgentReviewedTraceFixtureHumanReviewPackageService`：复用自动 workbench，不重新接受 caller traceId，不绕过 candidate preview 的 redacted replay / deterministic eval 证据判断。
- 新增 `AgentReviewedTraceFixtureHumanReviewPackageResponse`：发布 `agent-reviewed-trace-fixture-human-review-package.v1`，包含 `manualReviewFields`、`reviewChecklist`、`manifestQualityGatePreview`、候选草稿、endpoint map、policy/safety/privacy proof。
- 新增 `GET /api/agent/observability/eval/workbench/trace-sets/{traceSetId}/reviewed-fixture-human-review-package?limit={limit}`，通过 Controller admin guard、source-contract 和 Controller 单测保护。
- workbench capabilities 新增 `workbench-reviewed-fixture-human-review-package`，capabilityCount 变为 19；overview nextActions 新增 `open-reviewed-fixture-human-review-package`，trace set 行新增 `reviewedFixtureHumanReviewPackagePath`。
- 测试覆盖 ready 人审包、无候选 fail-closed、未知 trace set 404、manual review fields/runtime authority 关闭、raw audit/replay/report/fixture rows 不外泄，以及 capabilities/overview/Controller/security 合同。

安全边界：

- 人审包只帮助人工 Git review，不创建 `src/main/resources/observability/reviewed-trace-fixtures/*.json`，不上传 fixture，不写 `eval-trace-sets.json`，不把 candidate 直接变成 `qualityGateStatus=PASS`。
- 不接收 caller traceId；候选 traceId 仍只来自 redacted candidate discovery 和 candidate preview。
- 不运行 Tool/MCP/LLM/RAG/kube-manager，不写 HITL/audit/memory，不启用 eval runtime、CI blocking、release authority、依赖升级或 Phase 2 NIM/HPC/Slurm/BCM 能力。
- 响应允许嵌入 redacted workbench 和 candidate draft，但不嵌入 raw audit、replay steps、eval reports、fixtureRows、raw endpoint、raw reason、raw parameter values、token 或密码。

本轮验证：

- 已通过聚焦测试：`mvn -q "-Dtest=AgentReviewedTraceFixtureHumanReviewPackageServiceTest,AgentReviewedTraceFixtureCandidateWorkbenchServiceTest,AgentEvalWorkbenchCapabilitiesServiceTest,AgentEvalWorkbenchOverviewServiceTest,ObservabilityControllerTest,ObservabilityControllerSecurityContractTest" test`。

下一步计划：

- 用该人审包对真实自动候选做人工 Git review 准备；只有补齐 `sourceCommitSha/reviewer/reviewTimestamp/evidenceDigest` 且最终 sha256 digest 稳定后，才考虑提交首个真实 reviewed redacted fixture 文件。
- 或切到 `vue-kube-manager` eval workbench 只读渲染：展示自动候选预检、人审包、`reviewedFixtureReadiness`、failedQualityGates、缺口 checklist、nextActions，并确保前端没有 runtime action。
- 仍不启用上传、catalog runtime write、MCP `tools/call`、kube-manager 写、retrieval/vector runtime、CI blocking 或 release 权力。

## 上一完成切片：M5.85-44 Reviewed fixture candidate auto workbench

本切片继续推进 Eval trace evidence 主线，但仍不创建真实 fixture、不写 `eval-trace-sets.json`。它把 M5.85-26 的 redacted candidate discovery 和 M5.85-43 的 reviewed fixture candidate preview 组合成一个前端可直接渲染的只读工作台：管理员打开 trace set 后，后端会从 redacted recent audit 中自动选择第一个推荐候选，再生成候选预检草稿、阻断原因和下一步动作。

已完成：

- 新增 `AgentReviewedTraceFixtureCandidateWorkbenchService`：只组合 `AgentEvalTraceSetCandidateDiscoveryService.discover(...)` 和 `AgentReviewedTraceFixtureCandidateService.candidate(...)`，不接受 caller traceId。
- 新增 `AgentReviewedTraceFixtureCandidateWorkbenchResponse`：发布 `agent-reviewed-trace-fixture-candidate-workbench.v1`，包含 discovery summary、selected candidate、candidate preview、blocking reasons、nextActions、endpoint map、policy/safety/privacy proof。
- 新增 `GET /api/agent/observability/eval/workbench/trace-sets/{traceSetId}/reviewed-fixture-candidate-workbench?limit={limit}`，通过 Controller admin guard、source-contract 和 Controller 单测保护。
- workbench capabilities 新增 `workbench-reviewed-fixture-candidate-autopreview`，capabilityCount 变为 18；overview nextActions 新增 `open-reviewed-fixture-candidate-workbench`，trace set 行新增 `reviewedFixtureCandidateWorkbenchPath`。
- 测试覆盖自动选中推荐候选、无候选 fail-closed、未知 trace set 404、raw audit/replay/report/fixture rows 不外泄，以及 capabilities/overview/Controller/security 合同。

安全边界：

- 不接收 caller traceId；自动选择的 traceId 只来自 redacted candidate discovery 的推荐结果。
- 不创建 `src/main/resources/observability/reviewed-trace-fixtures/*.json`，不上传 fixture，不写 `eval-trace-sets.json`，不把候选 traceId 视为 reviewed evidence。
- 不运行 Tool/MCP/LLM/RAG/kube-manager，不写 HITL/audit/memory，不启用 eval runtime、CI blocking、release authority、依赖升级或 Phase 2 NIM/HPC/Slurm/BCM 能力。
- 响应允许嵌入 redacted candidate 列表和 candidate preview，但不嵌入 raw audit、replay steps、eval reports、fixtureRows、raw endpoint、raw reason、raw parameter values、token 或密码。

本轮验证：

- 已通过聚焦测试：`mvn -q "-Dtest=AgentReviewedTraceFixtureCandidateWorkbenchServiceTest,AgentReviewedTraceFixtureCandidateServiceTest,AgentEvalWorkbenchCapabilitiesServiceTest,AgentEvalWorkbenchOverviewServiceTest,ObservabilityControllerTest,ObservabilityControllerSecurityContractTest" test`。
- 已通过全量测试：`mvn -q test`。
- 已通过打包校验：`mvn -q "-DskipTests" package`。
- 已通过 `git diff --check`；仅有 Windows LF/CRLF 提示。
- 已完成真实测试密码字面量扫描，无源文件/文档命中。

下一步计划：

- 优先使用自动 candidate workbench 找到真实可人审 trace 候选，然后通过人工 Git review 准备首个 `qualityGateStatus=PASS` 的 reviewed redacted fixture 文件。
- 或切到 `vue-kube-manager` eval workbench 只读渲染：展示自动候选预检、candidate preview、`reviewedFixtureReadiness`、failedQualityGates、缺口 checklist、nextActions，并确保前端没有 runtime action。
- 仍不启用上传、catalog runtime write、MCP `tools/call`、kube-manager 写、retrieval/vector runtime、CI blocking 或 release 权力。

## 上一完成切片：M5.85-43 Reviewed fixture candidate preview

本切片继续推进 Eval trace evidence 主线，但仍没有伪造首个真实 fixture，也没有把 caller traceId 直接写入 `eval-trace-sets.json`。它在 template/schema 和 manifest 之间补上一站：管理员可以给某个 trace set 提交候选 traceId，后端只从已有 redacted replay / deterministic eval 读模型生成候选草稿、质量门摘要和剩余人审字段，帮助后续真实 fixture 入仓前做 Git review。

已完成：

- 新增 `AgentReviewedTraceFixtureCandidateService`：按 trace set 查 catalog，规范化 W3C-compatible traceId，只选择第一个合格候选；非法值只计数不回显，避免把 token、路径或任意用户输入写进响应。
- 新增 `AgentReviewedTraceFixtureCandidateResponse`：发布 `agent-reviewed-trace-fixture-candidate.v1`，返回 `candidateFixtureDraft`、`candidateGateSummary`、`remainingHumanReviewFields`、`blockingReasons`、`nextActions`、safety/privacy proof；`readyForFixtureCommit=false`，人审字段仍需填 `sourceCommitSha/reviewer/reviewTimestamp/evidenceDigest`。
- 新增 `POST /api/agent/observability/eval/workbench/trace-sets/{traceSetId}/reviewed-fixture-candidate`，通过 Controller admin guard、source-contract 和 Controller 单测保护。
- workbench capabilities 新增 `workbench-reviewed-fixture-candidate`，overview 的 trace set 行新增 `reviewedFixtureCandidatePath`，`nextActions` 增加 `preview-reviewed-fixture-candidate-before-git-review`。
- 测试覆盖 ready 候选、非法/缺失证据 fail-closed、未知 trace set 404、前端能力目录、overview 路径和 raw replay/report/fixture rows 不外泄。

安全边界：

- 不创建 `src/main/resources/observability/reviewed-trace-fixtures/*.json`，不上传 fixture，不写 `eval-trace-sets.json`，不把 caller traceId 视为 reviewed evidence。
- 不运行 Tool/MCP/LLM/RAG/kube-manager，不写 HITL/audit/memory，不启用 eval runtime、CI blocking、release authority、依赖升级或 Phase 2 NIM/HPC/Slurm/BCM 能力。
- 响应只允许摘要、digest、计数和布尔证明；不嵌入 replay steps、eval reports、fixtureRows、raw endpoint、raw reason、raw parameter values、token 或密码。

本轮验证：

- 已通过聚焦测试：`mvn -q "-Dtest=AgentReviewedTraceFixtureCandidateServiceTest,AgentEvalWorkbenchCapabilitiesServiceTest,AgentEvalWorkbenchOverviewServiceTest,ObservabilityControllerTest,ObservabilityControllerSecurityContractTest" test`。
- 已通过全量测试：`mvn -q test`。
- 已通过打包校验：`mvn -q "-DskipTests" package`。
- 已通过 `git diff --check`；仅有 Windows LF/CRLF 提示。
- 已完成真实测试密码字面量扫描，无源文件/文档命中。

下一步计划：

- 优先用候选预检寻找一个真实可人审的 trace 候选，然后人工 Git review 准备首个 `qualityGateStatus=PASS` 的 reviewed redacted fixture 文件。
- 或切到 `vue-kube-manager` eval workbench 只读渲染：展示 candidate preview、`reviewedFixtureReadiness`、failedQualityGates、缺口 checklist、nextActions，并确保前端没有 runtime action。
- 仍不启用上传、catalog runtime write、MCP `tools/call`、kube-manager 写、retrieval/vector runtime、CI blocking 或 release 权力。

## 上一完成切片：M5.85-42 Catalog patch review fixture 质量门 readiness

本切片继续推进 Eval trace evidence 主线，但仍不创建真实 fixture、不写 `eval-trace-sets.json`。它把 M5.85-41 已有的 manifest 行级质量门进一步投影到 workbench catalog patch review，让未来 `vue-kube-manager` eval workbench 在同一个 review 页面里看见当前 trace set 的质量门状态、失败门名、matching/ready/rework 计数，以及“没有嵌入 raw fixture rows”的证明。

已完成：

- `AgentEvalWorkbenchCatalogPatchReviewResponse.reviewedFixtureReadiness` 新增 `currentTraceSetQualityGateStatus`、`currentTraceSetMatchingFixtureFileCount`、`currentTraceSetReadyFixtureFileCount`、`currentTraceSetReworkFixtureFileCount`、`currentTraceSetFailedQualityGates`、`fixtureRowsEmbedded=false`、`rawFixtureFieldsEmbedded=false`。
- 当前 trace set 只要存在 `FIXTURE_NEEDS_REVIEW_REWORK` 行或失败质量门，即使同一 trace set 也有 ready fixture，catalog patch merge 前置证据仍保持 fail-closed，`requiredBeforeCatalogPatchMerge=true`。
- manifest 顶层状态同步 fail-closed：classpath fixture 批次中存在待返工行时返回 `REVIEWED_FIXTURES_PRESENT_BUT_NOT_READY`，`nextActions` 优先提示返工 reviewed fixture，而不是继续推进 catalog patch。
- Controller/service 测试补齐缺 fixture、坏 fixture、ready+rework 混合 fixture，以及 HTTP read model 输出字段与隐私边界断言；响应不嵌入 `fixtureRows`、reports、replay、fixture traceId、raw principal/org/conversation/endpoint 或测试 token。

验证：

- `mvn -q "-Dtest=AgentEvalWorkbenchCatalogPatchReviewServiceTest,AgentReviewedTraceFixtureManifestServiceTest,ObservabilityControllerTest,ObservabilityControllerSecurityContractTest" test`
- `mvn -q test`
- `mvn -q "-DskipTests" package`
- `git diff --check`
- `rg -n "<测试密码字面量>" -S --hidden --glob "!target/**" .`

安全不变量：

- 本切片仍是 admin-only / read-only / review-only / classpath manifest evidence。
- 未创建真实 fixture 文件，未提交占位 traceId，未上传 fixture，未接收 caller traceId 作为证据，未写 catalog，未运行 eval/replay runtime，未调用 Tool/MCP/LLM/RAG/kube-manager，未写 audit/memory，未打开 CI blocking、release authority 或二期 NIM/HPC/Slurm/BCM 权力。

下一步计划：

- 优先准备首个真实 reviewed redacted fixture 文件，必须让 manifest 行级 `qualityGateStatus=PASS`，再进入 catalog patch review。
- 或切到 `vue-kube-manager` eval workbench 只读渲染：展示 `reviewedFixtureReadiness`、`failedQualityGates`、缺口 checklist、nextActions 和禁用 runtime action。
- 继续保持 CI blocking、runtime catalog write、fixture upload、LLM-as-judge、retrieval prompt influence 关闭，直到 reviewed evidence 和 release 审查完成。

## 上一完成切片：M5.85-41 Reviewed fixture manifest 结构化质量门

本切片继续推进 Eval trace evidence 主线，但没有伪造首个真实 fixture。它先把 reviewed fixture manifest 从“字段存在即可”收紧为“结构化证据必须完整”：未来真实 fixture 入仓后，只有通过 redacted replay、redaction proof、deterministic eval proof、privacy proof、forbidden runtime claims 和 sha256 evidence digest 质量门，才会被计入 ready trace set。

已完成：

- `AgentReviewedTraceFixtureManifestResponse` 新增 manifest 行级 `qualityGateStatus`、`failedQualityGates`、`replaySourceRedacted`、`redactionProofComplete`、`deterministicEvalProofComplete`、`privacyProofComplete`、`forbiddenRuntimeClaimsClosed`。
- manifest ready 判断改为 fail-closed：缺 replay digest / redactedOnly / timelineStepCount、未显式关闭 raw principal/org/conversation/endpoint/reason/params、未显式关闭 LLM/external/tool/MCP/kube-manager、未显式关闭 token/password/prompt/document、缺 sha256 digest 或缺 forbidden runtime claims 时，只能返回 `FIXTURE_NEEDS_REVIEW_REWORK`。
- 当仓库存在 fixture 文件但没有任何文件通过质量门时，manifest 顶层状态会返回 `REVIEWED_FIXTURES_PRESENT_BUT_NOT_READY`，避免前端或人审者把“有文件”误读成“可 merge catalog patch”。
- `AgentReviewedTraceFixtureManifestServiceTest` 补齐一个通过质量门的 reviewed fixture 样例，并新增一个缺证明/暴露 token 的失败样例，证明失败 fixture 不会让 trace set coverage 变 ready。
- `AgentEvalWorkbenchCatalogPatchReviewServiceTest` 的内存 manifest fixture 补齐结构化 proof，保证 workbench readiness 继续基于真实质量门而不是弱 stub。

验证：

- `mvn -q "-Dtest=AgentReviewedTraceFixtureManifestServiceTest,AgentEvalWorkbenchCatalogPatchReviewServiceTest" test`
- `mvn -q "-Dtest=Batch4ReviewedTraceFixtureManifestChineseCommentContractTest,ObservabilityControllerTest,ObservabilityControllerSecurityContractTest" test`

安全不变量：

- 本切片仍是 admin-only / read-only / classpath manifest / review evidence 质量门。
- 未创建真实 fixture 文件，未提交占位 traceId，未上传 fixture，未接收 caller traceId，未写 `eval-trace-sets.json`，未运行 eval/replay runtime，未调用 Tool/MCP/LLM/RAG/kube-manager，未写 audit/memory，未开启 CI blocking、release authority 或二期 NIM/HPC/Slurm/BCM 权力。

下一步计划：

- 准备首个真实 reviewed redacted fixture 时，必须让 manifest 行级 `qualityGateStatus=PASS`，再进入 catalog patch review。
- 或推进 `vue-kube-manager` eval workbench 只读渲染：展示 `reviewedFixtureReadiness`、`failedQualityGates`、缺口 checklist、nextActions 和禁用 runtime action。
- 继续保持 CI blocking、runtime catalog write、fixture upload、LLM-as-judge、retrieval prompt influence 关闭，直到 reviewed evidence 和 release 审查完成。

## 上一完成切片：M5.85-40 真实 Agent 执行入口矩阵验证

本切片把“真实 Agent 执行链验证”从单点 smoke 扩展成长期质量门：所有主执行入口都必须回到 `SafeToolExecutor`，并用来源枚举、trace/audit 和受保护参数过滤证明不会绕过服务端安全边界。

已完成：

- `M4Px4ToolExecuteEntrypointContractTest` 新增入口矩阵契约，锁定 Graph `tool_call`、Plan `execute_node`、ReAct Action、Graph Bridge ToolCallback、legacy core ToolCallback 和 Orchestrator fallback 都必须构造 `SafeToolExecutionRequest` 并标注 `SafeToolExecutionSource`。
- `ReActEngineMultiStepE2ETest` 使用带内存审计的真实 `SafeToolExecutor`，验证两轮 ReAct READ Action 会留下 `REACT_ENGINE` 审计事件，且 traceId、orgId、conversationId、GET/READ/no-HITL、token 脱敏语义稳定。
- `KubeManagerReadOnlySmokeTest` 将 opt-in 真实 smoke 的 read timeout 改为 `atlas.kube-manager.smoke.read-timeout-seconds` / `ATLAS_KUBE_MANAGER_SMOKE_READ_TIMEOUT_SECONDS` 可配置，默认 30 秒，避免本地真实 READ 接口偶发慢响应误伤验证。
- 本地 `localhost:8100` 登录型 smoke 已跑通：测试账号登录成功、可信 orgId 反查为 `100002`，直接 Tool、6 条 `SafeToolExecutor` READ 链路和代表性 ToolCallback 链路均通过；测试过程未把密码或 token 写入源码、文档或恢复记忆。

验证：

- `mvn -q "-Dtest=M4Px4ToolExecuteEntrypointContractTest,ReActEngineMultiStepE2ETest" test`
- `mvn -q "-Dtest=SafeToolExecutorTest,AtlasToolCallbackSafeExecutorTest,AtlasToolCallbackTest,GraphToolCallSafetyGuardTest,GraphExecuteNodeSafetyGuardTest,KubeManagerReadOnlySmokeTest" test`
- 登录型真实 smoke：`ATLAS_KUBE_MANAGER_SMOKE_ENABLED=true`、`ATLAS_KUBE_MANAGER_SMOKE_BASE_URL=http://localhost:8100`、测试账号环境变量，执行 `mvn -q "-Dtest=KubeManagerReadOnlySmokeTest" test`
- `mvn -q test`
- `mvn -q "-DskipTests" package`
- `git diff --check`

安全不变量：

- 本切片只强化测试、文档和 opt-in smoke 超时配置，不打开 kube-manager 写入、敏感读、MCP `tools/call`、HITL marker 创建、audit/memory 写入、retrieval/vector runtime、CI blocking、release authority 或二期 NIM/HPC/Slurm/BCM 权力。
- `SafeToolExecutionSource` 只用于审计、回放和治理，不能被任何入口当成放宽 ToolPermission、HITL、durable audit、租户上下文或 kube-manager 权限的依据。

下一步计划：

- 优先回到 Eval trace evidence：准备首个真实 reviewed redacted fixture 文件，或补 Vue workbench 对 `reviewedFixtureReadiness` 的只读渲染。
- 并行小切片可继续做 Orchestrator/SSE 收尾，检查 Graph/ReAct/Plan 状态传播和前端展示是否还有重复或缺失。
- 不启用写接口、MCP runtime、retrieval prompt influence、CI blocking 或二期域能力，直到对应证据门和 release 审查完成。

## 上一完成切片：M5.85-39 Catalog patch review fixture readiness

本切片继续推进 Phase 1 的 Eval trace evidence 主线，把 reviewed trace fixture manifest 的准备度汇入 workbench catalog patch review。目标是让未来 `vue-kube-manager` eval workbench 在同一个 review 页面里看到：候选 catalog patch 是否可进 Git review、当前 trace set 是否有 reviewed fixture 文件、merge 前还缺什么证据。

已完成：

- `AgentEvalWorkbenchCatalogPatchReviewService` 通过可选构造器组合 `AgentReviewedTraceFixtureManifestService`；生产 Spring 注入会带上 manifest service，旧测试构造仍保持兼容。
- `AgentEvalWorkbenchCatalogPatchReviewResponse` 新增 `reviewedFixtureReadiness` 摘要，包含 manifest status、trace set/fixture 计数、当前 trace set fixture status、`requiredBeforeCatalogPatchMerge`、fixture/template/intake endpoint map 和关闭的 runtime 权力标记。
- `reviewChecklist` 和 `nextActions` 会在缺 reviewed fixture 时提示 `prepare-reviewed-redacted-fixture-file-before-catalog-merge`，但不把 `readyForGitReview` 变成 runtime catalog write 或 release authority。
- 服务测试新增内存 manifest 场景，控制器测试改成 manifest-aware service，验证响应不嵌入 raw fixture rows、raw audit secret、reports 或 replay。

验证：

- `mvn -q "-Dtest=AgentEvalWorkbenchCatalogPatchReviewServiceTest,ObservabilityControllerTest,ObservabilityControllerSecurityContractTest" test`

安全不变量：

- 本切片仍是 admin-only / read-only / review-only。
- 未开放 fixture 上传、caller traceId 证据接收、`eval-trace-sets.json` runtime 写入、eval/replay runtime、Tool/MCP/LLM/RAG/kube-manager 调用、audit/memory 写入、CI blocking、release authority 或二期 NIM/HPC/Slurm/BCM 权力。

下一步计划：

- 优先准备首个真实 reviewed redacted fixture 文件，按 template/schema 填入 redaction、privacy、determinism、source commit 和 evidence digest 证据。
- 或推进 `vue-kube-manager` eval workbench 只读渲染：展示 `reviewedFixtureReadiness`、缺口 checklist、nextActions 和禁用的 runtime action。
- 在 fixture 人审入仓前，不启用 CI blocking、不写 catalog、不运行 LLM-as-judge 或 retrieval eval runtime。

## 上一完成切片：M5.85-38 真实 Agent 执行链 READ smoke

本切片响应“真实 Agent 执行链验证还有多少需要做”的问题，把 kube-manager `8100` 只读 smoke 从直接 `tool.execute(...)` 推进到真实执行入口：`SafeToolExecutor` 和代表性 `AtlasToolCallback -> SafeToolExecutor`。目标不是开放写能力，而是证明当前统一执行边界能用真实后端、真实 token/orgId、真实 ToolRegistry 可见性和审计事件闭环跑通。

已完成：

- `KubeManagerReadOnlySmokeTest` 新增 opt-in `SafeToolExecutor` smoke：同一批 6 个稳定 GET/READ/no-HITL Tool 通过 `SafeToolExecutor.executeIntent(...)` 调用真实 kube-manager。
- 新增代表性 ToolCallback smoke：`DashboardImageCountTool` 通过 `AtlasToolCallback -> SafeToolExecutor` 调用真实 kube-manager。
- smoke 身份同时写入 `UserPermissionContext.onLogin(...)` 和 ThreadLocal token/orgId，覆盖 `AUTHENTICATED` Tool 可见性与 kube-manager HTTP 凭据传播。
- 新增 `SafeToolExecutionResult`、traceId、审计 source、`AgentAuditOutcome.SUCCESS`、trusted orgId、`requiresConfirmation=false` 和受保护参数摘要断言。
- 伪造 `organizationId/orgId/userId/token/writeAllowed/hitlApproved/auditReceipt/releaseDecision` 只进入审计脱敏摘要，不能覆盖服务端可信上下文。

验证：

- `mvn -q "-Dtest=KubeManagerReadOnlySmokeTest" test`
- 登录型真实 smoke：`localhost:8100` 上用测试账号完成登录、可信 orgId 解析、直接 Tool、6 条 `SafeToolExecutor` READ 链路和代表性 ToolCallback 链路验证。
- `mvn -q test`
- `mvn -q "-DskipTests" package`
- `git diff --check`
- 敏感扫描只命中 `<当前密码>`、`password=xxx` 等占位符，没有真实测试密码或 token 落盘。
- 远端分支拓扑仍只有 `develop` 和 `master`。

安全不变量：

- 本切片只扩展 opt-in 测试与文档，不修改生产 Tool 行为、不新增后端 Controller、不打开 kube-manager 写入。
- 未开放敏感读 Tool、MCP `tools/call`、HITL marker、audit/memory 写入、retrieval/vector runtime、CI blocking、release authority、依赖升级或二期 NIM/HPC/Slurm/BCM 权力。
- 测试密码和临时 token 只存在于进程环境/内存，没有写入源码、文档、恢复记忆或 Git。

下一步计划：

- 本切片已经完成全量验证并推送。
- 后续如继续扩展真实 READ smoke，仍要逐条保持 GET/READ/no-HITL、可信 orgId、审计和敏感扫描边界。

## 上一完成切片：M5.85-37 全量验证硬化

本切片按“还需要验证哪些，一次性做完”的要求，把 M5.85-36 后的验证口径扩到全量 Maven 测试、真实 kube-manager smoke、打包、格式、敏感扫描和分支/工作区检查。全量测试暴露了一个历史切片遗留的 Spring 构造器注入问题，并已修复。

已完成：

- 修复 `AgentReviewedTraceFixtureManifestService`：给生产构造器补 `@Autowired`，避免 Spring 在存在 package-private 测试构造器时找不到可用默认构造器。
- 复跑曾失败的 full-context 代表测试 `RuleMatcherTest` 和 `ToolRegistryPermissionTest`。
- 完成全量 `mvn test`、真实 `8100` 登录型 smoke、`mvn -DskipTests package`、格式检查、敏感扫描和 Git 状态确认。

验证：

- `mvn -q "-Dtest=RuleMatcherTest,ToolRegistryPermissionTest" test`
- `mvn -q test`
- 登录型真实 smoke：`localhost:8100` 上用测试账号完成登录、可信 orgId 解析和 6 条 GET/READ Tool 验证。
- `mvn -q "-DskipTests" package`
- `git diff --check`
- 敏感扫描只命中 `<当前密码>`、`password=xxx` 等占位符，没有真实测试密码或 token 落盘。

安全不变量：

- 本切片只修复 Spring DI 元数据和补验证，不修改生产运行时权力模型。
- 未开放 kube-manager 写入、敏感读 Tool、MCP `tools/call`、HITL marker、audit/memory 写入、retrieval/vector runtime、CI blocking、release authority、依赖升级或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-36 kube-manager READ smoke 批量扩展

本切片响应“最好直接完成一批整个任务”的要求，把真实 `8100` smoke 从两条链路扩成一组稳定的只读批次。目标是提高 kube-agent 对真实 kube-manager 只读 Tool 的覆盖面，同时仍保持默认跳过、显式开启、GET/READ/no-HITL、可信 orgId 和伪造控制字段验证这些安全边界。

已完成：

- `KubeManagerReadOnlySmokeTest` 的元数据 guard 现在覆盖 6 个 smoke target：`NodeQueryTool`、`NodeRemainingResourceTool`、`DashboardDeploymentCountTool`、`DashboardImageCountTool`、`DashboardEasyFlowCountTool`、`DashboardEasyFlowTool`。
- 真实 opt-in smoke 在同一次登录身份解析后依次执行 `/node`、`/node/remaining`、`/dashboard/deployment/count`、`/dashboard/image/count`、`/dashboard/easy-flow/count`、`/dashboard/easy-flow`。
- 每条 Tool 调用都继续传入伪造 `organizationId/orgId/token` 和探测性分页/搜索字段，证明 Tool 使用服务端 ThreadLocal 可信上下文，不相信调用方控制字段。
- `ClusterOverviewTool -> /dashboard/resources` 单独探测曾返回 200，但在 JUnit smoke 的 8 秒读超时内出现不稳定超时，因此暂不纳入稳定 smoke 批次。

验证：

- `mvn -q "-Dtest=KubeManagerReadOnlySmokeTest" test`
- 登录型真实 smoke：`localhost:8100` 上用测试账号完成登录、可信 orgId 解析和 6 条 GET/READ Tool 验证。

安全不变量：

- 本切片只扩展测试与文档，不修改生产 Tool 行为、不新增后端 Controller、不打开 kube-manager 写入。
- 未开放用户列表/日志/权限/订单/配额等敏感读 Tool，未开放 MCP `tools/call`、HITL marker、audit/memory 写入、retrieval/vector runtime、CI blocking、release authority、依赖升级或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-35 kube-manager 第二条 READ smoke

本切片按“先推进真实功能”的方向，把 kube-manager 真实 8100 smoke 从单条节点列表 GET 扩展到第二条低风险 Dashboard 部署统计 GET。目标不是做 fake 前端接口，也不是开放写能力，而是证明同一套登录、可信 orgId、ThreadLocal token 和 Tool 参数边界可以支撑多条真实只读链路。

已完成：

- 扩展 `KubeManagerReadOnlySmokeTest`：元数据 guard 现在同时锁住 `NodeQueryTool` 和 `DashboardDeploymentCountTool` 的 GET/READ/no-HITL/ToolPermission/endpoint 契约。
- 真实 opt-in smoke 在同一次登录身份解析后依次执行 `NodeQueryTool -> /api/{orgId}/node` 和 `DashboardDeploymentCountTool -> /api/{orgId}/dashboard/deployment/count`。
- 两条 Tool 调用都会传入伪造 `organizationId/orgId/token` 参数，证明 Tool 必须使用服务端 ThreadLocal 可信上下文，不能相信调用方控制字段。
- 源码级 guard 继续只允许一个认证 bootstrap POST `/api/login`；业务链路仍禁止 POST/PUT/PATCH/DELETE、MCP runtime、HITL、audit/memory 写入和二期 NIM/HPC/Slurm/BCM Tool。

验证：

- `mvn -q "-Dtest=KubeManagerReadOnlySmokeTest" test`
- 登录型真实 smoke：`localhost:8100` 上用测试账号完成登录、可信 orgId 解析和两条 GET/READ Tool 验证。

安全不变量：

- 本切片只扩展测试与文档，不修改生产 Tool 行为、不新增后端 Controller、不打开 kube-manager 写入。
- 未开放敏感读 Tool、用户列表 Tool、MCP `tools/call`、HITL marker、audit/memory 写入、retrieval/vector runtime、CI blocking、release authority、依赖升级或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-34 Reviewed trace fixture template/schema

本切片继续以功能推进为主，把 M5.85-33 的 “manifest 能看到缺口” 往前推成 “前端和人审者知道真实 fixture 应该怎么写”。目标不是创建 fake fixture 或写 catalog，而是提供一个 admin-only 只读模板/schema 端点和仓库目录说明，让后续真实 reviewed redacted fixture 可以按统一格式进入 Git review。

已完成：

- 新增 `AgentReviewedTraceFixtureTemplateService`：只读取 trace-set catalog，生成 fixture 作者模板，不创建文件、不接收请求体、不接收 caller traceId。
- 新增 `AgentReviewedTraceFixtureTemplateResponse`：发布 `agent-reviewed-trace-fixture-template.v1`，包含 JSON Schema、example fixture skeleton、structured proof blocks、traceSetTemplates、file naming rules、authoring workflow、forbidden shortcuts、endpoint map、safety proof 和 privacy proof。
- 新增 `GET /api/agent/observability/eval/reviewed-trace-fixture-template`，通过 Controller 方法级 admin guard、Spring Security MockMvc、source-contract 和 Controller 单测保护。
- 新增 `src/main/resources/observability/reviewed-trace-fixtures/README.md`，明确该目录只收人工 Git review 后的真实 reviewed fixture JSON；模板或占位 JSON 不能提交到扫描目录。
- 更新 fixture intake contract / manifest 的 endpoint map，让前端能从合同、模板、manifest 三个只读入口串起 fixture 准备流程。
- 新增 `AgentReviewedTraceFixtureTemplateServiceTest` 与 `Batch4ReviewedTraceFixtureTemplateChineseCommentContractTest`，把“template-only / read-only / schema-only”“不创建 fixture 文件”“不提交占位 traceId”“不写 catalog”写成可回归契约。

验证：

- `mvn -q "-Dtest=AgentReviewedTraceFixtureTemplateServiceTest,Batch4ReviewedTraceFixtureTemplateChineseCommentContractTest,ObservabilityControllerSecurityContractTest,ObservabilityControllerTest,AgentSecurityConfigWebMvcTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`

安全不变量：

- 新接口是 admin-only、read-only、template-only、schema-only。
- 没有创建真实 fixture JSON，没有提交 fake traceId，没有打开 fixture 上传、caller traceId 接收、runtime catalog write、`eval-trace-sets.json` 写入、eval/replay runtime、Tool/MCP/LLM/RAG/kube-manager 调用、HITL marker 创建、audit/memory 写入、CI blocking、release authority、依赖升级或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-33 Reviewed trace fixture manifest

本切片继续推进 Phase 1 的 Eval trace evidence 方向，把上一层 “reviewed trace fixture intake 合同” 落到 repo-native / classpath-native 的只读 manifest。目标不是立刻把 traceId 写进 `eval-trace-sets.json`，也不是伪造 fixture，而是让前端和人审者能看到当前仓库中哪些 reviewed redacted fixture 文件已经存在、哪些 trace set 还缺文件。

已完成：

- 新增 `AgentReviewedTraceFixtureManifestService`：只扫描 `classpath*:observability/reviewed-trace-fixtures/*.json`，并与 trace-set catalog 做覆盖关系比对。
- 新增 `AgentReviewedTraceFixtureManifestResponse`：发布 `agent-reviewed-trace-fixture-manifest.v1`，返回 fixture rows、trace set coverage、required fixture fields、forbidden shortcuts、endpoint map、safety proof 和 privacy proof。
- 新增 `GET /api/agent/observability/eval/reviewed-trace-fixture-manifest`，通过 Controller 方法级 admin guard、Spring Security MockMvc、source-contract 和 Controller 单测保护。
- 新增 `AgentReviewedTraceFixtureManifestServiceTest` 与 `Batch4ReviewedTraceFixtureManifestChineseCommentContractTest`，把“manifest-only / read-only / classpath-scan-only”“不上传 fixture”“不接收 caller traceId”“不写 catalog”“不运行 eval/replay”写成可回归中文教学契约。
- 当前仓库尚未放入 authoritative reviewed fixture 文件，所以 manifest 正确报告 `NO_REVIEWED_FIXTURE_FILES_FOUND`、`traceSetCount=7`、`fixtureFileCount=0`、`missingFixtureTraceSetCount=7`。

验证：

- `mvn -q "-Dtest=AgentReviewedTraceFixtureManifestServiceTest,Batch4ReviewedTraceFixtureManifestChineseCommentContractTest,ObservabilityControllerSecurityContractTest,ObservabilityControllerTest,AgentSecurityConfigWebMvcTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`

安全不变量：

- 新接口是 admin-only、read-only、manifest-only、classpath-scan-only。
- 没有打开 fixture 上传、caller traceId 接收、runtime catalog write、`eval-trace-sets.json` 写入、eval/replay runtime、Tool/MCP/LLM/RAG/kube-manager 调用、HITL marker 创建、audit/memory 写入、CI blocking、release authority、依赖升级或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-32 kube-manager 登录型 READ smoke

本切片把真实 8100 只读验证从“一次性手工登录拿 token/orgId”推进成可重复的 opt-in 测试入口。`KubeManagerReadOnlySmokeTest` 仍默认不访问外部服务；显式开启后既支持直接传入当前用户 token/orgId，也支持传入 username/password，由测试在进程内调用 kube-manager `/api/login` 获取临时 token，再复用生产同款 `KubeManagerHttpClient.resolveOrgId(username, token)` 通过 `/api/{orgId}/user` 反查服务端可信 orgId，最后执行 `NodeQueryTool -> GET /api/{orgId}/node`。

已完成：

- `KubeManagerReadOnlySmokeTest` 增加登录型身份来源：`ATLAS_KUBE_MANAGER_SMOKE_USERNAME` / `ATLAS_KUBE_MANAGER_SMOKE_PASSWORD` 或对应 system property。
- token/orgId 直传模式仍保留，但二者必须成对出现；只传一个会 fail-fast，避免旧 token 和手填 orgId 混搭。
- 登录表单 `organizationId` 被写清楚为 kube-manager 登录参数，不能作为 kube-agent 可信租户事实；可信 orgId 必须由当前用户 token 反查得到。
- 源码级 smoke guard 调整为只允许一个认证 bootstrap POST `/api/login`，业务链路仍禁止 POST/PUT/PATCH/DELETE 和二期 NIM/HPC/Slurm/BCM Tool。
- README、开发路线图、架构学习地图、恢复记忆和变更日志同步了登录型 smoke 命令与安全边界。

验证：

- `mvn -q "-Dtest=KubeManagerReadOnlySmokeTest" test`
- `mvn -q "-Dtest=KubeManagerReadOnlySmokeTest" "-Datlas.kube-manager.smoke.enabled=true" "-Datlas.kube-manager.smoke.base-url=http://localhost:8100" test`，通过环境变量注入 username/password；本地 kube-manager 登录成功，可信 orgId 由 `/api/{orgId}/user` 解析，`NodeQueryTool` GET 只读链路通过。
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只增强测试入口和文档，不改变生产登录、Session、Tool 执行或 kube-manager 写入逻辑。
- 密码和 token 只从当前进程 system property / 环境变量读取，不写入代码、文档、恢复记忆、日志或 git；测试失败消息也不回显响应体、密码或 token。
- 唯一允许的 POST 是认证 bootstrap `/api/login`；业务 smoke 仍固定在 READ/GET/no-HITL 的节点查询 Tool。
- 没有打开 kube-manager 写入、MCP `tools/call`、HITL marker 创建、audit/memory 写入、retrieval/vector runtime、A2A handoff、CI blocking、release authority、依赖升级或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-31 Git 分支治理

本切片把仓库工作流收敛为两个长期分支：`master` 作为主分支和远端默认分支，`develop` 作为后续后端开发分支。历史 `codex/...` 远端分支已删除，避免恢复记忆和后续协作继续扩散临时分支。

安全不变量：

- 分支治理只改变 Git 拓扑和恢复记忆，不改变 Java 运行时行为。
- 后续有效切片仍在 `develop` 完成测试、文档和恢复记忆，再按需同步 `master`。

## 上一完成切片：M5.85-30 Reviewed trace fixture intake 合同

本切片继续推进 Phase 1 的 Eval trace evidence 方向，聚焦 reviewed redacted trace fixture 在进入人审、Git review 和后续 catalog promotion 前必须满足的接入规范。目标是把“fixture 应该长什么样、需要哪些隐私/确定性证明、哪些捷径必须禁止”先定义成 admin-only 只读合同，而不是打开运行时上传或目录写入。

已完成：

- 新增 `AgentReviewedTraceFixtureIntakeContractService`：只读取 trace set catalog 来计算当前待补 fixture 范围，不接收请求体、不接受调用方 traceId、不读取 raw audit、不修改 `eval-trace-sets.json`。
- 新增 `AgentReviewedTraceFixtureIntakeContractResponse`：发布 `agent-reviewed-trace-fixture-intake-contract.v1`，包含 required fixture fields、review workflow、quality gates、trace set readiness、forbidden shortcuts、endpoint map、safety proof 和 privacy proof。
- 新增 `GET /api/agent/observability/eval/reviewed-trace-fixture-intake-contract`，并通过 Controller 方法级 admin guard、source-contract 和 MockMvc 测试保护。
- 新增 `AgentReviewedTraceFixtureIntakeContractServiceTest` 与 `Batch4ReviewedTraceFixtureIntakeChineseCommentContractTest`，把“intake-spec-only / read-only / contract-only”“不上传 fixture”“不接受 caller traceIds”“不写 catalog”“不运行 eval/replay”“不打开 release/CI 权力”写成可回归的中文教学契约。

验证：

- `mvn -q "-Dtest=AgentReviewedTraceFixtureIntakeContractServiceTest,Batch4ReviewedTraceFixtureIntakeChineseCommentContractTest,ObservabilityControllerSecurityContractTest,ObservabilityControllerTest,AgentSecurityConfigWebMvcTest" test`
- 后续提交前还需要运行 `mvn -q "-DskipTests" validate` 和 `git diff --check`。

安全不变量：

- 新接口是 admin-only、read-only、contract-only、intake-spec-only。
- 没有打开 fixture 上传、caller traceId 接收、runtime catalog write、`eval-trace-sets.json` 写入、eval/replay runtime、Tool/MCP/LLM/RAG/kube-manager 调用、HITL marker 创建、audit/memory 写入、CI blocking、release authority、依赖升级或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-29 顶级 Agent 学习指南知识卡片增强

本切片继续响应“把 `以kube-agent为例的顶级Agent开发学习指南.md` 再丰富一些，里面所用到的技术和知识点都详细解释，并附相关文档和学习地址”的教学目标，把指南从技术专题手册继续扩展为可跟读、可练习、可对照源码的学习手册。

已完成：

- 新增 `19. 技术知识点学习卡片`，把 kube-agent 已用或即将纳入治理的技术拆成学习卡片：Java/Maven/Spring Boot、Spring Security、LLM/Tool Calling/Structured Output、Graph/ReAct/Plan/Multi-Agent、Tool 治理、kube-manager HTTP outlet、HITL/Audit/Release Gate、Memory/RAG/Embedding/VectorStore、Observability/Tracing/Eval/Replay、MCP/A2A、测试/质量/供应链。
- 每张卡片都说明“解决什么问题、在 kube-agent 中对应哪里、容易误解什么、应该读哪些官方资料”，把学习点绑定到真实包和安全边界，而不是只堆链接。
- 新增 `20. 源码精读路线`，引导从 Security、Principal、Orchestrator、Brain/Intent、Graph/ReAct/Plan、ToolRegistry、SafeToolExecutor、kube-manager outlet、Audit/Observability、Memory/RAG、MCP、测试一路读完整 Agent 请求。
- 新增 `21. 官方文档精读顺序`、`22. 新技术采纳规则` 和 `23. 学习练习任务库`，明确学习最新技术不等于直接打开 runtime；新技术必须经过官方来源、版本对比、兼容矩阵、安全设计、read model、focused test、reviewed trace、release gate、恢复记忆和小步提交。
- 抽样校验官方/参考链接，并修正已迁移的 Spring AI / OpenAI 官方地址。

验证：

- 官方/参考链接抽样复测通过。
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只修改文档和恢复记忆，不改变 Java 运行时行为。
- 学习 Java 21/25、Spring Boot 4、Spring AI 2、MCP runtime、A2A、GraphRAG、reranker、LLM-as-judge、CI blocking 或写 Tool，不等于当前主干已启用这些能力。
- 没有打开 MCP `tools/call`、kube-manager 写入、HITL marker 创建、audit/memory 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking、runtime catalog write 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-28 Eval trace promotion workflow 中文教学注释

本切片继续推进 Phase 1 的 Eval trace evidence 方向，聚焦 trace-set promotion workflow 和 eval workbench wrapper。目标是让“候选发现 -> curation review -> catalog patch proposal -> human Git review”的链路清楚表达为只读审阅工作流，而不是运行时目录写入接口。

已完成：

- `AgentEvalTraceSetPromotionWorkflowService` 补充中文说明：它只编排 candidate discovery、curation review 和 catalog patch proposal；是 read-only / workflow-only / proposal-only 层，不修改 `eval-trace-sets.json`。
- `AgentEvalTraceSetPromotionWorkflowArtifact` 补充中文说明：它是前端可渲染的 read model，`readyForGitReview=true` 只说明可以进入人工 Git review，不是目录写权限或 release authority。
- `AgentEvalTraceSetPromotionWorkflowRequest` 补充中文说明：所有字段只影响 review artifact，不能授权 Tool 或 kube-manager 执行。
- `AgentEvalWorkbenchPromotionWorkflowService` 和 `AgentEvalWorkbenchPromotionWorkflowResponse` 补充中文说明：workbench wrapper 只组合只读证据，`uiSteps`、`patchSummary`、`candidateGateSummary` 和 `nextActions` 都是教学/审阅导航，不是执行按钮。
- `AgentEvalTraceSetPromotionWorkflowServiceTest` 与 `AgentEvalWorkbenchPromotionWorkflowServiceTest` 补充中文测试注释。
- 新增 `Batch4EvalTracePromotionWorkflowChineseCommentContractTest`，锁定 promotion workflow / workbench wrapper 的中文教学 marker。

验证：

- `mvn -q "-Dtest=Batch4EvalTracePromotionWorkflowChineseCommentContractTest,AgentEvalTraceSetPromotionWorkflowServiceTest,AgentEvalWorkbenchPromotionWorkflowServiceTest,AgentEvalWorkbenchCapabilitiesServiceTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只补中文注释和源码契约测试，不改变生产逻辑。
- Promotion workflow 和 workbench wrapper 仍是 read-model / workflow-only / proposal-only；`readyForGitReview` 只是 human Git review 证据，不是 runtime catalog write、catalog promotion authority 或 release authority。
- 没有打开 MCP `tools/call`、kube-manager 写入、HITL marker 创建、audit/memory 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking、runtime catalog write 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-27 顶级 Agent 学习指南技术专题深化

本切片继续响应“把 `以kube-agent为例的顶级Agent开发学习指南.md` 再丰富一些，详细解释所用技术和知识点，并附相关文档和学习地址”的学习目标，把指南从课程型导读进一步扩展为“技术专题精读手册”。

已完成：

- 新增“技术专题精读索引”，逐章解释 Java/Maven/Spring 控制平面、LLM 接入、Tool Calling、结构化输出、Graph/ReAct/Plan/多 Agent 编排、Tool 治理、kube-manager HTTP 出口、HITL/Guardrails/Audit/Release Gate、Memory/RAG/VectorStore/GraphRAG、Eval/Replay/Observability、MCP Resources/Prompts/Tools、Agent 安全和测试质量门。
- 每个专题都绑定 kube-agent 的真实设计边界，强调 LLM/Plan/ReAct/前端输出都是候选输入，`SafeToolExecutor` 仍是 Tool 执行边界，MCP `tools/call`、RAG retrieval runtime、A2A runtime handoff、CI blocking 和二期 NIM/HPC/Slurm/BCM 仍关闭。
- 新增“从小白到高手的项目化学习路线”，把学习阶段映射到源码阅读、审查清单、小切片交付和恢复记忆，让项目继续承担教学职责。
- 新增官方资料导航清单，按 Java/Maven、Spring Boot/Security、Spring AI、OpenAI Agent 思路、MCP、Kubernetes、OpenTelemetry、RAG/Embedding、安全治理、测试与质量分类整理学习入口。

验证：

- 新增官方/参考链接抽样 HTTP 检查通过。
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只修改文档和恢复记忆，不改变 Java 运行时行为。
- 没有打开 MCP `tools/call`、kube-manager 写入、HITL marker 创建、audit/memory 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-26 Eval trace evidence / curation 链路中文注释

本切片推进 Phase 1 的 Eval trace evidence 方向，聚焦 release-gate 证据链：trace set catalog、candidate discovery、curation review 和 catalog patch proposal。目标是让这条链路清楚表达“如何把运行轨迹变成 reviewed redacted evidence”，同时避免把运行时接口误解成自动发布开关。

已完成：

- `AgentEvalTraceSetCatalogService` 补充中文说明：目录来自 `classpath:observability/eval-trace-sets.json`，输出给 Observability/Eval 工作台；catalog/gate bundle/curation/patch proposal 都是 read-model/review-only，不直接写 classpath catalog。
- `AgentEvalTraceSetDefinition` 补充中文说明：traceIds 是 redacted replay evidence anchor，不是用户身份、租户、Tool 参数、HITL token、audit receipt 或 release authority。
- `AgentEvalTraceSetCandidateDiscoveryService`、`AgentEvalTraceSetCandidate`、`AgentEvalTraceSetCandidateDiscoveryResponse` 补充中文说明：候选发现只读取 redacted audit read model，输出“建议审阅”列表，不自动提升目录、不运行 eval。
- `AgentEvalTraceSetCurationReviewArtifact` 补充中文说明：curation review 是审阅产物，候选 traceId 通过 deterministic gate 后也必须进入人审、Git diff、CI gate bundle 和恢复记忆。
- `AgentEvalTraceSetCatalogPatchProposalArtifact` 补充中文说明：RFC6902 JSON Patch 只是 artifact，不执行 runtime catalog mutation，也不打开 CI blocking 或 release authority。
- 新增 `Batch4EvalTraceCurationChineseCommentContractTest`，锁定上述中文教学 marker。

验证：

- `mvn -q "-Dtest=Batch4EvalTraceCurationChineseCommentContractTest,AgentEvalTraceSetCatalogServiceTest,AgentEvalTraceSetCandidateDiscoveryServiceTest,AgentMemoryRagReviewedTraceEvidenceManifestServiceTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只补中文注释和源码契约测试，不改变生产逻辑。
- Eval traceIds 仍是脱敏 replay 证据锚点，不是身份、租户、Tool 参数、HITL token、audit receipt、release authority、retrieval runtime 或 CI blocking 权力。
- 没有打开 MCP `tools/call`、kube-manager 写入、HITL marker 创建、audit/memory 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-25 顶级 Agent 学习指南扩写

本切片响应“把 `以kube-agent为例的顶级Agent开发学习指南.md` 再丰富一些，详细解释所用技术和知识点，并附相关文档和学习地址”的请求，把学习指南从项目导读扩展成更系统的课程型文档。

已完成：

- 在 `docs/learning/以kube-agent为例的顶级Agent开发学习指南.md` 增加技术栈学习地图，覆盖 Java/Maven、Spring Boot、Spring Security、Spring AI、OpenAI tool/agent 思路、Spring AI Alibaba Graph/ReAct、MCP、kube-manager/Kubernetes、Resilience4j、Micrometer/OpenTelemetry、Memory/RAG、ONNX/DJL、Caffeine、OWASP LLM Top 10、NIST AI RMF、测试与质量工具，并附官方或主流学习地址。
- 增加核心知识点详解：Agent 不是单次 LLM 调用、Tool calling 是应用侧执行、Prompt Injection 不能只靠 Prompt 防、身份/租户/session/conversation 的区别、HITL 必要但不充分、Graph/ReAct/Plan 不是授权结构、RAG 难点是证据托管、Eval 是发布闸门、Observability 要解释 Agent 决策、MCP 不是安全豁免、Kubernetes API 思维、质量工具服务 release gate。
- 扩写请求生命线、身份边界、HITL、Tool 安全链、写操作、MCP、Memory/RAG、Eval/Replay/Observability、Graph/ReAct/Plan、前后端分工和切片闭环，加入常见误区、验收清单、审查路径和练习题。
- 新增分阶段学习任务和“判断自己是否真的学会”的问题清单，方便后续把项目开发当作 Agent 教学路线持续推进。

验证：

- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只修改文档和恢复记忆，不改变 Java 运行时行为。
- 没有打开 MCP `tools/call`、kube-manager 写入、HITL marker 创建、audit/memory 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-24 中文注释 Batch 5 第八片：支撑测试教学注释

本切片继续把测试文件作为学习材料维护，聚焦 config/store/intent 相关支撑测试。它们保护的是异步可信上下文、会话 owner 过滤、意图仲裁、Embedding 降级和规则匹配这些“看起来不像执行层、但实际会影响 Agent 安全边界”的契约。

已完成：

- `TokenPropagatingTaskDecoratorTest` 补充中文说明：异步线程执行时必须恢复服务端可信 token/orgId 快照，任务结束后必须恢复旧 ThreadLocal，不能从请求体、前端字段或 LLM 参数制造身份。
- `ConversationStoreTest` 补充中文说明：conversationId 只是资源定位符，详情、改名、删除仍必须按当前可信 owner 收敛，不能作为授权凭证、prompt、trace 或长期记忆。
- `IntentArbiterTest` 补充中文说明：多层意图仲裁输出只是候选路由证据，confidence、crossBoost、matchedLevel 和 priority 不能成为 Tool 授权、HITL、audit、release 或 kube-manager 写权限。
- `EmbeddingMatcherMockTest` 补充中文说明：L1 Embedding 不可用时必须 fail-soft 返回 null，让 L2/L3/L4 继续工作；相似度不是安全门禁。
- `RuleMatcherTest` 补充中文说明：关键词、正则和模糊分数只产生 L2/L4 候选，不调用真实 LLM/Embedding/RAG/Tool/MCP/kube-manager，也不能创建 HITL marker 或批准写操作。
- 新增 `Batch5SupportTestChineseCommentContractTest`，锁定上述测试级中文教学 marker。

验证：

- `mvn -q "-Dtest=Batch5SupportTestChineseCommentContractTest,TokenPropagatingTaskDecoratorTest,ConversationStoreTest,IntentArbiterTest,EmbeddingMatcherMockTest" test`
- `mvn -q "-Dtest=RuleMatcherTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只补测试注释和源码契约测试，不改变生产逻辑。
- `conversationId` 仍不是授权；intent 分数、crossBoost、embedding 相似度、关键词命中和正则命中仍只是路由证据。
- 没有打开 MCP `tools/call`、kube-manager 写入、HITL marker 创建、audit/memory 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-23 中文注释 Batch 5 第七片：Tool core 关键测试

本切片继续把测试也变成学习材料，聚焦 Tool core 关键测试文件。它们保护的是 LLM 可见工具目录、权限可见性、参数归一化、inputSchema、受保护字段过滤、组织 ID 治理和 legacy ToolCallback 安全执行等核心安全契约。

已完成：

- `ToolRegistryPromptContractTest` 补充 Prompt 可见性说明：LLM 只能看到权限过滤、脱敏后的工具目录，不应看到内部 endpoint、默认值实现或写入控制字段。
- `ToolRegistryPermissionTest` 补充可见工具目录说明：匿名、普通用户、管理员看到的 Tool 不同，但可见性仍不是最终执行授权。
- `ToolParameterNormalizerTest` 补充参数归一化说明：只做 alias/canonical 补齐，不决定身份、租户、HITL、audit、release 或写入权限。
- `ToolInputSchemaBuilderTest` 补充 inputSchema 测试边界：schema 只是 LLM 参数提示，不是权限系统。
- `ProtectedToolParameterFilterTest` 和 `ProtectedToolParameterFilterUsageContractTest` 补充受保护字段与统一过滤器架构不变量。
- `BaseToolOrganizationIdGovernanceTest` 补充租户来源说明：orgId 只能来自服务端可信上下文，缺失必须 fail-closed。
- `AtlasToolCallbackSafeExecutorTest` 补充 legacy callback 说明：兼容入口也必须委托 SafeToolExecutor，不能信任 LLM JSON 控制字段。
- 新增 `Batch5ToolCoreTestChineseCommentContractTest`，锁定这些测试级中文教学 marker。

验证：

- `mvn -q "-Dtest=Batch5ToolCoreTestChineseCommentContractTest,ToolParameterNormalizerTest,ToolInputSchemaBuilderTest,ProtectedToolParameterFilterTest,ProtectedToolParameterFilterUsageContractTest,BaseToolOrganizationIdGovernanceTest,AtlasToolCallbackSafeExecutorTest" test`
- `mvn -q "-Dtest=ToolRegistryPermissionTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只补测试注释和源码契约测试，不改变生产逻辑。
- ToolRegistry 可见性仍只是 Prompt/目录可见性，不是最终执行授权。
- 没有打开 MCP `tools/call`、kube-manager 写入、HITL marker 创建、audit/memory 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-22 中文注释 Batch 5 第六片：意图路由链路

本切片继续收尾 Batch 5 支撑层中文教学注释，聚焦自然语言进入 Agent 编排前的意图路由链路：`IntentRouter`、`IntentArbiter`、`RuleMatcher`、`EmbeddingMatcher`、`IntentsLoader` 和 `L3IntentClassifier`。

已完成：

- `IntentRouter` 补充说明：它是候选意图收集器，L1/L2/L3/L4 命中、短路和仲裁结果都只是路由建议，不是执行许可。
- `IntentArbiter` 补充说明：crossBoost、多层一致和高优意图只增强路由置信度，不能成为 Tool 授权、HITL、audit、release 或 kube-manager API 白名单。
- `RuleMatcher` 补充说明：关键词或正则命中不是权限证据，用户文本里的“删除/已确认/资源名”不能创建 HITL marker、决定 orgId/token/userId 或直接拼接到 kube-manager path/query/body。
- `EmbeddingMatcher` 补充说明：Embedding 相似度只发现语义相近意图，不访问 kube-manager、不执行 Tool、不写 audit/memory，不允许预计算失败默认命中高风险 intent。
- `IntentsLoader` 补充说明：intents.yml 是路由目录，不是 Tool 权限表、MCP manifest、kube-manager API 白名单或 Phase 2 域能力开关。
- `L3IntentClassifier` 补充说明：LLM 分类只是语义兜底增强，必须经过强类型解析、阈值、unknown 和 intent 白名单，不能动态注册能力或跳过 SafeToolExecutor。
- 新增 `Batch5IntentRoutingChineseCommentContractTest`，锁定上述中文教学 marker。

验证：

- `mvn -q "-Dtest=Batch5IntentRoutingChineseCommentContractTest,Batch5ChineseCommentContractTest,IntentArbiterTest,RuleMatcherTest,EmbeddingMatcherMockTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只补注释和源码契约测试，不改变路由算法或运行时执行路径。
- 意图路由仍只是候选信号，不能绕过 `SafeToolExecutor`。
- 没有打开 MCP `tools/call`、kube-manager 写入、HITL marker 创建、audit/memory 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-21 中文注释 Batch 5 第五片：Tool core adapter 支撑层

本切片继续收尾 Batch 5 支撑层中文教学注释，聚焦“胶水层”里最容易被误解为权限来源的对象：Tool 接口、legacy core ToolCallback、inputSchema 构建、ToolContext、结果转换、ToolResult 和默认值切面。

已完成：

- `AtlasTool` 补充中文说明：它只是最小 Tool 形状，参数是 LLM/Plan/前端整理出的候选业务输入，不是 token/orgId/userId/HITL 权威来源。
- `AtlasToolCallback` 补充 legacy Spring AI 适配器边界：LLM JSON 只做候选业务参数，必须委托 `SafeToolExecutor`，返回 JSON 只是观察/展示材料，不是 audit prewrite、release gate 或 kube-manager 写入证明。
- `ToolInputSchemaBuilder` 补充 inputSchema 边界：schema/required/aliases 只是给 LLM 的函数调用提示，不是权限系统、参数校验器或 kube-manager API 白名单。
- `AtlasToolResultConverter` 和 `AtlasToolResult` 补充结果展示边界：不能把失败改成成功，`success=true` 不能反向证明 HITL、audit、release 或后续写授权。
- `AtlasToolContext` 补充早期 Spring AI ToolContext 兼容说明：上下文 Map 不能从前端、LLM、PlanStep 或 MCP 参数里恢复权限，也不能写审计、记忆或 RAG。
- `DefaultValueAspect` 补充默认值切面边界：只补普通业务表单草稿字段，不能生成、覆盖或信任控制平面字段。
- 新增 `Batch5CoreAdapterChineseCommentContractTest`，锁定上述中文教学 marker。

验证：

- `mvn -q "-Dtest=Batch5CoreAdapterChineseCommentContractTest,Batch5ChineseCommentContractTest,Batch5SupportContractBehaviorTest,ToolInputSchemaBuilderTest,AtlasToolCallbackSafeExecutorTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只补注释和源码契约测试，不改变运行时执行路径。
- `SafeToolExecutor` 仍是唯一真实 Tool 执行边界。
- 没有打开 MCP `tools/call`、kube-manager 写入、HITL marker 创建、audit/memory 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-20 kube-manager READ 8100 smoke harness

本切片把“可以接入 8100 测试查询方法”落成一个可重复、默认安全的测试入口：`KubeManagerReadOnlySmokeTest` 默认不访问外部服务，只有显式开启并提供当前用户 token/orgId 时才会真实调用 `NodeQueryTool -> KubeManagerHttpClient -> /api/{orgId}/node`。

已完成：

- 新增 `src/test/java/com/atlas/e2e/KubeManagerReadOnlySmokeTest.java`。
- 始终运行的元数据测试锁定 smoke 目标必须是 `NodeQueryTool`、HTTP `GET`、`OperationType.READ`、无需 HITL、接口模板 `/api/{orgId}/node`。
- 源码级守卫禁止 smoke harness 引入 `.post/.put/.patch/.delete`、NIM/HPC/Slurm/BCM 二期 Tool 绑定。
- 真实 8100 smoke 由 `atlas.kube-manager.smoke.enabled=true` 或 `ATLAS_KUBE_MANAGER_SMOKE_ENABLED=true` 显式开启。
- 真实 smoke 必须提供当前用户 token 和可信 orgId；测试会绑定 `UserPermissionContext`，并传入伪造的 `organizationId/orgId/token` 参数，证明 Tool 不能信任参数里的控制面身份。
- README、路线图、变更日志、架构学习地图和恢复记忆已补充运行命令与安全边界。

验证：

- `Test-NetConnection localhost -Port 8100` 返回 `TcpTestSucceeded=False`，所以当前环境没有执行真实外部 smoke。
- `mvn -q "-Dtest=KubeManagerReadOnlySmokeTest,KubeManagerHttpClientUrlContractTest,KubeManagerHttpClientTracePropagationTest,VirtualMachineReadToolHttpContractTest" test` 通过；真实 8100 case 因未显式启用而按 JUnit assumption 跳过。
- `mvn -q "-DskipTests" validate` 通过。
- `git diff --check` 通过。

安全不变量：

- 本切片只新增测试入口，不打开生产运行时权力。
- 默认测试不会访问 kube-manager 8100；真实 smoke 需要显式开启并提供当前用户 token/orgId。
- smoke 被固定在 READ/GET/no-HITL 的节点查询 Tool，不调用写方法、MCP runtime、HITL、audit/memory 写入、retrieval/vector runtime、A2A handoff 或二期 NIM/HPC/Slurm/BCM 能力。

## 上一完成切片：M5.85-19 Orchestrator hardening 第六片：Supervisor Graph SSE 最终内容去重

本切片继续收敛主 `supervisorGraph` 的前端展示契约：direct_answer、tool_call、delegate、ReAct 事件流和 ReAct state fallback 现在共享同一个最终内容登记/去重 helper，避免同一份 answer 在多个 State key 或事件路径中重复推给前端，同时补上 direct_answer 普通回答的 SSE `content` 输出。

已完成：

- `AtlasOrchestrator` 新增 `emitSupervisorDisplayContent` 和 `registerSupervisorDisplayContent`，统一判断最终展示文本是否为空、是否为 `{}` 占位、是否已在本次 run 中发送过。
- ReAct `content` 事件在发送前先登记最终内容；后续 `react_node_result` / `answer` fallback 不会重复生成前端气泡。
- 主 `supervisorGraph` 的 Tool 展示、Tool fail-closed `answer`、delegate 展示、direct_answer `answer`、ReAct fallback 全部接入统一去重入口。
- `direct_answer` 节点现在会把已有 `answer` 明确推成 SSE `content`，解决普通问答路径只看到 thinking、不看到最终回复的展示断点。
- 补充中文教学注释，说明 SSE 去重只是展示层契约，不执行 Tool、不调用 LLM、不访问 kube-manager，不代表 HITL/audit/release/write 成功。
- `AtlasOrchestratorJsonTest` 增加反射测试：最终内容去重、空白归一、不同内容保留、空/占位拒绝，并直接验证重复内容不会再次调用 `StreamingEmitter.send`。
- `Batch3ChineseCommentContractTest` 增加 `direct_answer` / 去重 marker。

验证：

- `mvn -q "-Dtest=AtlasOrchestratorJsonTest,Batch3ChineseCommentContractTest,ToolResultMergeNodeTest,GraphToolCallSafetyGuardTest,GraphReActNodeSafetyGuardTest,GraphExecuteNodeSafetyGuardTest,SupervisorGraphReactRoutingTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只改变 SSE 展示投影和重复抑制，不打开任何新运行时权力。
- 去重后的自然语言内容不是 Tool 成功、HITL 已确认、audit 回执、release gate 或写入完成证明。
- 没有打开 MCP `tools/call`、kube-manager 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking、audit/memory 写入或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-18 Orchestrator hardening 第五片：delegate SSE 展示契约

本切片修复主 `supervisorGraph` 的 delegate 展示断点：delegate 节点返回专业 Agent 输出、缺失可信 orgId、未知 Agent 或子图异常时，现在都可以把已有 State 中的用户可见内容投影成 SSE `content`。

已完成：

- `AtlasOrchestrator` 新增 `delegateDisplayContent`：按 query/deploy/diag/rbac/storage/network result 再到 `answer` 的顺序选取 delegate 可展示内容。
- 主 `supervisorGraph` 流式回调遇到 `delegate` 节点时，会发出 `content` SSE。
- 补充中文教学注释，说明 delegate SSE 只是展示投影，不执行 Tool、不调用 LLM、不访问 kube-manager，不代表 HITL/audit/release/write 成功。
- `AtlasOrchestratorJsonTest` 增加反射测试：专业 Agent 结果优先、fail-closed answer fallback、空占位 Map 不展示。
- `Batch3ChineseCommentContractTest` 增加 delegate / fail-closed marker。

验证：

- `mvn -q "-Dtest=AtlasOrchestratorJsonTest,Batch3ChineseCommentContractTest,ToolResultMergeNodeTest,GraphToolCallSafetyGuardTest,GraphReActNodeSafetyGuardTest,GraphExecuteNodeSafetyGuardTest,SupervisorGraphReactRoutingTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只暴露已有 delegate State 给 SSE，不打开任何新运行时权力。
- delegate 自然语言内容不是 Tool 成功、HITL 已确认、audit 回执、release gate 或写入完成证明。
- 真实 Tool 调用仍必须由 ToolCallback / SafeToolExecutor 兜底校验。
- 没有打开 MCP `tools/call`、kube-manager 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking、audit/memory 写入或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-17 Orchestrator hardening 第四片：Graph merge_result 展示优先级修正

本切片修正旧 `atlasGraph` 最后一跳展示合并语义：`merge_result` 不再只看专业 Agent 输出和 `supervisor_result`，而是优先保留已有 `final_answer`、ReAct 结果和安全停止 `answer`，避免 direct_answer、ReAct 总结或 fail-closed 原因在 SSE 输出前丢失。

已完成：

- `ToolResultMergeNode` 新增明确结果优先级：`final_answer` -> `react_node_result` -> 专业 Agent result -> `answer` -> `supervisor_result` -> 兜底错误。
- 补充中文教学注释，说明 `merge_result` 只是展示投影，不执行 Tool、不调用 LLM、不访问 kube-manager，也不把文本解释为 HITL/audit/release/写入成功证据。
- 新增 `ToolResultMergeNodeTest`，覆盖 direct_answer 保留、ReAct 结果优先、安全停止原因展示、无结果兜底错误。
- 扩展 `Batch3ChineseCommentContractTest`，保护 `ToolResultMergeNode` 的中文学习 marker。

验证：

- `mvn -q "-Dtest=ToolResultMergeNodeTest,Batch3ChineseCommentContractTest,GraphToolCallSafetyGuardTest,GraphReActNodeSafetyGuardTest,GraphExecuteNodeSafetyGuardTest,SupervisorGraphReactRoutingTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片只修正展示层合并，不打开任何新运行时权力。
- `final_answer` 是 SSE 展示文本，不代表 Tool 成功、HITL 已确认、audit 已落盘、release gate 已通过或写入完成。
- 没有打开 MCP `tools/call`、kube-manager 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking、audit/memory 写入或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-16 Orchestrator hardening 第三片：Graph execute_node 可信 orgId 入口守卫

本切片把 Orchestrator hardening 推进到 Plan-and-Execute 自动执行候选入口：`execute_node` 现在会在创建 `SafeToolExecutionRequest` 前确认服务端可信 orgId，缺失时直接停止，不把租户边界问题推给 Tool 或 kube-manager。

已完成：

- `AtlasGraphConfig.buildExecuteNode` 在单步 READ Plan 候选通过参数/风险/确认检查后，先从 Graph State 读取 orgId。
- Graph State orgId 为空时，`execute_node` 只允许从服务端 `UserPermissionContext` ThreadLocal 恢复可信 orgId。
- 如果仍缺失可信 orgId，写入 `EXECUTE_TRUSTED_ORG_MISSING`、`execute_result.executed=false`、stepId、source 和用户可见 answer，不创建 `SafeToolExecutionRequest`。
- `SafeToolExecutionRequest` 现在使用解析后的 `orgId`，而不是再次直接读 State，更不会从 `PlanStep.parameters` 提取租户上下文。
- 新增 `GraphExecuteNodeSafetyGuardTest`，锁定 orgId 恢复、缺失 orgId 在 request 创建前阻断、request 构造不读取 Plan 参数作为控制上下文。

验证：

- `mvn -q "-Dtest=GraphExecuteNodeSafetyGuardTest,M42PlanExecuteSafetyContractTest,SafeToolExecutorTest,ProtectedToolParameterFilterUsageContractTest,SupervisorGraphReactRoutingTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片收紧 Plan 自动执行入口，不扩大原有单步 READ-only gate。
- PlanStep 参数仍是不可信业务输入，不能提供 orgId、token、userId、conversationId、HITL、audit、release 或 write authority。
- 没有打开 MCP `tools/call`、kube-manager 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking、audit/memory 写入或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-15 Orchestrator hardening 第二片：Graph ReAct 节点入口守卫

本切片把 Orchestrator hardening 从普通 Graph `tool_call` 推进到手写 ReAct 诊断路径：Graph `react_node` 现在会把服务端 trace 传入 ReActEngine，并在缺少可信 orgId 时于调用 LLM / Tool / kube-manager 前提前停止。

已完成：

- `AtlasGraphConfig.buildReActNode` 从 Graph State 读取 `traceId`，并写入 ReAct `initialParams`，让 ReAct 事件、SafeToolExecutor 和后续审计/观测使用同一条服务端 trace。
- `react_node` 在 Graph State 缺少 orgId 时，会尝试从服务端 `UserPermissionContext` ThreadLocal 恢复；如果仍缺失，则返回 fail-closed 更新，不调用 LLM、不执行 Tool、不访问 kube-manager。
- 新增 `failClosedGraphReActUpdates`：写入 `answer`、`react_node_result`、`tool_error_code`、`execute_result.executed=false`、source 和 traceId，方便 SSE、测试和学习文档解释“为什么没有进入 ReAct”。
- 补充 ReAct 节点中文教学注释，明确 `initialParams` 是服务端可信上下文，LLM 每轮 `Action.params` 只是候选业务输入。
- 新增 `GraphReActNodeSafetyGuardTest`，锁定 trace 注入、缺失 orgId 在 `engine.runWithEvents` 前阻断、未执行结果不伪装成 `tool_result`。

验证：

- `mvn -q "-Dtest=GraphReActNodeSafetyGuardTest,M523TracePropagationContractTest,ReActEngineParamMergeTest,ReActEngineMultiStepE2ETest,SupervisorGraphReactRoutingTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`，仅有 Windows LF-to-CRLF 提示。

安全不变量：

- 本切片收紧 ReAct 入口，不打开任何新运行时权力。
- `traceId` 只用于可观测链路关联，不是 Tool 业务参数、身份、租户、HITL、audit 或 release authority。
- ReAct 缺少可信 orgId 时不会先调用 LLM 或 Tool 再补救；必须 fail-closed。
- 没有打开 MCP `tools/call`、kube-manager 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking、audit/memory 写入或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-14 Orchestrator hardening 第一片：Graph tool_call 入口守卫

本切片把 Orchestrator hardening 从文档计划推进到代码：Graph `tool_call` 在创建 `SafeToolExecutionRequest` 前新增一层快速 fail-closed 守卫，让明显不可信的候选调用不会进入统一执行器。

已完成：

- `AtlasGraphConfig` 新增 `guardGraphToolCallCandidate`：空 Tool 目标、缺失可信 orgId、参数中出现受保护控制字段时直接返回未执行状态。
- `AtlasGraphConfig` 新增 `failClosedGraphToolCallUpdates`：写入 `answer`、`tool_error_code`、`execute_result.executed=false`、source 和 traceId，形成可展示、可测试的阻断证据。
- Graph `tool_call` 在 Graph State orgId 为空时可从服务端 ThreadLocal 恢复可信 orgId，但没有可信 orgId 时仍 fail-closed。
- `AtlasOrchestrator` 主 `supervisorGraph` 流和实验 `/chat/graph` 流都能在没有 `tool_result` 时把安全停止原因通过 SSE 发给前端。
- 新增 `GraphToolCallSafetyGuardTest`，覆盖缺失 target、缺失 orgId、嵌套受保护字段拒绝、普通业务参数继续放行到 `SafeToolExecutor` 的契约。
- 修正 `PlanEngine` 注释中的脆弱源码契约误判，不改变运行时行为。

验证：

- `mvn -q "-Dtest=GraphToolCallSafetyGuardTest,SupervisorGraphReactRoutingTest,M513HitlFailClosedContractTest,M42PlanExecuteSafetyContractTest,ProtectedToolParameterFilterUsageContractTest" test`

安全不变量：

- 本切片收紧 Graph `tool_call`，不打开任何新运行时权力。
- `SafeToolExecutor` 仍是最终真实 Tool 执行边界；Graph 守卫只是更早留下 fail-closed 证据。
- token、orgId、userId、conversationId、traceId、HITL、audit、release、writeAllowed 等字段不能通过 `BrainDecision.parameters` 伪造成控制面事实。
- 没有打开 MCP `tools/call`、kube-manager 写入、retrieval/vector runtime、A2A handoff、依赖升级、CI blocking、audit/memory 写入或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-13 中文注释 Batch 5 第四片：分析/目录 query helper

本切片完成虚拟机、镜像目录、产品报价、行业应用和成本账单分析类 query helper 的中文教学注释，并补充 helper 行为契约测试。

已完成：

- `VirtualMachineQuerySupport` 补充 VM 名称会进入 URL path，必须先做 path segment 校验和编码；VM 名称只是定位符，不打开启动、停止、删除等写操作。
- `RepositoryCatalogQuerySupport` 补充 repository catalog/tag 是敏感只读目录；NIM 相关筛选只代表目录过滤，不是镜像拉取、部署创建、NIM 创建或二期运行时恢复。
- `SaleProductQuerySupport` 补充 sale 域涉及价格、订单和支付；helper 只构造只读 GET query，金额预估不是下单、支付、续费或报价确认。
- `IndustryAppQuerySupport` 补充行业应用模板、实例和 API 历史查询只构造 read query/path，不创建实例、不调用 API、不重放历史、不传 requestBody。
- `FinancialAnalysisQuerySupport` 补充成本/账单是敏感只读能力，query 字段不能表达身份、租户、支付、写入或 release 状态。
- 新增 `Batch5AnalysisQuerySupportChineseCommentContractTest` 和 `AnalysisQuerySupportContractTest`。

验证：

- `mvn -q "-Dtest=Batch5AnalysisQuerySupportChineseCommentContractTest,AnalysisQuerySupportContractTest,VirtualMachineReadToolHttpContractTest,RepositoryCatalogToolHttpContractTest,SaleProductAnalysisToolHttpContractTest,IndustryAppAnalysisToolHttpContractTest,FinancialAnalysisToolHttpContractTest" test`

安全不变量：

- 本切片只改注释、测试和文档，不改变运行时行为。
- analysis/catalog helper 输出仍是 GET-only query/path 过滤，不是 Tool 权限、敏感读取确认、订单/支付、部署创建、NIM runtime、release 或 Phase 2 授权。
- 没有打开 Tool 执行、MCP runtime、kube-manager 调用语义变化、LLM/Embedding 运行时变化、retrieval/vector runtime、audit/memory 写入、CI blocking、订单/支付/部署/NIM runtime 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-12 中文注释 Batch 5 第三片：query/path/body 支撑 helper

本切片完成 kube-manager query/path/body 支撑 helper 的中文教学注释，并补充 helper 行为契约测试。

已完成：

- `DownloadTaskQuerySupport` 补充下载任务 ID 会进入 kube-manager URL path，必须只接受成熟后端返回的正整数 ID；任务 ID 只是定位符，不是授权凭证。
- `CoursewareQuerySupport` 补充 coursewareId 只是课件资源定位符，不代表课程访问授权；读取仍依赖当前可信用户、组织上下文和 kube-manager 权限。
- `TemplateQuerySupport` 补充模板 ID 必须来自模板列表等成熟后端返回结果，不能来自 LLM 自行构造的路径片段。
- `TensorBoardQuerySupport` 补充 deploymentId 只是 TensorBoard 资源定位符，不代表用户有权读取训练任务 runs。
- `FileStorageQuerySupport` 补充文件/存储参数只做必填非空收敛，不是路径安全证明；具体 Tool 还应结合后端白名单、ID 形状或可选项来源继续校验。
- `UserRiskMutationSupport` 补充用户启用、禁用、删除、充值是高风险写操作；目标用户 ID 和充值 body 只是业务参数，不是写授权；body 只能保留 `userId`、`amount`、`remark`。
- 新增 `Batch5QuerySupportChineseCommentContractTest` 和 `QuerySupportPathAndBodyContractTest`，强化 `HighRiskMutationToolHttpContractTest` 充值 body 白名单。

验证：

- `mvn -q "-Dtest=Batch5QuerySupportChineseCommentContractTest,QuerySupportPathAndBodyContractTest,DownloadTaskProgressToolHttpContractTest,HighRiskMutationToolHttpContractTest" test`

安全不变量：

- 本切片只改注释、测试和文档，不改变运行时行为。
- path/query/body helper 输出仍只是 kube-manager HTTP 前的轻量参数收敛，不是权限、HITL、audit、release、MCP runtime、kube-manager 写入或部署授权。
- 没有打开 Tool 执行、MCP runtime、kube-manager 调用语义变化、LLM/Embedding 运行时变化、retrieval/vector runtime、audit/memory 写入、CI blocking 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-11 中文注释 Batch 5 第二片：支撑契约层

本切片完成 Tool 参数 schema、统一执行结果、执行来源、trace 上下文、意图分类、默认值补参和 Tool 异常等支撑契约层中文教学注释，并补充行为契约测试。

已完成：

- `ToolParameterSpec` 补充参数 schema / aliases 只是业务输入形状说明，不负责身份、权限、HITL、审计、release、token/orgId 或 HTTP 授权。
- `SafeToolExecutionResult` 补充 `executed`、`success`、`requiresClarification` 和 Graph updates 的区别：执行回执不是新的权限来源，`success=true` 不能反向证明 HITL、durable audit、release gate 或 kube-manager 权限。
- `SafeToolExecutionSource` 补充来源只用于审计、诊断和未来策略收敛，不能因来自 Graph / Plan / ReAct / ToolCallback / fallback 而绕过统一安全执行链。
- `AgentTraceContext` 补充 traceId 是日志、SSE、审计、OTel 和回放关联键，不是身份、租户、Session、审计 receipt、release evidence 或 Tool 授权；外部候选值必须过滤以防日志/header 注入和线程池 MDC 污染。
- `IntentDefinition`、`IntentResult`、`ScoreNormalizer`、`EmbeddingConfig`、`L3ClassificationResult` 补充意图配置、分数和 LLM 自报结果只是路由/观测候选，不能注册 Tool、导出 MCP、批准写操作或授予 release authority。
- `IntentDefaults`、`DefaultValueRegistry`、`DefaultValueApplier` 补充默认值只是普通表单草稿补参，不能生成 token/orgId/userId/HITL/audit/release/writeAllowed/admin 等控制事实。
- `AtlasToolValidationException`、`PermissionDeniedException` 补充错误码/建议/权限拒绝字段用于澄清、提示和审计，不应泄露 token、raw endpoint、跨租户资源或内部栈，也不能被 LLM/前端/HITL clarify 自动解除。
- 新增 `Batch5SupportContractBehaviorTest`，扩展 `Batch5ChineseCommentContractTest`。

验证：

- `mvn -q "-Dtest=Batch5ChineseCommentContractTest,Batch5SupportContractBehaviorTest,AgentTraceContextTest,M521DefaultValueSafetyContractTest,DefaultValueRegistryTest" test`

安全不变量：

- 本切片只改注释、测试和文档，不改变运行时行为。
- schema / result / source / trace / intent / defaults / exception 都是支撑契约，不是权限、发布、HITL、审计、MCP runtime、kube-manager 写入或部署授权。
- 没有打开 Tool 执行、MCP runtime、kube-manager 调用、LLM/Embedding 运行时变化、retrieval/vector runtime、audit/memory 写入、CI blocking 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-10 中文注释 Batch 5 首批：支撑层地基

本切片完成 DTO / store / config 支撑层中最关键地基文件的中文教学注释，并增加源码契约测试保护学习 marker。

已完成：

- `ApiResponse` 补充“前端展示契约不是权限契约”：`success=true` 不代表 Tool 已授权、HITL 已确认、eval/release 已通过或 kube-manager 写操作可执行。
- `LoginRequest`、`LoginResponse`、`SessionData` 补充登录请求 orgId 只是 kube-manager 登录候选参数、sessionId 不是 kube-manager JWT、token 不能返回前端或进入日志/Memory/RAG/prompt/eval 的说明。
- `Conversation`、`ConversationItemDto`、`ConversationDetailDto` 补充 conversation 是会话元数据，不是 Agent trace、审计证据、长期记忆或 RAG 文档来源；conversationId 只是资源定位符，不是授权凭证。
- `SessionStore` 补充服务端可信身份上下文、SecureRandom sessionId、token 日志禁止、orgId 不可来自前端 claim 的说明。
- `ConversationStore` 补充 owner 收敛、用户会话隔离、标题/消息计数不能作为 prompt/eval/release 证据的说明。
- `AtlasAsyncConfig` 补充异步线程 token/orgId 二元组传播、任务结束恢复旧值、不能从请求体/LLM/前端字段推导身份的说明。
- `AtlasConfiguration` 补充 L1 Embedding / L3 LLM 是可选增强，失败时降级；意图分类不能绕过 `SafeToolExecutor`、HITL、审计、kube-manager 权限、Memory/RAG source custody 或 release gate。
- 新增 `Batch5ChineseCommentContractTest`，保护支撑层中文教学 marker。

验证：

- `mvn -q "-Dtest=Batch5ChineseCommentContractTest,ConversationStoreTest,ConversationControllerTest,AuthControllerLoginFailSafeTest" test`
- `mvn -q "-DskipTests" validate`
- `git diff --check`

安全不变量：

- 本切片只改注释、测试和文档，不改变运行时行为。
- API success、sessionId、conversationId、登录请求 orgId、意图分类结果、异步上下文传播都不是执行/发布/写入权限事实。
- 没有打开 Tool 执行、MCP runtime、kube-manager 调用、LLM/Embedding 运行时变化、retrieval/vector runtime、audit/memory 写入、CI blocking 或二期 NIM/HPC/Slurm/BCM 权力。

## 上一完成切片：M5.85-9 中文注释 Batch 4：证据链

本切片完成 Memory / RAG / Audit / Observability / Replay / Eval 证据链的中文教学注释，并增加源码契约测试保护学习 marker。

已完成：

- `MemoryRagSourceEvidenceDigestDeriver`、`MemoryRagSourceEvidenceInput`、`MemoryRagSourceEvidenceDigestResult` 补充来源/片段/完整证据 digest 分层、稳定证据指纹、raw marker 拦截、citationSeed 不是 prompt 权威等说明。
- `AgentAuditRecorder`、`AgentAuditEventFactory`、`InMemoryAgentAuditRecorder`、`JsonlAgentAuditDurableSink`、`JsonlAgentAuditQueryService` 补充 durable audit、redacted-only、参数存在性摘要、JSONL 有界反向扫描和高风险 prewrite fail-closed 说明。
- `ObservabilityController` 补齐 admin-only 只读控制面说明，修复 replay endpoint 乱码注释，并明确 Memory/RAG、Audit、Replay、Eval 入口不执行 Tool/MCP/kube-manager、不调用 LLM、不改变 prompt、不授予 release authority。
- `AgentReplayTimelineService`、`AgentReplayTimelineResponse`、`AgentReplayTimelineStep` 补充 replay 是脱敏时间线渲染，不是重新执行。
- `AgentEvalReportService`、`AgentEvalReportResponse` 补充 deterministic eval 只读 redacted replay DTO，不参与 Tool 放行，不授予 release authority。
- `AgentReviewedEvalTraceEvidenceService`、`AgentMemoryRagReadinessService`、`AgentMemoryRagSourceEvidenceDigestContractService` 补充 reviewed trace、Memory/RAG readiness 和 digest contract 的只读边界。
- 新增 `Batch4ChineseCommentContractTest`，保护证据链中文教学 marker，并检查触碰文件中常见 mojibake 片段。

验证：

- `mvn -q "-Dtest=Batch4ChineseCommentContractTest,MemoryControllerTest,ConversationSummaryMemoryStoreTest,MemoryRagSourceEvidenceDigestDeriverTest,AgentAuditRecorderTest,JsonlAgentAuditDurableSinkTest,ObservabilityControllerTest,ObservabilityControllerSecurityContractTest,AgentReplayTimelineServiceTest,AgentEvalReportServiceTest,AgentReviewedEvalTraceEvidenceServiceTest,AgentMemoryRagReadinessServiceTest,AgentMemoryRagSourceEvidenceDigestContractServiceTest" test`
- `mvn -q "-DskipTests" validate`

安全不变量：

- 本切片只改注释、测试和文档，不改变运行时行为。
- Memory/RAG digest 是证据指纹，不是内容真实性证明、检索运行时绑定或 prompt 引用授权。
- Audit durable JSONL 只存 redacted evidence，不保存 raw principal、raw reason、raw endpoints 或 raw parameter values。
- Replay 只是只读脱敏审计时间线，不重新执行 Tool/MCP/kube-manager。
- Eval 只是确定性治理信号，不调用 LLM/Tool/MCP/kube-manager，不参与 Tool 放行，不授予 release authority。

## 上一完成切片：M5.85-8 中文注释 Batch 3：编排链路

本切片完成 Orchestrator / Graph / ReAct / Plan 编排链路的中文教学注释，并增加源码契约测试保护学习 marker。

已完成：

- `AtlasOrchestrator` 补充 SSE 入口、可信 RuntimeIdentity、supervisorGraph streaming、clarify-vs-HITL、Graph State 运行期对象边界、conversationId 归属校验和 fail-closed 说明。
- `AtlasGraphConfig` 补充 StateGraph 状态合并策略、`BrainDecision.ActionType` 条件边、`plan_node`、`execute_node`、`react_node`、`tool_call`、delegate 子图、ThreadLocal 恢复和 SafeToolExecutor 双层边界说明。
- `ReActEngine` / `ReActPromptBuilder` / `ReActMemory` 补充 Thought -> Action -> Observation、LLM Action JSON 只是候选业务参数、受保护字段过滤、展示脱敏、短期 ReAct 记忆和真实 Tool 出口说明。
- `PlanEngine` 补充 Plan 是计划证据、不是执行许可；当前不调用 Tool、不访问 kube-manager、不写 audit、不创建 HITL marker。
- `AtlasToolCallback`、`DirectAnswerNode`、`SseEmitNode`、`ToolResultMergeNode` 补充桥接、展示和合并边界说明。
- 新增 `Batch3ChineseCommentContractTest`，保护编排链路中文教学 marker。

验证：

- `mvn -q "-Dtest=Batch3ChineseCommentContractTest,SupervisorGraphReactRoutingTest,AtlasOrchestratorRuntimeIdentityTest,AtlasOrchestratorJsonTest,ReActEnginePolicyTest,ReActEngineParamMergeTest" test`
- `mvn -q "-DskipTests" validate`

安全不变量：

- 本切片只改注释、测试和文档，不改变运行时行为。
- Graph 条件边只决定路由，不授予执行权。
- ReAct Action JSON 和 PlanStep 仍是候选业务输入，不是身份、租户、HITL、audit、release 或写入授权。
- Graph State 不应保存 SseEmitter、Lambda 等运行期对象；运行期事件通过 registry 旁路传递。
- 真实 Tool 执行仍必须进入 `SafeToolExecutor`。

## 上一完成切片：M5.85-7 全代码审计、边界修复与学习文档

本切片对当前已完成后端代码做了一轮 review，并把文档重新对齐到代码事实。

已完成：

- 修复 HITL confirm / clarify：先读取 checkpoint 并校验当前 principal、user_id、checkpoint orgId、当前 principal orgId，再消费 confirmToken 或删除 pending 决策，避免跨用户或跨租户失败请求破坏他人待确认状态。
- 修复登录组织上下文：前端请求中的 `organizationId` 只作为 kube-manager 登录参数；本地 Session 只接受 kube-manager 响应字段或本次 token 反查得到的可信 orgId。
- 修复 Bearer-only 上下文传播：`UserPermissionContext` 登录缓存保存可信 orgId，`bind(token)` 能恢复 token+orgId 原子上下文，并清理陈旧 ThreadLocal。
- 收紧 `/api/agent/mcp/**` 为 admin-only，与 readiness、文档和治理面语义一致。
- 高风险写默认 fail-closed：durable audit 缺失时，即使 HITL 确认匹配也不能执行 CREATE/UPDATE/DELETE/ACTION。
- MCP manifest 增加 NIM/HPC/Slurm/BCM 二期域阻断，避免一期导出暂停专家域能力。
- 新增 `docs/learning/以kube-agent为例的顶级Agent开发学习指南.md`，把当前项目作为学习顶级 Agent 的案例。
- 修正 README、路线图、文档索引、文档治理、ADR、认证设计和 Tool 规范中的过时或错误表述。

安全不变量：

- confirmToken 不是 owner 事实，必须由 checkpoint + 当前 principal + 双侧 orgId 一致性共同证明。
- 请求体里的 userId/orgId/role/HITL/confirmed/audit/release 字段都不能成为执行权威。
- MCP manifest/governance 是管理员治理读模型，不是普通用户能力目录，也不是 tools/call。
- Memory 当前是 caller-submitted bounded summary + best-effort redaction，不是可信 RAG prompt authority。
- Phase 2 NIM/HPC/Slurm/BCM 仍暂停；历史代码可以保留，但一期 runtime authority 和 MCP export 都关闭。

## 上一文档清理切片：M5.85-5 / M5.85-6

已完成：

- `README.md`、`开发路线图.md`、`docs/文档索引.md`、`docs/文档治理规则.md` 作为当前主线入口。
- 主线文档改为中文文件名，保留 `README.md` / `AGENTS.md` 这两个约定入口。
- 旧 M4/M5 波次报告、旧 `docs/v3.1/**`、重复 review log、旧会话快照、旧根目录审计报告、旧文档治理报告和过期技术栈审计已从当前 docs 树移除。
- 主线文档镜像到 `codex-memory/kube-agent/current`。

## 最近代码学习批次

### M5.85-10：中文注释 Batch 5 首批

覆盖 DTO / store / config 支撑层地基：

- `ApiResponse`
- `LoginRequest`
- `LoginResponse`
- `SessionData`
- `Conversation`
- `ConversationItemDto`
- `ConversationDetailDto`
- `SessionStore`
- `ConversationStore`
- `AtlasAsyncConfig`
- `AtlasConfiguration`
- `Batch5ChineseCommentContractTest`

学习重点：

- 前端响应成功不等于权限成功。
- 登录请求体里的 organizationId 只是候选参数，不是本地可信租户事实。
- sessionId 是 kube-agent 会话句柄，不是 kube-manager JWT。
- conversationId 是资源定位符，不是授权凭证。
- 异步线程必须传播服务端可信 token/orgId 快照，并在任务结束后恢复旧值。
- LLM/Embedding 意图分类是候选智能能力，不是 Tool 执行许可。

### M5.85-9：中文注释 Batch 4

覆盖 Memory / RAG / Audit / Observability / Replay / Eval 证据链：

- `MemoryController`
- `ConversationSummaryMemoryStore`
- `MemoryRagSourceEvidenceDigestDeriver`
- `MemoryRagSourceEvidenceInput`
- `MemoryRagSourceEvidenceDigestResult`
- `AgentAuditRecorder`
- `AgentAuditEventFactory`
- `InMemoryAgentAuditRecorder`
- `JsonlAgentAuditDurableSink`
- `JsonlAgentAuditQueryService`
- `ObservabilityController`
- `AgentReplayTimelineService`
- `AgentReplayTimelineResponse`
- `AgentReplayTimelineStep`
- `AgentEvalReportService`
- `AgentEvalReportResponse`
- `AgentReviewedEvalTraceEvidenceService`
- `AgentMemoryRagReadinessService`
- `AgentMemoryRagSourceEvidenceDigestContractService`
- `Batch4ChineseCommentContractTest`

学习重点：

- Memory 当前是 caller-submitted bounded summary，不是可信 RAG 证据源。
- Memory/RAG digest 是 source/chunk/evidence 三层证据指纹，不读取文档、不执行检索、不调用向量库/LLM/MCP/kube-manager。
- Audit 事件只保存参数存在性摘要和 redacted durable evidence，不保存原始参数值。
- Replay 是 admin-only 脱敏时间线渲染，不重新执行 Tool。
- Eval 只读 redacted replay DTO，是治理信号，不参与 Tool 放行，也不授予 release authority。

### M5.85-8：中文注释 Batch 3

覆盖 Orchestrator / Graph / ReAct / Plan 编排链路：

- `AtlasOrchestrator`
- `AtlasGraphConfig`
- `ReActEngine`
- `ReActPromptBuilder`
- `ReActMemory`
- `PlanEngine`
- `AtlasToolCallback`
- `DirectAnswerNode`
- `SseEmitNode`
- `ToolResultMergeNode`
- `Batch3ChineseCommentContractTest`

学习重点：

- SSE 入口必须先得到服务端可信 RuntimeIdentity，不能信任请求体 userId/orgId。
- Graph 是状态机和路由表，不是执行授权系统。
- `plan_node` 产出计划证据；`execute_node` 只允许极窄的单步 READ 候选，并继续委托 `SafeToolExecutor`。
- ReAct 的 Thought -> Action -> Observation 中，Action JSON 只是 LLM 候选参数。
- ReAct 时间线和记忆要做展示脱敏，不能保存 token/orgId 等受保护字段。
- delegate 子图必须显式绑定并恢复 ThreadLocal。

### M5.85-4：中文注释 Batch 2

覆盖 Tool / MCP / kube-manager 执行边界：

- `SafeToolExecutor`
- `SafeToolExecutionRequest`
- `ToolRegistry`
- `ProtectedToolParameterFilter`
- `McpToolManifestService`
- `McpManifestController`
- `KubeManagerHttpClient`
- `KubeManagerHttpResiliencePolicy`
- `Batch2ChineseCommentContractTest`

学习重点：

- LLM、Plan、前端参数只是候选业务输入。
- token、orgId、userId、HITL marker、audit receipt、release decision、writeAllowed、trace 字段必须来自服务端可信证据。
- `SafeToolExecutor` 是进入真实 `BaseTool.execute` 的统一边界。
- MCP Manifest 是 admin-only 的只读治理目录，不是普通用户能力目录，也不是 `tools/call` 执行授权。
- kube-manager HTTP 是真实外部网络出口，业务请求禁止透明降级到 sysadmin token。
- 读重试和写重试不是同一类风险。

### M5.85-3：中文注释 Batch 1

覆盖安全入口和 HITL 边界：

- `AgentSecurityConfig`
- `AuthTokenFilter`
- `AgentPrincipal`
- `AgentPrincipalResolver`
- `UserPermissionContext`
- `AuthController`
- `HITLController`
- `HitlGuard`
- `Batch1ChineseCommentContractTest`

学习重点：

- kube-manager session 如何进入 kube-agent。
- `AgentPrincipal` 是服务端只读身份快照，不是前端 claim。
- HITL confirm 创建服务端 marker，clarify 只是补充上下文。
- `HitlGuard` fail-closed，但它只是完整 Tool 安全链的一环。

### M5.85-2：中文代码注释规则

已写入 `AGENTS.md` 和全局 Codex 规则。所有新增/修改代码都要优先补中文教学注释，尤其是类、接口、record、Controller endpoint、Service 编排入口、复杂私有方法、测试和安全边界。

## 当前安全边界记忆

- `/api/agent/**` 当前已从临时 permitAll 收敛到 authenticated/admin 保护路径。
- Observability、Eval、Memory/RAG、Top-tier、MCP governance 目前主要是 admin-only 读模型；Memory summaries 是 authenticated 用户级摘要接口。
- MCP 只开放 admin-only 只读 Manifest/governance，不开放 runtime `tools/call`，且阻断二期域导出。
- A2A 目前是 provenance / compatibility / review 证据，不开放 runtime handoff。
- Memory/RAG 目前是契约、证据读模型和 caller-submitted bounded summary；retrieval prompt influence、vector store、embedding/reranker/GraphRAG runtime 仍关闭。
- Eval 目前有 admin-only deterministic replay/eval 入口和 gate evidence；CI blocking、LLM eval、Memory/RAG retrieval eval runtime 仍需 reviewed trace evidence。
- kube-manager 高风险写操作默认 fail-closed，必须具备 HITL、幂等、durable audit prewrite、post-write readback、release gate 和恢复记忆证据。

## 下一步恢复指令

如果新会话要继续本项目，优先按这个顺序读：

1. `codex-memory/kube-agent/current/当前恢复状态.md`
2. `README.md`
3. `开发路线图.md`
4. `docs/文档索引.md`
5. `docs/顶级Agent架构与技术学习地图.md`
6. `AGENTS.md`

下一步开发建议：

1. 继续 Orchestrator hardening：检查 `execute_node`、`react_node`、delegate 子图和 SSE result merge 的安全停止语义是否一致。
2. 继续中文注释 Batch 5 收尾：只补仍承载隐性控制面字段的剩余 support/config/test helper，不大规模重写。
3. 运行 `mvn -q "-DskipTests" validate`。
4. 更新 `变更日志.md`、本文件、架构学习文档和 `codex-memory`。
5. 提交并推送。
