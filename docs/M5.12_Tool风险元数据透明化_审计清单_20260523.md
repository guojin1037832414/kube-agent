# M5.12 Tool 风险元数据透明化前后端同步治理 — 审计清单

生成时间：2026-05-23 21:06 CST

## 1. 后端交付物

| 路径 | 状态 | 说明 |
|---|---|---|
| `src/main/java/com/atlas/tool/core/ToolRegistry.java` | PASS | `ToolMetadata` 接收风险字段；Prompt 展示风险摘要且不展示 endpoint。 |
| `src/main/java/com/atlas/react/ReActEvent.java` | PASS | `toolStart/toolDone` 支持 extra metadata，旧方法兼容。 |
| `src/main/java/com/atlas/react/ReActEngine.java` | PASS | SSE tool 事件透传 `httpMethod/operationType/requiresConfirmation`。 |
| `src/test/java/com/atlas/tool/core/ToolRegistryPromptContractTest.java` | PASS | 覆盖风险标签展示、endpoint 不泄露。 |
| `src/test/java/com/atlas/react/ReActEventRiskMetadataTest.java` | PASS | 覆盖 `params/costMs` 与风险 metadata 合并。 |

## 2. 前端交付物

| 路径 | 状态 | 说明 |
|---|---|---|
| `/mnt/f/gitProject/kube-agent-vue/src/types/index.ts` | PASS | 新增 Tool 风险元数据类型。 |
| `/mnt/f/gitProject/kube-agent-vue/src/components/ChatBubble.vue` | PASS | ReAct 时间线展示风险 chip、建议确认、DELETE/PLACEHOLDER 提示。 |

## 3. 验证记录

| 验证项 | 命令 | 结果 |
|---|---|---|
| 后端定向测试 | `mvn -q -Dtest=ToolRegistryPromptContractTest,ReActEventRiskMetadataTest test` | PASS |
| 前端类型检查+构建 | `npm run build` | PASS |
| 后端 diff 空白检查 | `git diff --check` | PASS |
| 前端 diff 空白检查 | `git diff --check` | PASS |

## 4. 安全边界审计

- PASS：Prompt 不展示 `/api/...` endpoint。
- PASS：SSE metadata 不透传 `apiEndpoints`。
- PASS：未执行真实删除/修改类 kube-manager 请求。
- PASS：M5.12 文档明确“风险透明化不是强安全边界”。
- DEFERRED：M5.13 需实现 fail-closed HITL 强拦截。

## 5. 结论

M5.12 小样本前后端同步治理通过。当前交付可作为 M5.13 HITL 强拦截的元数据与 UI 基础。
