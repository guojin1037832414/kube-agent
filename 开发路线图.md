# Atlas Kube-Agent 当前开发路线图

> 最后更新：2026-07-07
> 目标：一期就完成顶级 Agent Core。NIM / HPC / Slurm / BCM 只是二期暂停，不代表一期降级。

## 总原则

1. 先做安全、证据和可恢复，再打开运行时权力。
2. Java / Spring 继续作为 Phase 1 主控制平面；先进技术通过官方来源审查、兼容矩阵、测试证据和 release gate 引入。
3. 所有新增或修改代码都优先补中文教学注释，解释设计意图、输入来源、输出去向和禁止动作。
4. 每个有意义切片都要更新 `codex-memory/kube-agent/current`，运行校验，提交并推送。
5. 当前线程聚焦后端；前端由独立会话推进，但后端需要持续提供只读契约和学习文档。

## 已形成的 Phase 1 核心能力

| 能力块 | 当前状态 |
|---|---|
| 身份与权限入口 | kube-manager 登录/session bridge、`AgentPrincipal`、Spring Security、ThreadLocal 兼容桥已具备，并已补第一批中文教学注释。 |
| Tool 执行边界 | `SafeToolExecutor`、ToolRegistry、受保护参数过滤、HITL、kube-manager HTTP outlet 已成为统一执行边界，并已补第二批中文教学注释。 |
| Graph / ReAct / Plan | StateGraph、AtlasBrain、ReAct、Plan/execute 最小闭环已存在，并已补第三批中文教学注释；M5.85-14 已完成 Graph `tool_call` 入口守卫，M5.85-15 已完成 Graph `react_node` 的可信 trace 传递和缺失 orgId 提前 fail-closed，M5.85-16 已完成 `execute_node` 的可信 orgId 入口守卫，M5.85-17 已修正旧 `atlasGraph` 的 `merge_result` 展示优先级，M5.85-18 已补主 `supervisorGraph` delegate 结果 / fail-closed 原因 SSE 展示契约，M5.85-19 已收敛主 `supervisorGraph` direct_answer/tool/delegate/ReAct 最终内容去重与展示契约；M5.85-40 已补终极 Agent 多入口执行矩阵质量门，锁定 Graph `tool_call`、Plan `execute_node`、ReAct Action、Graph Bridge ToolCallback、legacy core ToolCallback 和 Orchestrator fallback 都只能委托 `SafeToolExecutor` 并保留来源审计。 |
| Memory / RAG / Audit / Replay / Eval | 已补第四批中文教学注释，覆盖来源证据摘要、caller-submitted memory 边界、durable audit redacted-only、replay 只读时间线、deterministic eval 不授予 release authority；M5.85-26 已补 Eval trace catalog / candidate discovery / curation review / patch proposal 这条 release-gate 证据链教学注释；M5.85-28 已补 trace-set promotion workflow / workbench wrapper 教学注释，明确 `readyForGitReview` 只是人审 Git review 信号，不是目录写权限；M5.85-30 已新增 reviewed redacted trace fixture intake 只读合同；M5.85-33 已新增 reviewed fixture manifest；M5.85-34 已新增 reviewed fixture template/schema；M5.85-39 到 M5.85-42 已把 reviewed fixture readiness 和质量门投影到 workbench review；M5.85-43 已新增手动 reviewed fixture candidate preview；M5.85-44 已新增自动 candidate workbench，把 redacted candidate discovery 的首个推荐 trace 接到 candidate preview，仍不接受 caller traceId、不开放上传、目录写或 CI/release 权力。 |
| DTO / Store / Config / Contract / Helper 支撑层 | 已补第五批前八片中文教学注释，覆盖 API 响应、登录请求/响应、SessionData、Conversation DTO、SessionStore、ConversationStore、异步上下文配置、全局意图配置、Tool 参数 schema、统一执行结果、执行来源、trace 上下文、意图分类契约、默认值补参、Tool 异常、query/path/body helper、分析/目录类 GET query helper、Tool core adapter 层、意图路由链路、Tool core 关键测试，以及 config/store/intent 支撑测试的教学注释。 |
| MCP 治理 | 当前只开放 admin-only 只读 Manifest 和治理读模型，不开放 `tools/call`；二期域 Tool 不进入一期导出清单。 |
| kube-manager outlet | 已新增并真实跑通 opt-in `8100` READ smoke harness；默认测试不访问真实后端，可显式提供 token/orgId，也可用 username/password 登录型 smoke 在进程内获取临时 token、反查可信 orgId 后验证 6 条 GET/READ/no-HITL 链路：节点列表、节点剩余资源、Dashboard 部署/镜像/EasyFlow 统计和 EasyFlow 列表。M5.85-38 已把同一批只读链路跑过 `SafeToolExecutor` 真实 Agent 执行边界，并补代表性 `AtlasToolCallback -> SafeToolExecutor` 真后端调用与审计断言；M5.85-40 将 opt-in smoke 读超时改为可配置、默认 30 秒，并用本地 `localhost:8100` 登录型 smoke 再次跑通直接 Tool、`SafeToolExecutor` 与 ToolCallback 只读链路。高风险写默认要求 ready durable audit prewrite，写重试继续受 idempotency、audit、HITL、release evidence 约束。 |
| Observability / Eval / Memory-RAG | 已有大量 admin-only 读模型和确定性 replay/eval 入口；reviewed trace fixture 已从 intake 合同推进到 repo/classpath manifest 覆盖视图、作者模板/schema、catalog patch review readiness、手动 candidate preview，以及 M5.85-44 的自动 candidate workbench；workbench capabilities 现在能发现 `workbench-reviewed-fixture-candidate-autopreview` 和 `workbench-reviewed-fixture-candidate`，trace set overview 行也暴露自动预检路径；候选草稿必须经人工 Git review 补齐 `sourceCommitSha/reviewer/reviewTimestamp/evidenceDigest` 后才可能进入真实 fixture 文件，CI blocking、LLM eval、retrieval prompt influence、durable memory runtime 仍关闭。 |
| Multi-Agent / Expert Review | 已有多专家审查聚合读模型；运行时 A2A handoff 仍关闭。 |
| 文档与恢复 | 当前主线文档已精简，旧波次报告从 docs 树移除，恢复记忆保留在 `codex-memory`；M5.85-29 已把顶级 Agent 学习指南继续扩展为知识卡片和源码精读手册，覆盖 Java/Spring、LLM/Tool、Graph/ReAct/Plan、多 Agent、MCP/A2A、RAG、Eval、Observability、安全、测试质量门、新技术采纳规则和练习任务库。 |

## 接下来开发顺序

| 顺序 | 切片 | 目标 | 主要验证 |
|---:|---|---|---|
| 1 | Eval trace evidence | M5.85-26 已补 trace evidence / curation 链路教学注释，M5.85-28 已补 promotion workflow / workbench wrapper 教学注释，M5.85-30 已补 reviewed redacted trace fixture intake 合同，M5.85-33 已补 reviewed fixture repo/classpath manifest，M5.85-34 已补 fixture 作者模板/schema，M5.85-39 已完成 catalog patch review readiness 绑定，M5.85-41 已补 manifest 结构化质量门，M5.85-42 已把质量门失败摘要投影到 catalog patch review，M5.85-43 已新增手动 candidate preview，M5.85-44 已新增自动 candidate workbench。下一步优先用自动工作台找出可人审的真实 trace 候选，再通过人工 Git review 准备首个 `qualityGateStatus=PASS` 的 reviewed redacted fixture；或切到 `vue-kube-manager` 只读渲染自动候选预检/readiness/checklist/nextActions/failedQualityGates，且前端不能触发 runtime action。 | eval contract tests + fixture review |
| 2 | 中文注释 Batch 5 收尾支撑类 | M5.85-24 已补 config/store/intent 支撑测试教学注释；M5.85-29 已继续扩展学习指南为技术知识卡片、源码精读路线和练习任务库。下一步继续扫描剩余 support/test helper 与高频联调用例，优先补真实安全边界相关注释，避免为注释而重写历史代码。 | source-contract + selected tests |
| 3 | Orchestrator hardening 收尾 | M5.85-40 已把 Graph/ReAct/Plan/ToolCallback/Orchestrator fallback 的执行入口矩阵收口到 `SafeToolExecutor`。下一步继续检查 Graph/ReAct/Plan 状态传播、错误语义、SSE 事件和 Tool result 合并是否还有遗漏；若发现展示断点或重复内容，再补小切片。 | Graph/Orchestrator contract tests |
| 4 | kube-manager READ E2E 扩展 | M5.85-32 已跑通登录型 `NodeQueryTool` 只读链路；M5.85-35 扩展到 Dashboard 部署统计；M5.85-36 批量扩展到节点剩余资源、Dashboard 镜像统计、EasyFlow 统计和 EasyFlow 列表；M5.85-38 已补真实 Agent 执行链验证：同一批 READ Tool 通过 `SafeToolExecutor` 执行并留下 `REACT_ENGINE` 审计事件，代表性 Dashboard 镜像统计通过 `AtlasToolCallback -> SafeToolExecutor` 执行并留下 `TOOL_CALLBACK` 审计事件；M5.85-40 已把真实 smoke 读超时改为可配置、默认 30 秒，并确认本地 `localhost:8100` 登录、orgId=100002 反查、直接 Tool、`SafeToolExecutor` 和 ToolCallback 只读链路通过。`ClusterOverviewTool` 的 `/dashboard/resources` 仍暂不纳入稳定批次。后续如要扩展更多 READ Tool，必须逐个增加 GET/READ/no-HITL 元数据守卫、参数白名单、执行来源审计和真实后端只读验证，不能顺手引入业务写接口、敏感读或二期域 Tool。 | `KubeManagerReadOnlySmokeTest` + focused READ Tool contracts |
| 5 | Multi-Agent review workflow | 把多专家审查从聚合读模型推进到更明确的角色、证据、轮次和阻断项工作流。 | read-model tests + security tests |
| 6 | Advanced tech gated adoption | 对 Java/Spring/Spring AI/MCP/A2A/RAG/OTel/CI 等做兼容分支或证据包，不盲目改主干依赖。 | compatibility matrix + source review |

## Phase 1 完成判据

Phase 1 不以“能聊天”为完成，而以下列条件作为顶级 Agent Core 的完成标准：

- 身份、权限、HITL、Tool、MCP、kube-manager outlet 的安全边界在代码、测试、文档和中文注释中一致。
- ReAct / Plan / Graph 能被解释、测试、恢复和审计，不依赖隐式 prompt 运气。
- Memory/RAG 和 Eval 有 reviewed trace evidence、source custody、privacy/tenant 边界和 release gate；caller-submitted 摘要不能直接成为 prompt 权威。
- 多 Agent 审查有角色、证据、审查轮次和 runtime 权力关闭说明。
- 前端只渲染后端 owned read models，不把按钮或布尔值伪装成权限、release 或成功。
- `codex-memory` 能让新会话恢复目标、当前状态、下一步和关键安全不变量。

## Phase 2 暂停范围

以下内容保留为二期插件/专家域，不在当前一期继续扩展运行时能力：

- NIM 创建和 NGC/NIM 凭据链路。
- HPC / Slurm 作业链路。
- BCM 资源分配链路。
- 与上述范围直接绑定的写入、审计、release 和运行时执行。

当前可以保留代码里的 fail-closed / HOLD / 只读证据和历史测试，但不新增一期 runtime authority，也不把这些域导出到一期 MCP manifest。
