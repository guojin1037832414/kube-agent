# kube-agent Workspace Recovery Status

- Project: kube-agent
- Recovery home: F:\gitProject\kube-agent\codex-memory\kube-agent\current
- Previous external backup: H:\codex重要文件\kube-agent
- Branch: codex/m521-29-top-agent-mission
- Latest completed wave: M5.84-1
- Latest completed title: Top-tier Vue workbench migration package
- Workspace: F:\gitProject\kube-agent
- Last synchronized: 2026-06-11 Asia/Shanghai
- Goal-state recovery note on 2026-06-11: the Codex app-level thread Goal can become `null` after session/tool-state transitions, context compaction, or thread/runtime reset. This is not the same as losing the project mission. The durable source of truth is this workspace-local `codex-memory` tree plus `docs/AGENT_ARCHITECTURE_AND_TECHNICAL_LEARNING.md` and `docs_PROJECT_MISSION_AND_MEMORY.md`. The active app-level Goal has been restored to: Phase 1 must build kube-agent into a top-tier Agent learning and production project on top of kube-manager / vue-kube-manager; NIM / HPC / Slurm / BCM stay paused for Phase 2; continue Chinese docs, architecture notes, recovery memory, and commit/push after meaningful slices.
- 2026-06-11 dev-run addendum: local backend startup on `http://localhost:8500` is restored after explicitly marking the production dependency constructors with `@Autowired` for MCP and top-tier observability/readiness services that also keep package-private `Clock` constructors for tests. Verified `mvn -q "-Dtest=AgentAdvancedTechnologyCompatibilityMatrixEvidenceReadinessServiceTest,AgentAdvancedTechnologyCompatibilityMatrixServiceTest,AgentAdvancedTechnologyCompatibilityMatrixVueBindingSpecServiceTest,AgentBackendTechnologyModernizationDecisionServiceTest,AgentTopTierReadinessOverviewServiceTest,ObservabilityControllerTest,McpGovernanceOverviewServiceTest" test`, `mvn -q -DskipTests package`, and `GET http://localhost:8500/api/agent/health` returning `status=UP`, `totalTools=183`, `supervisorGraphEnabled=true`, `graphEnabled=true`.
- 2026-06-11 frontend integration addendum: `F:\gitProject\kube-agent-vue` dev server is running on `http://localhost:5173`; its Vite `/api` proxy successfully reads `http://localhost:8500/api/agent/health` and displays Agent backend `UP`. Browser verification showed title `kube-agent workbench`, vue-kube-manager-style sidebar/topbar/admin content, visible `Agent Workbench`, `UP`, runtime panel, and no console errors. Real login/chat still requires a valid kube-manager `8100` account/session and a real OpenAI-compatible key instead of the local placeholder.
- 2026-06-11 frontend governance addendum: `kube-agent-vue` now renders four dedicated read-only governance pages at `/agent/top-tier`, `/agent/memory`, `/agent/eval`, and `/agent/kube-manager`. They bind to existing admin-only GET read models under `/api/agent/observability/**`, show endpoint paths, policy tags, key fields, collection counts, and raw JSON, and intentionally expose no write buttons, runtime switches, MCP `tools/call`, retrieval execution, CI blocking, kube-manager state-changing actions, retry enablement, or Phase 2 NIM / HPC / Slurm / BCM reopening controls. Browser verification covered all four routes in unauthenticated mode with clear permission guidance and no console errors.
- 2026-06-11 frontend governance scan addendum: `kube-agent-vue` added `scripts/verify-governance-readonly.mjs` and `npm run verify:governance`. The scan checks required governance routes, observability endpoint prefixes, and the unified read-only loader, while rejecting mutating HTTP methods, MCP `tools/call`, CI blocking enablement, write retry enablement, kube-manager state-changing actions, retrieval runtime execution, and Phase 2 domain reopening controls.
- 2026-06-11 frontend unified quality gate addendum: `kube-agent-vue` added `scripts/verify-all.mjs` and `npm run verify`. This is now the preferred frontend acceptance command because it runs governance read-only scanning, TypeScript checking, and production build in one sequence. Vite/Rolldown third-party pure-annotation warnings and Element Plus chunk-size warnings remain non-blocking known issues.
- 2026-06-11 frontend governance teaching addendum: `kube-agent-vue` governance pages now render structured `learningNotes` and `blockedActions` panels. `/agent/top-tier` browser verification confirmed visible `学习要点`, `当前关闭的权力`, `MCP tools/call`, `NIM/HPC/Slurm/BCM`, and no console errors. The governance scan now also requires teaching props, checks boundary markers, covers AgentOps placeholder routes, and scans `path/url/endpoint/href/to` plus camelCase/snake_case/kebab-case handler names for runtime-authority shortcuts.
- 2026-06-11 recovery entry addendum: `kube-agent-vue` is the current Vue 3 / Element Plus temporary AgentOps workbench; formal `vue-kube-manager` integration remains a future reviewed migration target through the M5.84 package. Quick restore path: backend `http://localhost:8500`, frontend `http://localhost:5173/agent/workbench`, verify with `npm run verify`, and remember real login/chat needs kube-manager `8100` credentials plus a real OpenAI-compatible key.
- 2026-06-11 frontend trace explorer addendum: `kube-agent-vue` now mounts `/agent/trace` as a real read-only Trace Explorer. It consumes existing backend M5.32/M5.33 admin-only GET read models, `GET /api/agent/observability/replay/trace/{traceId}?limit=...` and `GET /api/agent/observability/eval/trace/{traceId}?limit=...`, through the frontend read-only observability loader. It renders redacted replay timeline and deterministic eval evidence only; it does not execute replay, run eval suites, mutate trace-set catalogs, call MCP `tools/call`, execute Tools, call kube-manager state-changing APIs, or infer release authority. Frontend verification target: `http://localhost:5173/agent/trace`.
- 2026-06-11 frontend tools governance addendum: `kube-agent-vue` now mounts `/agent/tools` as a real read-only Tools Governance page. It consumes existing backend MCP read models, `GET /api/agent/mcp/manifest` and `GET /api/agent/mcp/governance/overview`, through the frontend read-only loader. It renders safe manifest metadata, export policy, governance cards, blocked capabilities, future enablement protocol, and raw JSON only; it does not expose MCP `tools/call`, execute Tools, mutate ToolRegistry state, accept caller-provided tool arguments, export write tools, approve runtime MCP, or issue release decisions. Frontend verification target: `http://localhost:5173/agent/tools`.
- 2026-06-11 frontend runs ledger addendum: `kube-agent-vue` now mounts `/agent/runs` as a local read-only session ledger. It derives one current-session ledger item from the Pinia `session / conversation / messages / events` state and does not call backend runs APIs, persist run history, infer task success, infer policy/HITL/audit/eval decisions, or expose rerun/retry/approval controls. Frontend verification target: `http://localhost:5173/agent/runs`.
- 2026-06-11 frontend HITL evidence addendum: `kube-agent-vue` now mounts `/agent/hitl` as a local read-only HITL signal viewer. It derives clarify/HITL/approval-like/human-review evidence from current frontend SSE events and messages only; it does not call backend HITL queue APIs, invoke confirm/clarify endpoints, approve decisions, override policy, resume checkpoints, execute Tools, or infer queue status. Frontend verification target: `http://localhost:5173/agent/hitl`.
- 2026-06-11 frontend observability addendum: `kube-agent-vue` now mounts `/agent/observability` as a real read-only observability evidence page. It consumes existing admin-only GET read models `GET /api/agent/observability/snapshot`, `GET /api/agent/observability/audit/index`, `GET /api/agent/observability/audit/trace/{traceId}?limit=...`, and `GET /api/agent/observability/audit/id/{auditId}` through the frontend read-only loader. It renders metrics counters, durable audit readiness, redaction/index metadata, trace audit events, auditId lookup, local runtime context, and raw JSON evidence only; it does not execute replay, run eval suites, mutate trace-set catalogs, call MCP `tools/call`, execute Tools, call kube-manager state-changing APIs, mutate runtime state, or infer release authority. Frontend verification target: `http://localhost:5173/agent/observability`.
- 2026-06-11 frontend settings contract addendum: `kube-agent-vue` now mounts `/agent/settings` as a local read-only settings contract evidence page. It displays frontend runtime values and already-held session/health/readiness facts only: API base, Workbench mode, operator session, conversation anchor, backend health, readiness fact count, runtime status text, and local error text. It does not call backend settings APIs, save configuration, call `fetch`, call `postJson`, mutate runtime state, infer config completeness, or enable policy/model/tool/HITL/eval/memory/kube outlet authority. The frontend governance scan now requires `SettingsContractView`, so no AgentOps placeholder routes remain. Frontend verification target: `http://localhost:5173/agent/settings`.
- 2026-06-11 frontend Memory/RAG workbench addendum: `kube-agent-vue` now mounts `/agent/memory` as a dedicated read-only Memory/RAG Workbench instead of a generic governance JSON page. It consumes existing admin-only GET read models under `/api/agent/observability/memory-rag/**`: readiness, citation/source contract, source digest contract, durable lifecycle contract, eval gate contract, eval-suite binding contract, trace-set curation contract, curation overview, and reviewed trace manifest. It renders summary metrics, learning notes, closed runtime authority, readiness cards, trace-set curation rows, reviewed manifest rows, eval gate/suite binding, citation/source JSON, lifecycle/privacy/safety JSON, and raw read models. It does not run retrieval, call vector stores/embeddings/rerankers/GraphRAG, mutate prompts, write memory, mutate trace-set catalogs, run eval suites, enable CI blocking, call MCP `tools/call`, execute Tools, call kube-manager state-changing APIs, or reopen NIM/HPC/Slurm/BCM. Frontend verification target: `http://localhost:5173/agent/memory`.
- 2026-06-11 frontend readiness matrix addendum: `kube-agent-vue` now adds a `Governance Evidence Matrix` to `/agent/top-tier`. It labels backend, local, derived, and unknown evidence separately and states that health UP, local auth, and SSE done do not prove readiness, permission, policy, HITL, eval, SLO, cost, release authority, or task success. The frontend governance scan now covers 31 files, status utilities, Memory/RAG boundary markers, readiness no-inference markers, and ChatConsole quick prompts as draft-only.
- 2026-06-11 frontend Eval Workbench addendum: `kube-agent-vue` now mounts `/agent/eval` as a dedicated read-only Eval Workbench instead of a generic governance JSON page. It consumes existing admin-only GET read models under `/api/agent/observability/eval/**`: workbench capabilities, workbench overview, reviewed trace evidence, release-blocking gate contract, gate-bundle summary, and trace-set catalog. It renders release eligibility, reviewed trace evidence progress, CI blocking state, trace-set gate rows, reviewed trace rows, release checks, review workflow, next actions, gate bundle/catalog JSON, quality/safety/privacy JSON, and raw read models. It does not run eval suites, replay traces, execute candidate discovery/curation/promotion/catalog patch workflows, mutate trace-set catalogs, POST raw gate bundles, enable CI blocking, approve releases, call MCP `tools/call`, execute Tools, call kube-manager state-changing APIs, run retrieval/vector/memory runtime, or reopen NIM/HPC/Slurm/BCM. Frontend verification target: `http://127.0.0.1:5174/agent/eval` when 5173 is occupied, otherwise `http://localhost:5173/agent/eval`.
- 2026-06-11 frontend neutral status addendum: `kube-agent-vue` now treats health/session/conversation/SSE done/tool_done as conservative evidence states rather than success/pass. `MetricItem` uses shared `StatusKind`, `statusLabel`, and source; `MetricGrid` renders these directly; `agentEvidence` maps done/completed/tool_done to `readonly/Observed`. The governance scan now covers 35 files and guards Eval Workbench markers plus neutral status semantics.
- 2026-06-11 frontend top-tier latest technology workbench addendum: `kube-agent-vue` now renders `/agent/top-tier` as a dedicated `Latest Technology Workbench` instead of a generic readiness page. It binds 15 backend admin-only GET read models: readiness overview, advanced technology adoption contract, technology introduction playbook, compatibility matrix, compatibility matrix Vue binding spec, compatibility evidence readiness, backend modernization decision, official version/protocol watch, official watch dashboard, official watch Vue binding spec, Vue workbench implementation package, Vue workbench acceptance contract, Vue workbench migration package, Phase 1 execution roadmap, and Vue readiness control plane. It renders latest-tech lanes, official source watch, compatibility/evidence readiness, backend modernization, Phase 1 roadmap, Vue readiness, disabled runtime actions, blocked runtime shortcuts, review gates, build order, and raw JSON only. It exposes no dependency upgrade, compatibility branch creation, MCP `tools/call`, A2A handoff, retrieval/vector/reranker/GraphRAG runtime, eval run, trace-set mutation, CI blocking switch, kube-manager mutation, write retry switch, HITL override, memory/audit write, durable receipt action, or Phase 2 NIM/HPC/Slurm/BCM reopening control. Browser verification target: `http://127.0.0.1:5174/agent/top-tier` when `5173` is occupied; verified visible `Top-tier Readiness`, `Latest Technology Workbench`, `GET-only read models`, `Runtime controls absent`, `No MCP tools/call endpoint`, and `NIM/HPC/Slurm/BCM`, with only read/session buttons visible.
- 2026-06-11 frontend backend-flag semantics addendum: `kube-agent-vue` now treats backend boolean/diagnostic fields as evidence rather than UI authority. `backendFlagStatus(...)` and `backendFlagLabel(...)` render true/false as readonly backend flag evidence. Runtime Graph, Eval CI blocking, and Observability durable audit ready/enabled diagnostics no longer appear as naked `Enabled`, `Ready`, `success`, or `warning` conclusions. The governance scan protects this boundary and Browser smoke checks covered `/agent/workbench`, `/agent/eval`, and `/agent/observability` with no dangerous buttons, no relevant console errors, and no `CI ready` / `Graph Enabled` / `CI blocking disabled` fragments.
- Latest implementation commit before this checkpoint: pending M5.84 implementation commit
- Pushed to remote: pending M5.84 push
- Recovery policy: new progress and memory files are written to this workspace-local directory first to avoid external filesystem approval prompts.
- Verification: M5.84-1 implementation passed `mvn -q "-Dtest=AgentTopTierVueWorkbenchMigrationPackageServiceTest,AgentTopTierVueWorkbenchAcceptanceContractServiceTest,AgentVueReadinessControlPlaneServiceTest,AgentPhase1ExecutionRoadmapServiceTest,AgentAdvancedTechnologyAdoptionContractServiceTest,AgentTopTierReadinessOverviewServiceTest,ObservabilityControllerTest,ObservabilityControllerSecurityContractTest,AgentSecurityConfigWebMvcTest" test`, `mvn -q "-DskipTests" validate`, and `git diff --check` before implementation commit. `git diff --check` reported Windows LF-to-CRLF warnings only.
- M5.84-1 security invariant: top-tier Vue workbench migration package is admin-only, GET-only, read-only, migration-package-only, dry-run-only, source-contract-composition-only, and external-call-free at request time. It composes only `acceptanceContractService.contract()` and fails closed if the source acceptance contract is missing, not ready, or unexpectedly opens runtime authority. It does not modify `pom.xml`, upgrade dependencies, switch Java/Spring/Spring AI baselines, write `vue-kube-manager`, call kube-manager or port `8100`, run evals, execute Tools, call `SafeToolExecutor`, invoke HITL, expose MCP runtime `tools/call`, run A2A runtime handoff, execute retrieval/vector/embedding/reranker/GraphRAG, write memory, write audit, issue durable receipts, enable CI blocking, or touch NIM / HPC / Slurm / BCM Phase 2 scope.
- M5.84-1 latest-technology note: Phase 1 still targets a top-tier Agent, not a reduced MVP. Official-source calibration on 2026-06-10 keeps Spring Boot 4, Spring AI 2, OpenAI Agents/Responses patterns, MCP 2025-11-25, A2A 1.0.0, OTel GenAI, GraphRAG/rerankers/vector stores, CI hard gates, and kube-manager write governance in scope through compatibility/evidence lanes. Runtime authority remains blocked until source contracts, deterministic tests, reviewed evidence, Vue visibility, release review, and recovery memory pass.
- M5.84-1 Vue/recovery addendum: `vue-kube-manager` should next apply the M5.84 migration package as a reviewed patch after the repo is writable/trusted. The package records `F:/gitProject/vue-kube-manager`, Git safe-directory need, `asyncRoutes`, `BackendLayout`, absolute `/agent/top-tier/*` child paths, exact menu permission matching, GET-only `@/utils/request` exports, `response.data` unwrapping, mocked fixtures, Jest test blueprints, validation scans, and forbidden runtime assertions. Workspace recovery memory remains under `F:\gitProject\kube-agent\codex-memory\kube-agent\current`; `H:\codex重要文件\kube-agent` is a historical backup path only and should not be written unless explicitly requested.
- M5.84-1 multi-expert note: Parfit / frontend explorer confirmed the real `vue-kube-manager` routing, permission, API, and Jest constraints. Carver / backend architecture review recommended a migration package rather than a write generator. Prior Hypatia and Planck M5.83 findings remain embedded through the source acceptance contract.
- Security invariant: Graph Bridge ToolCallback, ReActEngine, legacy core AtlasToolCallback, and AtlasOrchestrator fallback no longer directly call BaseTool.execute; they delegate to SafeToolExecutor with source-specific audit markers. Production code now has exactly one permanent real BaseTool.execute boundary: SafeToolExecutor. M5.23-1 adds a shared traceId kernel across Orchestrator, `/chat/graph`, HITL resume, ReAct, Graph state, SafeToolExecutor, ToolCallbacks, SSE timeline metadata, and Graph updates. M5.24-1 propagates trace evidence to kube-manager user/business HTTP outlet through `X-Trace-Id` and W3C `traceparent` when possible. M5.25-1 binds SafeToolExecutor decisions to `AgentAuditEvent` records with `auditId`, `traceId`, Tool metadata, outcome, execution flags, and redacted parameter summaries. M5.26-1 projects audit events into stable `atlas.agent.*` telemetry attributes plus isolated experimental OTel/GenAI attributes without exposing raw principal, conversation, reason, endpoint strings, or parameter values. M5.27-1 publishes the redacted projection to Micrometer Observation (`atlas.agent.audit` / `atlas.agent.audit.recorded`) with low-cardinality bounded tags and high-cardinality volatile evidence separated; publisher failure is non-fatal for the diagnostic recorder. M5.28-1 routes kube-manager GET through Resilience4j read retry/circuit/bulkhead and routes POST/PATCH/PUT/DELETE through write circuit/bulkhead without automatic retry. M5.29-1 registers `AuthTokenFilter` inside Spring Security, bridges cached kube-manager Bearer sessions into standard `Authentication`, keeps raw Bearer token out of `SecurityContext` credentials, protects `/api/agent/observability/**` and non-health/info `/actuator/**` with admin roles, and keeps ordinary Agent APIs temporarily `permitAll` for incremental migration. M5.29-2 adds `AgentPrincipal` / `AgentPrincipalResolver`, prioritizes real `SecurityContext` authentication, ignores anonymous authentication, falls back to legacy `UserPermissionContext`, and migrates ObservabilityController to the unified resolver. M5.29-3 binds SafeToolExecutor audit actor extraction to an `AgentPrincipal` snapshot captured before request ThreadLocal binding, so audit `userId` / `organizationId` prefer server-side SecurityContext or legacy permission context over caller-supplied request fields. M5.29-4 bridges frontend `X-Session-Id` through server-side `SessionStore` into Spring Security `Authentication`, protects `/api/agent/memory/**` and `/api/agent/mcp/**` with `.authenticated()`, preserves Bearer precedence, keeps raw kube-manager token out of `Authentication.credentials`, and migrates MemoryController ownership to trusted principal-first semantics. M5.29-5 migrates ConversationController metadata ownership to trusted principal username, protects `/api/agent/conversations` and child resources with `.authenticated()`, and keeps conversationId/X-Session-Id as locators rather than authorization facts. M5.29-6 migrates Chat/SSE/Graph/ReAct/HITL runtime identity to trusted principal + server-side session + owner-checked conversation context, protects `/api/agent/chat/stream`, `/api/agent/chat/graph`, and `/api/agent/hitl/**` with `.authenticated()`, uses `run-*` / `graph-*` runtime ids instead of raw `ses_*`, and prevents cross-user HITL checkpoint resume. M5.29-7 closes the temporary ordinary Agent API `permitAll` window by enabling method security, adding `/api/agent/**` authenticated fallback, and adding method-level admin guard to `ObservabilityController#snapshot()`. M5.30-1 adds optional redacted durable audit storage through `AgentAuditDurableSink` / `JsonlAgentAuditDurableSink`, exposes durability readiness in admin snapshots, and lets `SafeToolExecutor` fail closed before high-risk `CREATE` / `UPDATE` / `DELETE` / `ACTION` / `PLACEHOLDER` execution when durable audit is configured as mandatory but unavailable. M5.30-2 adds admin-only redacted audit query APIs for current `auditId` / `traceId` lookup. M5.30-3 adds `AgentAuditDurableReceipt`, `AgentAuditOutcome.PREPARED`, JSONL `recordPhase=PRE_EXECUTION` / `FINAL`, process-local JSONL write locking, and a `SafeToolExecutor` durable prewrite receipt gate: high-risk execution in mandatory durable mode now requires this specific call's pre-execution durable evidence, and missing / null / UNKNOWN risk metadata fails closed before real Tool execution. M5.31-1 adds `JsonlAgentAuditQueryService` and lets the primary audit query facade prefer durable JSONL lookup when available, returning redacted newest-first `auditId` / `traceId` evidence including multi-phase `PRE_EXECUTION/PREPARED -> FINAL` records. M5.32-1 adds admin-only replay timeline DTOs and `/api/agent/observability/replay/trace/{traceId}`, converting redacted audit query events into oldest-first frontend steps without raw principal, organization, conversation, endpoint, reason text, or parameter values. M5.32-2 preserves `recordPhase` through audit query and replay steps. M5.33-1 adds admin-only deterministic eval reports at `/api/agent/observability/eval/trace/{traceId}`; eval consumes redacted replay DTOs only, sets `deterministic=true`, `llmUsed=false`, `externalCalls=false`, and cannot authorize Tool execution. M5.34-1 adds admin-only eval suite reports at `/api/agent/observability/eval/suite`; suite eval aggregates per-trace reports into future release-gate evidence with minimum score and fail-on-warning policy while remaining deterministic, redacted-only, and non-executing. M5.34-2 hardens suite release-gate semantics with service-owned defaults, bounded per-trace replay limits, a maximum 50-case suite cap, explicit skipped trace ids, and fail-closed oversized-suite behavior. M5.35-1 adds admin-only named eval suite catalog/run APIs at `/api/agent/observability/eval/suites` and `/api/agent/observability/eval/suites/{suiteId}/run`; suite definitions are metadata-only, named runs still delegate to the hardened deterministic suite gate, and both remain redacted-only, non-executing, no-LLM, no-external-call, no-kube-manager-call paths. M5.36-1 adds admin-only compact CI/release gate artifacts at `/api/agent/observability/eval/suites/{suiteId}/gate`; the artifact omits embedded reports/replay and carries only machine-readable verdict, score, coverage, trace-anchor, policy, and privacy evidence. M5.37-1 adds admin-only versioned eval trace-set catalog/gate APIs at `/api/agent/observability/eval/trace-sets` and `/api/agent/observability/eval/trace-sets/{traceSetId}/gate`; trace-set gates bind curated catalog evidence to named suite gates, ignore request trace-id overrides, and fail closed when curated trace IDs are empty. M5.38-1 adds admin-only trace-set gate bundle artifacts at `/api/agent/observability/eval/trace-sets/gate-bundle`; CI now generates `target/agent-eval/trace-set-gate-bundle.json` during tests and uploads `target/agent-eval/` while keeping `ciBlockingEnabled=false` until curated real trace IDs exist. M5.39-1 adds admin-only trace-set curation review artifacts at `/api/agent/observability/eval/trace-sets/{traceSetId}/curation-review`; candidate trace IDs are filtered to W3C-compatible anchors, evaluated through the attached suite gate, and returned as review-only evidence without mutating `observability/eval-trace-sets.json` or promoting candidates to catalog evidence. M5.40-1 adds admin-only trace-set candidate discovery at `/api/agent/observability/eval/trace-sets/{traceSetId}/candidates`; it reads only redacted `AgentAuditQueryEvent` recent events from in-memory/JSONL read models, groups by W3C-compatible trace anchor, and returns advisory candidate summaries without raw evidence, Tool execution, kube-manager calls, or catalog mutation. Observability snapshot is admin-only, includes `schemaVersion`, `generatedAt`, `replayCapabilities`, and durable audit readiness, and exposes only redacted audit diagnostics. Malformed/null request and blank intent produce BLOCKED audit events. Tool-level `TOOL_EXECUTION_ERROR` stays outward fail-closed but audits as `ERROR + executed=true + success=false`. External `X-Trace-Id` and LLM/action `traceId` values are validated and treated as control-plane data, not business Tool params. Non-W3C external traces do not forge `traceparent`. A2A is documented as compatibility-matrix work, not implemented Agent Card / JSON-RPC A2A support. nim_create remains HOLD/mock-first and is now Phase 2; no real 8100 write, NIM HTTP call, Authorization header sending, deployment POST, validation result signer, release decision signer, code switch implementation, runtime write behavior, Elasticsearch, ISysLogService, sys_log write, or real kube-manager state-changing call was added.
- Teaching map: docs/AGENT_ARCHITECTURE_AND_TECHNICAL_LEARNING.md is now the long-lived overall architecture and technical-learning document.
- M5.41-1 security invariant: catalog patch proposal is admin-only, review-only, RFC 6902-style evidence for Git review; it reuses curation review, does not mutate `observability/eval-trace-sets.json`, does not execute Tool code, does not call kube-manager, and does not expose raw audit fields.
- M5.42-1 security invariant: promotion workflow is admin-only, orchestration-only, and composes discovery/review/patch-proposal without adding runtime catalog write authority, Tool execution, kube-manager calls, external calls, LLM use, or raw audit exposure.
- M5.43-1 security invariant: capability manifest is admin-only and metadata-only; it describes backend workbench capabilities and schema versions without audit query, eval run, Tool execution, kube-manager calls, LLM/external calls, or catalog mutation.
- M5.44-1 security invariant: workbench overview is admin-only and read-only; it composes capability metadata, trace-set catalog rows, and compact gate-bundle state without candidate discovery, raw audit query, embedded replay/reports, Tool execution, kube-manager calls, LLM/external calls, or catalog mutation.
- M5.45-1 security invariant: workbench trace-set detail is admin-only and read-only; it explains one trace set's evidence requirements, compact gate state, next actions, and endpoint templates without candidate discovery execution, raw audit query, embedded replay/reports, Tool execution, kube-manager calls, LLM/external calls, or catalog mutation.
- M5.46-1 security invariant: workbench promotion workflow result is admin-only and wrapper-only; it may run existing redacted candidate discovery / curation review / patch proposal services, but it does not mutate the catalog, write runtime catalog data, execute Tools, call kube-manager, use LLM/external calls, or embed replay/report payloads. Catalog promotion authority remains human Git review only.
- M5.47-1 security invariant: workbench catalog patch review is admin-only and review-only; it wraps the existing redacted catalog patch proposal into sanitized Vue/Git-review fields without applying JSON Patch, mutating `observability/eval-trace-sets.json`, writing runtime catalog data, executing Tools, calling kube-manager, using LLM/external calls, or embedding replay/report payloads. Catalog promotion authority remains human Git review only.
- M5.48-1 security invariant: workbench gate bundle summary is admin-only, read-only, and summary-only; it wraps the current catalog gate bundle into Vue release-gate rows without accepting caller trace IDs, enabling CI blocking, mutating `observability/eval-trace-sets.json`, writing runtime catalog data, executing Tools, calling kube-manager, using LLM/external calls, or embedding replay/report payloads. Catalog promotion authority remains human Git review only.
- M5.49-1 security invariant: kube-manager HTTP outlet health summary is admin-only, local-process-only, read-only, and summary-only; it reads local Spring config and Resilience4j registry state without calling `KubeManagerHttpClient`, `RestClient`, kube-manager `8100`, `/api/login`, Tools, LLMs, or external services, without exposing raw base URL/token/password/backend paths/bodies, without mutating circuit breaker or bulkhead state, and without enabling write retry.
- M5.50-1 security invariant: kube-manager write retry readiness is admin-only, local-process-only, read-only, summary-only, and GET-only; it returns `NOT_READY`, `writeRetryEnabled=false`, and `automaticWriteRetryAllowed=false`, accepts no caller idempotency/release/retry/write-control inputs, does not call `KubeManagerHttpClient`, `RestClient`, kube-manager `8100`, `/api/login`, Tools, LLMs, or external services, does not write audit or issue durable receipts, does not mutate resilience registries or runtime configuration, does not expose raw base URL/token/password/backend paths/bodies, and does not enable write retry.
- M5.51-1 security invariant: kube-manager write idempotency contract defines a pure Java server-derived SHA-256 key derivation primitive and an admin-only read model; it accepts no caller-provided idempotency key, is not bound to `KubeManagerHttpClient`, does not inject HTTP headers, does not call kube-manager/RestClient/Tools/LLMs/external services, does not write audit or issue durable receipts, does not mutate resilience registries, does not expose raw key/evidence, and does not enable write retry.
- M5.52-1 security invariant: kube-manager write operation safety contract defines a pure Java static review-only catalog for generic allowlist/RBAC/readback evidence; runtime retry eligible write operation count remains `0`, caller-provided allowlist entries and caller success claims are rejected by contract, the admin-only endpoint is not bound to `KubeManagerHttpClient`, does not call kube-manager/RestClient/Tools/LLMs/external services, does not execute readback, does not write audit or issue durable receipts, does not inject HTTP headers, does not mutate resilience registries, does not expose raw principal/organization/body/path secrets, and does not enable write retry. NIM / HPC / Slurm / BCM remain Phase 2 paused scope.
- M5.53-1 security invariant: kube-manager write retry governance contract defines pure Java static review-only failure classes, a bounded retry predicate contract, and operator-review-only compensation policies; `contractStatus=CONTRACT_DEFINED_NOT_BOUND`, runtime retryable failure class count remains `0`, automatic compensation policy count remains `0`, no retry predicate is bound to Resilience4j or the HTTP outlet, no compensation executor exists, the admin-only endpoint does not call kube-manager/RestClient/Tools/LLMs/external services, does not execute readback or compensation, does not write audit or issue durable receipts, does not inject HTTP headers, does not mutate resilience registries, does not expose raw principal/organization/body/path/exception secrets, and does not enable write retry. NIM / HPC / Slurm / BCM remain Phase 2 paused scope.
- M5.54-1 security invariant: kube-manager write release gate contract defines pure Java static review-only durable receipt and HITL/release evidence contracts; `contractStatus=CONTRACT_DEFINED_NOT_BOUND`, runtime release gate open count remains `0`, no durable receipt issuer exists, no server HITL/release evidence is bound to the HTTP outlet, caller-provided release evidence is rejected by contract, the admin-only endpoint does not call kube-manager/RestClient/Tools/HITL/LLMs/external services, does not write audit or durable storage, does not issue durable receipts, does not sign release decisions, does not inject HTTP headers, does not execute readback or compensation, does not mutate resilience registries, does not expose raw principal/organization/body/path/release/receipt secrets, and does not enable write retry. NIM / HPC / Slurm / BCM remain Phase 2 paused scope.
- M5.55-1 security invariant: kube-manager HTTP outlet governance workbench overview is admin-only, local-process-only, read-only, overview-only, and frontend-navigation-only; it composes M5.49-M5.54 read models into six governance cards and keeps `workbenchStatus=WRITE_GOVERNANCE_NOT_READY`, `boundRuntimeContractCount=0`, `releaseGateOpen=false`, `writeRetryEnabled=false`, `automaticWriteRetryAllowed=false`, `runtimeReleaseGateOpenCount=0`, `runtimeRetryableFailureClassCount=0`, and `automaticCompensationPolicyCount=0`. It does not call kube-manager/RestClient/Tools/HITL/LLMs/external services, does not write audit or durable storage, does not issue durable receipts, does not inject HTTP headers, does not execute readback or compensation, does not mutate resilience registries, does not open a release switch, does not expose raw principal/organization/path/endpoint/body/token/release/receipt secrets, and does not enable write retry. NIM / HPC / Slurm / BCM remain Phase 2 paused scope.
- M5.56-1 security invariant: MCP governance overview is authenticated, read-only, manifest-only, and contract-only; it composes the safe MCP manifest into governance cards and keeps `governanceStatus=MANIFEST_ONLY_NOT_CALLABLE`, `mcpServerRuntimeEnabled=false`, `toolsCallEnabled=false`, `externalToolExecutionEnabled=false`, and `callerProvidedToolCallAccepted=false`. It does not add a real MCP runtime server, `tools/call` handler, streaming call plane, external Agent tool execution, Tool execution, `SafeToolExecutor` invocation, HITL invocation, audit write, durable receipt issuance, LLM/external call, runtime Tool registry mutation, write-tool export, sensitive-read Tool export, kube-manager call, `RestClient`, or `WebClient`. It does not expose raw endpoint, raw backend path, Authorization header, token, password, raw principal, raw organization, raw request body, or raw response body. NIM / HPC / Slurm / BCM remain Phase 2 paused scope.
- M5.57-1 security invariant: top-tier Agent readiness overview is admin-only, read-only, summary-only, and local read-model composition only; it composes kube-manager governance, eval workbench capabilities, and MCP governance into nine capability cards and keeps `readinessVerdict=PHASE_1_TOP_TIER_CORE_IN_PROGRESS`, `phase1TopTierGoalPreserved=true`, `writeAuthorityClosed=true`, `toolExecutionTriggered=false`, `kubeManagerCalls=false`, and `llmUsed=false`. Its safety proof explicitly states `endpointMapNavigationOnly=true`, `endpointMapDoesNotGrantAccess=true`, `safeToolExecutorInvocation=false`, `hitlInvocation=false`, `kubeManagerHttpClientBinding=false`, `mcpToolsCall=false`, and `phase2NimHpcSlurmBcmTouched=false`. It does not add MCP runtime `tools/call`, external Agent tool execution, Tool execution, HITL invocation, audit write, durable receipt issuance, runtime Tool registry mutation, kube-manager call, `RestClient`, `WebClient`, write-tool export, or sensitive-read export. NIM / HPC / Slurm / BCM remain Phase 2 paused scope, and Phase 1 top-tier quality is preserved.
- M5.58-1 security invariant: Memory/RAG readiness is admin-only, read-only, summary-only, local-process-only, and fail-closed-no-retrieval; it reports `readinessVerdict=MEMORY_RAG_CONTRACT_DEFINED_NOT_READY`, `currentSafeSummaryMemoryEnabled=true`, `durableMemoryReady=false`, `ragReady=false`, `citationContractReady=false`, and `evalCoverageReady=false`. It reads only `ConversationSummaryMemoryStore.userCount()` and `MAX_SUMMARIES_PER_USER`. It does not bind a vector store, execute retrieval, call embedding/reranker/LLM, write memory, expose raw conversation/document/retrieved chunk content, execute Tools, invoke `SafeToolExecutor`, invoke HITL, write audit, issue durable receipts, call kube-manager, use `RestClient` / `WebClient`, expose MCP runtime `tools/call`, or touch NIM / HPC / Slurm / BCM Phase 2 scope.
- M5.59-1 security invariant: Memory/RAG citation-source contract is admin-only, read-only, contract-only, and fail-closed; it reports `contractStatus=CONTRACT_DEFINED_NOT_BOUND`, `boundToRetrievalRuntime=false`, `promptEvidenceAllowedNow=false`, `uncitedAnswerAllowed=false`, and `rawDocumentExposureAllowed=false`. It defines required source evidence fields, citation fields, and prompt evidence rules without executing retrieval, binding a vector store, calling embedding/reranker/LLM, mutating prompts, writing memory, ingesting documents, exposing raw document/prompt/retrieved chunk content, executing Tools, invoking `SafeToolExecutor`, invoking HITL, writing audit, issuing durable receipts, calling kube-manager, using `RestClient` / `WebClient`, exposing MCP runtime `tools/call`, or touching NIM / HPC / Slurm / BCM Phase 2 scope.
- M5.66-1 security invariant: reviewed eval trace evidence is admin-only, read-only, contract-only, summary-only, and fail-closed. It reports `evidenceStatus=NEEDS_REVIEWED_REDACTED_TRACE_EVIDENCE`, `reviewedEvidenceReady=false`, `releaseBlockingAllowedNow=false`, `ciBlockingEnabled=false`, `runtimeMutationAllowed=false`, `traceSetCount=4`, `reviewedTraceSetCount=0`, and `reviewedTraceAnchorCount=0` until all trace sets contain reviewed anchors. It publishes review pipeline, quality gates, standards alignment, next actions, endpoint map, safety proof, and privacy proof without running evals, changing CI blocking, mutating trace-set catalogs, querying raw audit evidence, embedding replay payloads, executing Tools, invoking `SafeToolExecutor`, invoking HITL, calling kube-manager, exposing MCP runtime `tools/call`, calling LLMs/external services, writing audit, issuing durable receipts, writing memory, executing retrieval, upgrading dependencies, or touching NIM / HPC / Slurm / BCM Phase 2 scope.
- M5.60-1 security invariant: Memory/RAG source evidence digest contract is admin-only, read-only, contract-only, and fail-closed; it reports `contractStatus=CONTRACT_DEFINED_NOT_BOUND`, `sourceEvidenceDigestDeriverDefined=true`, `boundToIngestionRuntime=false`, `boundToRetrievalRuntime=false`, `sampleUsesSyntheticEvidenceOnly=true`, and `promptEvidenceAllowedNow=false`. The pure Java deriver accepts only stable ids, bounded enums, and SHA-256 digests, deriving `sourceDigest`, `chunkDigest`, `evidenceDigest`, and `citationSeed` without executing ingestion/retrieval, binding a vector store, calling embedding/reranker/LLM, mutating prompts, writing memory, exposing raw source/prompt/retrieved chunk content, executing Tools, invoking `SafeToolExecutor`, invoking HITL, writing audit, issuing durable receipts, calling kube-manager, using `RestClient` / `WebClient`, exposing MCP runtime `tools/call`, or touching NIM / HPC / Slurm / BCM Phase 2 scope.
- Goal truth source: Phase 1 must deliver the full top-tier kube-manager Agent Core. Moving NIM / HPC / Slurm / BCM to Phase 2 only postpones specialist domain plugins; it must not reduce Phase 1 standards for orchestration, Tool governance, safe execution, frontend workflow, observability, evaluation, documentation, and recovery memory.
- M5.22-1 learning note: advanced engineering means verified upgrade paths, not unbuildable version chasing. Java 17 remains the current verified baseline; Java 21/25, Spring Boot 4, and Spring AI 2 are compatibility-matrix follow-ups.
- M5.22-1 delivered: Spring Boot 3.5.14, Spring AI 1.1.7, Resilience4j, Micrometer Tracing/OpenTelemetry OTLP, Testcontainers, Maven Enforcer, Surefire/Failsafe, JaCoCo, CycloneDX SBOM, SpotBugs quality profile, GitHub Actions backend quality workflow, environment-driven production defaults, and docs/tech-stack/BACKEND_ADVANCED_TECH_STACK_ROADMAP_20260608.md.
- M5.22-2 delivered: Graph Bridge AtlasToolCallback safe execution migration, SafeToolExecutionSource.TOOL_CALLBACK result marker, forged protected/control param regression tests, missing trusted org fail-closed regression, and direct-execute allowlist shrink from 4 to 3 entries.
- M5.22-3 delivered: ReActEngine safe execution migration, SafeToolExecutionSource.REACT_ENGINE request construction, trusted execution/display timeline parameter separation, sanitized ReAct memory/SSE event tests, HITL fail-closed ReAct contract updates, and direct-execute allowlist shrink from 3 to 2 entries.
- M5.22-4 delivered: legacy core AtlasToolCallback safe execution migration, injectable compatibility runtime, forged protected/control param regression, missing trusted org fail-closed regression, and direct-execute allowlist shrink from 2 to 1 entry.
- M5.22-5 delivered: AtlasOrchestrator fallback safe execution migration, SafeToolExecutionSource.ORCHESTRATOR_FALLBACK marker, direct-execute allowlist shrink from 1 to 0, and production SafeToolExecutor-only real execution boundary.
- M5.23-1 delivered: AgentTraceContext trace kernel, safe external trace candidate validation, traceId propagation through SafeToolExecutionRequest/Result, SafeToolExecutor, Orchestrator, `/chat/graph`, HITL resume, ReAct timeline events, Graph tool/execute nodes, Graph Bridge callback, legacy core callback, and protected trace control-plane filtering.
- M5.24-1 delivered: AgentTraceContext W3C traceparent conversion, kube-manager HTTP outlet `X-Trace-Id` / `traceparent` propagation for GET/POST/PATCH/PUT/DELETE/resolveOrgId, HTTP trace propagation contract tests, and source-level guard against hand-written business `X-Token` headers that skip trace helper.
- M5.25-1 delivered: generic `com.atlas.audit` evidence package, SafeToolExecutor trace-aware audit recording for success/business failure/blocked/error outcomes, in-memory diagnostic recorder, redacted observability audit snapshot, and audit/execute-boundary regression tests.
- M5.26-1 delivered: audit telemetry projection contract, stable `atlas.agent.*` attributes, isolated experimental OTel/GenAI attributes, and redacted telemetry data inside admin audit summaries.
- M5.27-1 delivered: audit telemetry Observation publisher, cardinality-aware Observation key values, telemetry-aware in-memory recorder injection, non-fatal publisher failure handling, and redaction/cardinality regression tests.
- M5.28-1 delivered: Resilience4j kube-manager HTTP outlet policy, GET read retry/circuit/bulkhead, WRITE no-auto-retry circuit/bulkhead, Spring Retry annotation removal, and read/write retry regression tests.
- M5.29-1 delivered: Spring Security dependency and `SecurityFilterChain`, kube-manager Bearer session to `Authentication` bridge, admin-only observability/actuator rules, default basic/form/logout disabled, explicit kube-agent `UserDetailsService`, raw token excluded from SecurityContext credentials, and unit/contract/MockMvc security tests.
- M5.29-2 delivered: `AgentPrincipal` / `AgentPrincipalResolver`, SecurityContext-first current actor resolution with legacy fallback, ObservabilityController resolver migration, and resolver/observability security tests.
- M5.29-3 delivered: principal-bound audit actor extraction, SafeToolExecutor optional resolver injection, pre-execution audit principal snapshot, principal-aware audit factory overload, and SecurityContext/legacy fallback audit actor regression tests.
- M5.29-4 delivered: `X-Session-Id` to Spring Security bridge through `SessionStore`, Bearer-over-session precedence, memory/mcp authenticated endpoint rules, MemoryController trusted-principal-first ownership, and session/security/memory regression tests.
- M5.29-5 delivered: ConversationController trusted-principal owner migration, conversations authenticated endpoint rules, raw session/anonymous owner fallback removal, and conversation/security regression tests.
- M5.29-6 delivered: Chat/SSE runtime identity migration, chat/graph/HITL authenticated endpoint rules, owner-checked conversation runtime context, non-sensitive run ids, HITL checkpoint owner verification, and runtime/security regression tests.
- M5.29-7 delivered: `/api/agent/**` default authenticated fallback, `@EnableMethodSecurity`, observability method-level admin guard, unknown Agent API anonymous rejection, and security contract regression tests.
- M5.30-1 delivered: `AgentAuditDurableSink`, `AgentAuditDurabilityStatus`, `AgentAuditProperties`, redacted append-only `JsonlAgentAuditDurableSink`, optional `atlas.audit.durable.*` config, durability status in admin snapshots, and a SafeToolExecutor high-risk durable-audit readiness gate.
- M5.30-2 delivered: `AgentAuditQueryService`, redacted `AgentAuditQueryEvent` / `AgentAuditQueryResponse`, admin-only `/api/agent/observability/audit/**` query endpoints, `auditId` / `traceId` lookup, and index metadata.
- M5.30-3 delivered: `AgentAuditDurableReceipt`, `AgentAuditOutcome.PREPARED`, `AgentAuditDurableSink#prewriteHighRisk(...)`, `AgentAuditRecorder#prewriteHighRisk(...)`, JSONL `recordPhase=PRE_EXECUTION` / `FINAL`, process-local JSONL write locking, SafeToolExecutor high-risk durable prewrite receipt gate, and fail-closed handling for missing / null / UNKNOWN risk metadata in mandatory durable mode.
- M5.31-1 delivered: `JsonlAgentAuditQueryService`, JSONL newest-first durable audit lookup, recorder facade JSONL preference when available, multi-phase `auditId` evidence lookup, JSONL index metadata, and redaction regression tests.
- M5.31-2 delivered: durable audit retention/export/query policy configuration, admin index metadata for lifecycle governance, bounded JSONL scan/result limits, and metadata-only export policy (`downloadEndpointImplemented=false`, `redactedOnly=true`).
- M5.32-1 delivered: `AgentReplayTimelineService`, `AgentReplayTimelineResponse`, `AgentReplayTimelineStep`, admin-only `/api/agent/observability/replay/trace/{traceId}`, oldest-first replay steps, privacy metadata, outcome-to-phase/kind/status mapping, and replay redaction/security regression tests.
- M5.32-2 delivered: `recordPhase` on `AgentAuditQueryEvent`, JSONL `recordPhase` normalization, `AgentReplayTimelineStep.recordPhase`, and regression coverage proving `FINAL` / `PRE_EXECUTION` survive durable query and replay projection.
- M5.33-1 delivered: `AgentEvalReportService`, `AgentEvalReportResponse`, `AgentEvalCheck`, admin-only `/api/agent/observability/eval/trace/{traceId}`, deterministic eval checks over redacted replay evidence, auditId-level high-risk prewrite evidence validation, execution semantics validation, truncation warning, privacy proof, and eval security/redaction regression tests.
- M5.34-1 delivered: `AgentEvalSuiteRequest`, `AgentEvalSuiteResponse`, admin-only `/api/agent/observability/eval/suite`, suite-level trace deduplication, per-trace eval aggregation, minimum-score release gate, fail-on-warning policy, privacy proof aggregation, and suite security/redaction regression tests.
- M5.34-2 delivered: service-owned suite defaults/caps, bounded per-trace replay limit, clamped minimum score, maximum 50-case eval suite cap, fail-closed oversized-suite gate, skipped trace-id evidence, warning-policy regression tests, and null/default request contract tests.
- M5.35-1 delivered: `AgentEvalSuiteDefinition`, `AgentEvalSuiteCatalogResponse`, `AgentEvalSuiteRunResponse`, `AgentEvalSuiteCatalogService`, admin-only `/api/agent/observability/eval/suites`, admin-only `/api/agent/observability/eval/suites/{suiteId}/run`, four built-in Phase 1 suite definitions, named run default-policy application, and catalog/run security/redaction regression tests.
- M5.36-1 delivered: `AgentEvalSuiteGateArtifact`, `AgentEvalSuiteCatalogService#gate(...)`, admin-only `/api/agent/observability/eval/suites/{suiteId}/gate`, compact CI/release gate payload without embedded reports/replay, and service/controller/security regression tests.
- M5.37-1 delivered: `AgentEvalTraceSetDefinition`, `AgentEvalTraceSetCatalogResponse`, `AgentEvalTraceSetGateArtifact`, `AgentEvalTraceSetCatalogService`, classpath catalog `observability/eval-trace-sets.json`, admin-only `/api/agent/observability/eval/trace-sets`, admin-only `/api/agent/observability/eval/trace-sets/{traceSetId}/gate`, fail-closed empty curated evidence, request trace-id override ignoring, and service/controller/security regression tests.
- M5.38-1 delivered: `AgentEvalTraceSetGateBundleArtifact`, `AgentEvalTraceSetCatalogService#gateBundle(...)`, admin-only `/api/agent/observability/eval/trace-sets/gate-bundle`, CI artifact generation at `target/agent-eval/trace-set-gate-bundle.json`, GitHub Actions upload of `target/agent-eval/`, and bundle/service/controller/security/workflow regression tests.
- M5.39-1 delivered: `AgentEvalTraceSetCurationReviewArtifact`, `AgentEvalTraceSetCatalogService#curationReview(...)`, admin-only `/api/agent/observability/eval/trace-sets/{traceSetId}/curation-review`, W3C-compatible candidate trace-id filtering, review verdicts, review-only curation policy, no catalog mutation, and service/controller/security regression tests.
- M5.40-1 delivered: `AgentAuditQueryService#recentEvents(...)`, in-memory and JSONL redacted recent audit queries, `AgentEvalTraceSetCandidate`, `AgentEvalTraceSetCandidateDiscoveryResponse`, `AgentEvalTraceSetCandidateDiscoveryService`, admin-only `/api/agent/observability/eval/trace-sets/{traceSetId}/candidates`, trace-set-aware candidate recommendation logic, and audit/service/controller/security regression tests.
- M5.41-1 delivered: `AgentEvalTraceSetCatalogPatchProposalArtifact`, `AgentEvalTraceSetCatalogService#catalogPatchProposal(...)`, admin-only `/api/agent/observability/eval/trace-sets/{traceSetId}/catalog-patch-proposal`, RFC 6902-style review-only JSON Patch proposal, Git-review-only promotion semantics, no runtime catalog mutation, and service/controller/security regression tests.
- M5.42-1 delivered: `AgentEvalTraceSetPromotionWorkflowRequest`, `AgentEvalTraceSetPromotionWorkflowArtifact`, `AgentEvalTraceSetPromotionWorkflowService`, admin-only `/api/agent/observability/eval/trace-sets/{traceSetId}/promotion-workflow`, bounded recommended trace selection, typed discovery-to-review-to-patch orchestration, and service/controller/security regression tests.
- M5.43-1 delivered: `AgentEvalWorkbenchCapability`, `AgentEvalWorkbenchCapabilitiesResponse`, `AgentEvalWorkbenchCapabilitiesService`, admin-only `/api/agent/observability/eval/workbench/capabilities`, contract-driven workbench capability discovery, recommended workflow metadata, and service/controller/security regression tests.
- M5.44-1 delivered: `AgentEvalWorkbenchTraceSetView`, `AgentEvalWorkbenchOverviewResponse`, `AgentEvalWorkbenchOverviewService`, admin-only `/api/agent/observability/eval/workbench/overview`, UI-ready trace-set rows, compact gate-bundle overview state, next-action guidance, and service/controller/security regression tests.
- M5.45-1 delivered: `AgentEvalWorkbenchTraceSetDetailResponse`, `AgentEvalWorkbenchTraceSetDetailService`, admin-only `/api/agent/observability/eval/workbench/trace-sets/{traceSetId}`, detail-layer evidence requirements, promotion checklist, endpoint templates, and service/controller/security regression tests.
- M5.46-1 delivered: `AgentEvalWorkbenchPromotionWorkflowResponse`, `AgentEvalWorkbenchPromotionWorkflowService`, admin-only `/api/agent/observability/eval/workbench/trace-sets/{traceSetId}/promotion-workflow`, UI steps, patch summary, candidate gate summary, next actions, endpoint templates, and service/controller/security regression tests.
- M5.47-1 delivered: `AgentEvalWorkbenchCatalogPatchReviewResponse`, `AgentEvalWorkbenchCatalogPatchReviewService`, admin-only `/api/agent/observability/eval/workbench/trace-sets/{traceSetId}/catalog-patch-review`, sanitized patch operations, trace delta, candidate gate summary, review checklist, next actions, endpoint templates, and service/controller/security regression tests.
- M5.48-1 delivered: `AgentEvalWorkbenchGateBundleSummaryResponse`, `AgentEvalWorkbenchGateBundleSummaryService`, admin-only `/api/agent/observability/eval/workbench/gate-bundle-summary`, bundle summary, trace-set gate rows, CI artifact metadata, blocker summary, next actions, endpoint templates, and service/controller/security regression tests.
- M5.49-1 delivered: `AgentKubeManagerHttpOutletHealthSummaryResponse`, `AgentKubeManagerHttpOutletHealthSummaryService`, admin-only `/api/agent/observability/kube-manager/http-outlet/health-summary`, redacted backend configuration facts, effective GET retry policy, effective WRITE no-auto-retry policy, circuit breaker state, bulkhead state, safety/privacy proof, and service/controller/security regression tests.
- M5.50-1 delivered: `AgentKubeManagerWriteRetryReadinessResponse`, `AgentKubeManagerWriteRetryReadinessService`, admin-only `/api/agent/observability/kube-manager/http-outlet/write-retry-readiness`, fail-closed readiness verdict, future evidence checklist, current evidence/gap summary, endpoint templates, safety/privacy proof, and service/controller/security regression tests.
- M5.51-1 delivered: `KubeManagerWriteIdempotencyKeyInput`, `KubeManagerWriteIdempotencyKeyResult`, `KubeManagerWriteIdempotencyKeyDeriver`, `AgentKubeManagerWriteIdempotencyContractResponse`, `AgentKubeManagerWriteIdempotencyContractService`, admin-only `/api/agent/observability/kube-manager/http-outlet/write-idempotency-contract`, updated M5.50 readiness evidence, and deriver/service/controller/security regression tests.
- Latest technology strategy: all advanced Phase 1 technologies are in scope, but split into stable mainline and compatibility matrix. Stable mainline covers verified execution, trace/audit, MCP safe manifest, OTel OTLP, resilience, CI gates, evals, and frontend replay contracts. Compatibility matrix covers Java 21/25, Spring Boot 4, Spring AI 2, evolving MCP call protocol, OpenTelemetry GenAI semantic conventions, and A2A adapters.
- M5.24-1 multi-expert note: current Java/Spring technology line is advanced enough; Phase 1 P0 priorities are now real Resilience4j HTTP outlet governance, CI hard gates, SafeToolExecutor-only enforcement, OTel span mapping, Spring Security identity migration, and Testcontainers-backed integration tests rather than blind framework stacking.
- 2026-06-09 Archimedes multi-expert refresh: Phase 1 mainline should continue Spring Security, Resilience4j, Micrometer/OTel span/timeline, durable audit, minimal Spring AI VectorStore RAG, persistent memory, and read-only MCP manifest/schema adapter. Java 21/25, Spring Boot 4, Spring AI 2, complete external MCP server/broker, A2A, GraphRAG/reranker/vector-store experiments, virtual threads/structured concurrency, and NIM/HPC/Slurm/BCM specialist plugins remain compatibility-matrix or Phase 2 items.
- 2026-06-09 Java backend technology audit refresh: Java/Spring remains the right top-tier Agent Core control-plane choice. Gaps are not language-level; they are Spring Security mainline adoption, durable audit, hard quality gates, OTel span/timeline completion, fine-grained retry predicates, RAG/persistent Memory, Agent eval, and read-only MCP schema adapter. Java 21/25, Spring Boot 4, Spring AI 2, A2A, full MCP broker, GraphRAG, and virtual threads remain compatibility-matrix work until tests prove them safe.
- Resume hint: continue Phase 1 generic manager Agent Core. The master readiness endpoint is `/api/agent/observability/top-tier/readiness-overview`; the latest-technology watch endpoint is `/api/agent/observability/top-tier/official-version-protocol-watch`; Memory/RAG endpoints now include `/api/agent/observability/memory-rag/readiness` and `/api/agent/observability/memory-rag/citation-source-contract`. Next technical slices should first wire `vue-kube-manager` to these readiness/watch maps, then populate reviewed redacted eval trace evidence, promote gate bundles from evidence-only to reviewed blocking mode, and implement durable Memory/RAG source DTO/digest, retention/delete/export, redacted ingestion, retrieval policy, and citation eval gates. Frontend/API integration targets now include `/api/agent/observability/top-tier/readiness-overview`, `/api/agent/observability/top-tier/official-version-protocol-watch`, `/api/agent/observability/memory-rag/readiness`, `/api/agent/observability/memory-rag/citation-source-contract`, `/api/agent/observability/kube-manager/http-outlet/governance-workbench/overview`, `/api/agent/observability/kube-manager/http-outlet/health-summary`, `/api/agent/observability/kube-manager/http-outlet/write-retry-readiness`, `/api/agent/observability/kube-manager/http-outlet/write-idempotency-contract`, `/api/agent/observability/kube-manager/http-outlet/write-operation-safety-contract`, `/api/agent/observability/kube-manager/http-outlet/write-retry-governance-contract`, `/api/agent/observability/kube-manager/http-outlet/write-release-gate-contract`, `/api/agent/observability/eval/workbench/capabilities`, `/api/agent/observability/eval/workbench/overview`, `/api/agent/observability/eval/workbench/gate-bundle-summary`, `/api/agent/observability/eval/workbench/trace-sets/{traceSetId}`, `/api/agent/observability/eval/workbench/trace-sets/{traceSetId}/promotion-workflow`, `/api/agent/observability/eval/workbench/trace-sets/{traceSetId}/catalog-patch-review`, `/api/agent/mcp/governance/overview`, `/api/agent/mcp/manifest`, `/api/agent/observability/replay/trace/{traceId}`, `/api/agent/observability/eval/trace/{traceId}`, `/api/agent/observability/eval/suite`, `/api/agent/observability/eval/suites`, `/api/agent/observability/eval/suites/{suiteId}/run`, `/api/agent/observability/eval/suites/{suiteId}/gate`, `/api/agent/observability/eval/trace-sets`, `/api/agent/observability/eval/trace-sets/{traceSetId}/candidates`, `/api/agent/observability/eval/trace-sets/{traceSetId}/gate`, `/api/agent/observability/eval/trace-sets/{traceSetId}/curation-review`, `/api/agent/observability/eval/trace-sets/{traceSetId}/catalog-patch-proposal`, `/api/agent/observability/eval/trace-sets/{traceSetId}/promotion-workflow`, and `/api/agent/observability/eval/trace-sets/gate-bundle`. Do not add runtime MCP `tools/call`, kube-manager writes, write retry enablement, retrieval execution, vector-store binding, HITL invocation, blind dependency upgrades, or NIM/HPC/Slurm/BCM Phase 2 work unless explicitly reopened and backed by release evidence.
- M5.41 resume addendum: the backend patch proposal endpoint now exists at `/api/agent/observability/eval/trace-sets/{traceSetId}/catalog-patch-proposal`; next frontend/eval work should include this endpoint between curation review and gate-bundle regeneration.
- M5.42 resume addendum: the backend promotion workflow endpoint now exists at `/api/agent/observability/eval/trace-sets/{traceSetId}/promotion-workflow`; next frontend/eval workbench should prefer this typed workflow artifact over manually reconstructing promotion state from multiple endpoints.
- M5.43 resume addendum: the backend capability manifest endpoint now exists at `/api/agent/observability/eval/workbench/capabilities`; next frontend/eval workbench should discover workbench features and schema versions from this manifest.
- M5.44 resume addendum: the backend overview endpoint now exists at `/api/agent/observability/eval/workbench/overview`; next frontend/eval workbench should use it as the landing read model before drilling into promotion workflow, replay timeline, or eval reports.
- M5.45 resume addendum: the backend trace-set detail endpoint now exists at `/api/agent/observability/eval/workbench/trace-sets/{traceSetId}`; next frontend/eval workbench should use it after overview row selection before opening candidate discovery, promotion workflow, replay timeline, or eval reports.
- M5.46 resume addendum: the backend workbench promotion workflow endpoint now exists at `/api/agent/observability/eval/workbench/trace-sets/{traceSetId}/promotion-workflow`; next frontend/eval workbench should prefer this page-ready response over directly composing raw workflow, patch proposal, and gate summary state in Vue.
- M5.47 resume addendum: the backend workbench catalog patch review endpoint now exists at `/api/agent/observability/eval/workbench/trace-sets/{traceSetId}/catalog-patch-review`; next frontend/eval workbench should render sanitized `patchOperations`, `traceDelta`, `candidateGateSummary`, `reviewChecklist`, and `nextActions` for Git review, not add a runtime apply-patch action.
- M5.48 resume addendum: the backend workbench gate bundle summary endpoint now exists at `/api/agent/observability/eval/workbench/gate-bundle-summary`; next frontend/eval workbench should render `bundleSummary`, `traceSetGateRows`, `ciArtifact`, `blockerSummary`, and `nextActions` for release-gate orientation, not accept caller trace IDs or enable CI blocking from the page.
- M5.49 resume addendum: the backend kube-manager HTTP outlet health summary endpoint now exists at `/api/agent/observability/kube-manager/http-outlet/health-summary`; future frontend observability pages should render local resilience policy and bounded registry state, not add ping/login/token-refresh/circuit-breaker-reset/write-retry-enable actions around this read model.
- M5.50 resume addendum: the backend kube-manager write retry readiness endpoint now exists at `/api/agent/observability/kube-manager/http-outlet/write-retry-readiness`; future frontend observability pages should render the fail-closed readiness verdict, required evidence, current gaps, and blocked reasons, not add a runtime enable-write-retry action.
- M5.51 resume addendum: the backend kube-manager write idempotency contract endpoint now exists at `/api/agent/observability/kube-manager/http-outlet/write-idempotency-contract`; future frontend observability pages should render contract-defined-but-unbound status, not a runtime key derivation or header injection action.
- M5.52 resume addendum: the backend kube-manager write operation safety contract endpoint now exists at `/api/agent/observability/kube-manager/http-outlet/write-operation-safety-contract`; future frontend observability pages should render allowlist/RBAC/readback contract-defined-but-unbound status, not a runtime allowlist editor or readback executor.
- M5.53 resume addendum: the backend kube-manager write retry governance contract endpoint now exists at `/api/agent/observability/kube-manager/http-outlet/write-retry-governance-contract`; future frontend observability pages should render failure classes, bounded predicate, and compensation guidance as review-only contracts, not a runtime retry toggle or compensation execution action.
- M5.54 resume addendum: the backend kube-manager write release gate contract endpoint now exists at `/api/agent/observability/kube-manager/http-outlet/write-release-gate-contract`; future frontend observability pages should render durable receipt and HITL/release evidence as contract-defined-but-unbound prerequisites, not a runtime HITL trigger, receipt issuer, release switch, or write-retry enable action.
- M5.55 resume addendum: the backend kube-manager HTTP outlet governance workbench overview endpoint now exists at `/api/agent/observability/kube-manager/http-outlet/governance-workbench/overview`; future frontend observability pages should use it as the landing read model for M5.49-M5.54 cards before drilling into individual contracts, not add runtime write/retry/release controls.
- M5.56 resume addendum: the backend MCP governance overview endpoint now exists at `/api/agent/mcp/governance/overview`; future MCP work should use it with `/api/agent/mcp/manifest` as the discovery/governance layer, not add runtime `tools/call`, streaming calls, external Agent execution, write-tool export, or sensitive-read export until identity, tenant, consent, HITL, durable audit, eval gates, rate limits, and `SafeToolExecutor` binding are implemented and reviewed.
- M5.57 resume addendum: the backend top-tier readiness overview endpoint now exists at `/api/agent/observability/top-tier/readiness-overview`; future Phase 1 planning should treat it as the master control-plane read model. Current gaps are eval evidence, blocking gate promotion, durable Memory/RAG citation contract, Vue consumption, and future MCP execution binding. Endpoint map fields are navigation only and do not grant access to sibling endpoints.
- M5.58 resume addendum: the backend Memory/RAG readiness endpoint now exists at `/api/agent/observability/memory-rag/readiness`; future Memory/RAG work should use it as the fail-closed contract before adding durable store, vector retrieval, ingestion, citations, or evals. Do not wire retrieval into runtime prompts until source digest, tenant/privacy, redaction, eval, and Vue governance evidence are implemented.
- M5.59 resume addendum: the backend Memory/RAG citation-source contract endpoint now exists at `/api/agent/observability/memory-rag/citation-source-contract`; future RAG work should implement source evidence DTO/digest derivation, durable source storage, redacted ingestion, retrieval policy, and citation evals before binding retrieval runtime.
- M5.60 resume addendum: the backend Memory/RAG source evidence digest contract endpoint now exists at `/api/agent/observability/memory-rag/source-evidence-digest-contract`; future RAG work should bind all source/chunk/evidence/citation seeds to redacted, tenant-scoped SHA-256 evidence before ingestion or retrieval can influence prompts.
- M5.61 resume addendum: the backend Memory/RAG durable memory lifecycle contract endpoint now exists at `/api/agent/observability/memory-rag/durable-memory-lifecycle-contract`; future durable memory work must preserve tenant partition digest, source evidence digest, retention policy id, delete proof digest, export proof digest, recovery checkpoint digest, and eval gate digest before binding any real store or prompt evidence. Do not add DB writes, vector-store runtime binding, retention/delete/export/recovery jobs, retrieval execution, prompt mutation, kube-manager calls, MCP runtime `tools/call`, or NIM/HPC/Slurm/BCM Phase 2 work in this line without separate release evidence.
- M5.62 resume addendum: the backend Memory/RAG eval gate contract endpoint now exists at `/api/agent/observability/memory-rag/eval-gate-contract`; future retrieval work must bind citation fidelity, source digest integrity, privacy leakage, tenant isolation, retention/staleness, delete/export/recovery proof, retrieval policy budget, unsupported-answer, and prompt-injection boundary checks before memory can influence prompts. Do not add eval runtime execution, CI blocking changes, trace-set mutation, retrieval execution, vector-store binding, prompt mutation, kube-manager calls, MCP runtime `tools/call`, or NIM/HPC/Slurm/BCM Phase 2 work without separate release evidence.
- M5.63 security invariant: the top-tier advanced technology adoption contract endpoint now exists at `/api/agent/observability/top-tier/advanced-technology-adoption-contract`; it is admin-only, read-only, contract-only, and fail-closed. It separates stable mainline technologies from compatibility-matrix technologies and keeps `runtimeUpgradePerformed=false`, `dependencyUpgradePerformed=false`, `externalAgentRuntimeBound=false`, `javaSpringControlPlanePreserved=true`, and `phase2NimHpcSlurmBcmPaused=true`. It does not upgrade dependencies, bind external Agent runtimes, call LLMs, execute Tools, invoke `SafeToolExecutor`, invoke HITL, call kube-manager, expose MCP runtime `tools/call`, write audit, issue durable receipts, write memory, execute retrieval, mutate prompts, or touch NIM/HPC/Slurm/BCM Phase 2 scope.
- M5.63 resume addendum: future Phase 1 planning should use `/api/agent/observability/top-tier/advanced-technology-adoption-contract` as the policy for "latest technology" adoption. Stable mainline remains Java/Spring control plane, Spring AI 1.1.x, SafeToolExecutor, deterministic eval, trace/audit/replay, Memory/RAG contracts, MCP governance, and kube-manager governance. Compatibility matrix tracks Java 21/25/26, Spring Boot 4, Spring AI 2, Responses/Agents-style runtime mapping, full MCP runtime, OTel GenAI, A2A, GraphRAG, rerankers, and vector stores. Do not blind-upgrade the backend stack; bind advanced runtimes only after source-owned contracts, identity/tenant/privacy proof, trace/audit/replay, deterministic eval, Vue read models, and recovery memory exist.
- M5.64 security invariant: the Phase 1 execution roadmap endpoint now exists at `/api/agent/observability/top-tier/phase1-execution-roadmap`; it is admin-only, read-only, roadmap-only, and fail-closed. It keeps `roadmapStatus=PHASE_1_TOP_TIER_ROADMAP_ACTIVE`, `phase1TopTierGoalPreserved=true`, `phase2NimHpcSlurmBcmPaused=true`, `roadmapOnly=true`, and `runtimeMutationAllowed=false`. It does not execute evals, mutate trace sets, bind retrieval, call LLMs, execute Tools, invoke `SafeToolExecutor`, invoke HITL, call kube-manager, expose MCP runtime `tools/call`, write audit, issue durable receipts, write memory, upgrade dependencies, or touch NIM/HPC/Slurm/BCM Phase 2 scope.
- M5.64 resume addendum: future Phase 1 work should use `/api/agent/observability/top-tier/phase1-execution-roadmap` as the ordered plan. Next order is Vue readiness control plane, reviewed redacted eval trace evidence, release-blocking eval gates, Memory/RAG eval suite binding, durable memory store binding, retrieval runtime binding, MCP runtime safe call plane, and Agent handoff/A2A provenance. Do not start NIM/HPC/Slurm/BCM Phase 2, kube-manager state-changing writes, unsafe MCP `tools/call`, retrieval prompt influence, or blind Spring Boot 4 / Spring AI 2 mainline migration before their gates are satisfied.
- M5.65 security invariant: the Vue readiness control plane endpoint now exists at `/api/agent/observability/top-tier/vue-readiness-control-plane`; it is admin-only, read-only, Vue-contract-only, and fail-closed. It keeps `controlPlaneStatus=BACKEND_CONTRACT_READY_FOR_VUE_BINDING`, `vueBindingReady=true`, `runtimeControlAllowed=false`, `runtimeMutationAllowed=false`, and `phase2NimHpcSlurmBcmPaused=true`. It does not mutate frontend state, add runtime control buttons, execute Tools, invoke `SafeToolExecutor`, invoke HITL, call kube-manager, expose MCP runtime `tools/call`, call LLM/external services, write audit, issue durable receipts, write memory, execute retrieval, upgrade dependencies, or touch NIM/HPC/Slurm/BCM Phase 2 scope.
- M5.65 resume addendum: future Vue work should render the backend-owned dashboard contracts from `/api/agent/observability/top-tier/vue-readiness-control-plane`: top-tier command center, advanced technology adoption, Phase 1 roadmap, kube-manager governance, Memory/RAG readiness, eval workbench, and MCP governance. Do not add write-retry toggles, kube-manager state-changing actions, MCP runtime calls, retrieval prompt controls, CI blocking switches, HITL triggers, durable receipt buttons, dependency upgrade actions, or Phase 2 reopening controls to the readiness UI.



- M5.67-1 security invariant: release-blocking eval gate contract exists at `/api/agent/observability/eval/release-blocking-gate-contract`; it is admin-only, read-only, contract-only, summary-only, and fail-closed. It composes reviewed eval trace evidence with eval workbench gate-bundle summary and keeps `contractStatus=BLOCKED_BY_REVIEWED_TRACE_EVIDENCE`, `releaseBlockingGateDefined=true`, `releaseBlockingEnabled=false`, `ciBlockingEnabled=false`, `releaseGateCanOpenNow=false`, `runtimeMutationAllowed=false`, `reviewedEvidenceReady=false`, `gateBundleReleaseEligible=false`, and `emptyTraceSets=4` in the current catalog state. Even synthetic future-ready evidence can only report `READY_FOR_MANUAL_RELEASE_GATE_PROMOTION`; CI blocking and release blocking remain disabled until a later explicit reviewed CI wiring slice.
- M5.67-1 resume addendum: future eval/release work should use `/api/agent/observability/eval/release-blocking-gate-contract` after `/api/agent/observability/eval/reviewed-trace-evidence` and `/api/agent/observability/eval/workbench/gate-bundle-summary`. Do not mutate GitHub Actions, enable CI blocking, run evals, mutate trace-set catalogs, query raw audit evidence, embed replay payloads, execute Tools, invoke `SafeToolExecutor`, invoke HITL, call kube-manager, expose MCP runtime `tools/call`, call LLMs/external services, write audit, issue durable receipts, write memory, execute retrieval, upgrade dependencies, or touch NIM/HPC/Slurm/BCM Phase 2 scope in this line without separate release evidence.
- M5.67-1 latest-technology note: Phase 1 remains top-tier by absorbing OpenAI Agents/Evals tracing and guardrails, MCP tool-call governance, OpenTelemetry GenAI semantic direction, OWASP LLM safety gates, and W3C Trace Context into Java/Spring typed contracts, tests, docs, and recovery memory before runtime authority. Current mainline remains verified Spring Boot 3.5.14 / Spring AI 1.1.7; Spring Boot 4.0.6, Spring AI 2.0.0-RC1, Java 21/25/26, full MCP runtime, A2A, GraphRAG, rerankers, vector stores, virtual threads, and structured concurrency stay in compatibility-matrix work until contract/eval evidence proves them safe.
- M5.68-1 security invariant: Memory/RAG eval-suite binding contract exists at `/api/agent/observability/memory-rag/eval-suite-binding-contract`; it is admin-only, read-only, contract-only, summary-only, and fail-closed. It maps the nine M5.62 Memory/RAG gate checks to future deterministic suite check codes and declares required trace sets for citation fidelity, privacy/tenant isolation, and lifecycle policy. It keeps `contractStatus=CONTRACT_DEFINED_NOT_BOUND`, `memoryRagEvalSuiteBound=false`, `memoryRagTraceSetBound=false`, `evalRuntimeExecuted=false`, `ciBlockingEnabled=false`, `retrievalRuntimeAllowedNow=false`, `mappedGateCheckCount=0`, and `missingGateCheckCount=9` in the current catalog state.
- M5.68-1 resume addendum: future Memory/RAG work should use `/api/agent/observability/memory-rag/eval-suite-binding-contract` after `/api/agent/observability/memory-rag/eval-gate-contract` and before any durable memory retrieval runtime. Next safe steps are to define Memory/RAG suite check codes through Git review, add reviewed redacted trace-set catalog entries, generate advisory gate bundles, wire Vue evidence views, and only later promote retrieval or CI blocking in separate reviewed slices. Do not run eval runtime, mutate trace-set catalogs, enable CI blocking, execute retrieval, bind vector stores, call embedding/reranker/LLM/tools/kube-manager/MCP `tools/call`, write memory or audit, invoke HITL, issue durable receipts, upgrade dependencies, or touch NIM/HPC/Slurm/BCM Phase 2 scope without separate release evidence.
- M5.68-1 latest-technology note: Phase 1 continues to include OpenAI Agents SDK / Responses patterns for tools, handoffs, guardrails, tracing, and eval loops; Spring AI ChatClient, advisors, chat memory, VectorStore RAG, MCP, eval, and observability; MCP 2025-11-25 tools/resources/prompts governance; OpenTelemetry GenAI development semantic conventions through stable internal adapters; and A2A v1.0 Agent Card/task/artifact provenance. All remain governed by backend-owned contracts, deterministic evals, reviewed evidence, Vue operator visibility, and workspace recovery memory before runtime authority.
- M5.69-1 security invariant: Memory/RAG release-gate suite catalog now exists as `memory-rag-release-gate` in `/api/agent/observability/eval/suites`; it defines all nine Memory/RAG deterministic check codes with default minimum score `95` and `failOnWarnings=true`. The eval-suite binding contract now reports `contractStatus=SUITE_CHECKS_DEFINED_TRACE_SETS_NOT_CURATED`, `memoryRagEvalSuiteBound=true`, `memoryRagTraceSetBound=false`, `mappedGateCheckCount=9`, `missingGateCheckCount=0`, `availableSuiteCount=5`, `evalRuntimeExecuted=false`, `ciBlockingEnabled=false`, and `retrievalRuntimeAllowedNow=false`. Here `memoryRagEvalSuiteBound=true` only means suite check-code mapping is complete; it does not mean reviewed trace evidence, eval execution, CI blocking, or retrieval prompt influence is enabled. The suite is catalog-only in M5.69 with `runtimeExecutionAllowed=false`; existing named suite `/run` and `/gate` endpoints fail closed for `memory-rag-release-gate` until a later reviewed advisory runtime slice opens it.
- M5.69-1 resume addendum: future Memory/RAG work should add Git-reviewed trace-set catalog entries for `memory-rag-citation-fidelity`, `memory-rag-privacy-tenant`, and `memory-rag-lifecycle-policy`; then curate reviewed redacted trace ids; then add advisory Memory/RAG gate-bundle and Vue workbench visibility; then promote CI blocking only in a separate reviewed slice; then bind durable memory/retrieval runtime only after source digest, lifecycle, tenant/privacy, eval evidence, Vue visibility, and recovery memory pass. Do not run eval runtime, mutate trace-set catalogs without Git review, enable CI blocking, execute retrieval, bind vector stores, call embedding/reranker/LLM/tools/kube-manager/MCP `tools/call`, write memory or audit, invoke HITL, issue durable receipts, upgrade dependencies, or touch NIM/HPC/Slurm/BCM Phase 2 scope without separate release evidence.
- M5.69-1 latest-technology note: the latest Agent technology is being introduced as evidence-first architecture, not blind runtime wiring. OpenAI Agents / Responses patterns, Spring AI Memory/RAG/MCP/eval/observability, MCP tools/resources/prompts governance, OpenTelemetry GenAI telemetry, A2A handoff/provenance, GraphRAG/reranker/vector-store ideas, and future Java/Spring upgrades remain Phase 1 targets or compatibility-matrix work, but production authority expands only through Java/Spring-owned contracts, deterministic suites, reviewed redacted traces, audit/replay evidence, Vue operator visibility, and recovery checkpoints. NIM / HPC / Slurm / BCM remain Phase 2 paused.
- M5.72-1 security invariant: Memory/RAG trace-set curation workbench overview now exists at `/api/agent/observability/memory-rag/workbench/trace-set-curation/overview`; it is admin-only, read-only, overview-only, Vue-workbench-only, and fail-closed. It composes only `AgentMemoryRagTraceSetCurationContractService.contract()`, `AgentMemoryRagEvalSuiteBindingContractService.contract()`, and `AgentMemoryRagReadinessService.readiness()`. It reports `workbenchStatus=WORKBENCH_READY_TO_RENDER_REVIEWED_EVIDENCE_GAPS`, `curationCardCount=3`, `blockingCardCount=3`, `requiredTraceSetCount=3`, `definedTraceSetCount=3`, `reviewedTraceSetCount=0`, `sourceReadModelsEmbedded=true`, and `runtimeControlAllowed=false`. It publishes three Vue cards for `memory-rag-citation-fidelity`, `memory-rag-privacy-tenant`, and `memory-rag-lifecycle-policy`, plus `suiteLatchCard`, `disabledRuntimeActions`, `renderHints`, `workbenchPolicy`, `safety`, and `privacy`. It does not run evals, call gate/gateBundle, query raw audit, discover candidates, execute curation review, promote trace IDs, accept caller trace IDs, mutate trace-set catalogs, enable CI blocking, execute retrieval, bind vector stores, call embedding/reranker/LLM, mutate prompts, write memory, write audit, issue durable receipts, execute Tools, invoke `SafeToolExecutor`, invoke HITL, call kube-manager or port `8100`, expose MCP runtime `tools/call`, call external services, upgrade dependencies, or touch NIM / HPC / Slurm / BCM Phase 2 scope.
- M5.72-1 resume addendum: future Memory/RAG work should use the M5.72 workbench as the Vue operator surface for reviewed trace evidence gaps. Next safe step is to curate reviewed redacted trace IDs for `memory-rag-citation-fidelity`, `memory-rag-privacy-tenant`, and `memory-rag-lifecycle-policy` through human/Git review. Only after reviewed traces exist should a separate reviewed slice generate advisory Memory/RAG gate-bundle evidence. CI blocking, retrieval runtime, durable memory runtime, MCP tools/call, kube-manager write authority, and Phase 2 NIM/HPC/Slurm/BCM remain closed.
- M5.72-1 latest-technology note: Phase 1 remains top-tier by keeping Java/Spring as the governed control plane and introducing Spring AI RAG/VectorStore/MCP, MCP runtime tools/resources/prompts, OpenTelemetry GenAI adapters, OpenAI Agents SDK-style tracing/guardrails/handoffs/evals, A2A provenance, GraphRAG, rerankers, and vector stores through contracts, read models, eval evidence, Vue visibility, and compatibility matrices before runtime authority.
- M5.71-1 security invariant: Memory/RAG trace-set curation contract now exists at `/api/agent/observability/memory-rag/trace-set-curation-contract`; it is admin-only, read-only, contract-only, summary-only, and fail-closed. It composes only `AgentEvalTraceSetCatalogService.catalog()` and `AgentEvalSuiteCatalogService.catalog()`, reports `contractStatus=TRACE_SETS_DEFINED_REVIEWED_EVIDENCE_NOT_CURATED`, `requiredTraceSetCount=3`, `definedTraceSetCount=3`, `reviewedTraceSetCount=0`, `allRequiredTraceSetsDefined=true`, `allRequiredTraceSetsPolicyClosed=true`, `suiteRuntimePolicyClosed=true`, `evalRuntimeAllowedNow=false`, `retrievalRuntimeAllowedNow=false`, and `ciBlockingAllowedNow=false`. It exposes `suiteRuntimeLatch` plus per-row `rowStatus`, `policyKeysPresent`, `missingPolicyKeys`, `policyMismatches`, `policyLatchDeclaredClosed`, `blockedReasons`, and `missingEvidence` for Vue/Git review. Missing policy keys fail closed as visible blockers instead of being hidden by safe defaults. It does not run evals, query raw audit, mutate trace-set catalogs, discover candidates, execute curation review, accept caller trace IDs, enable CI blocking, execute retrieval, bind vector stores, call embedding/reranker/LLM, mutate prompts, write memory, write audit, issue durable receipts, execute Tools, invoke `SafeToolExecutor`, invoke HITL, call kube-manager, expose MCP runtime `tools/call`, call external services, upgrade dependencies, or touch NIM / HPC / Slurm / BCM Phase 2 scope.
- M5.71-1 resume addendum: future Memory/RAG work should wire Vue to render trace-set curation rows and blockers, then curate reviewed redacted trace ids for `memory-rag-citation-fidelity`, `memory-rag-privacy-tenant`, and `memory-rag-lifecycle-policy` through Git review, then generate advisory Memory/RAG gate-bundle evidence, then promote CI blocking only in a separate reviewed slice, then bind durable memory/retrieval runtime only after source digest, lifecycle, tenant/privacy, eval evidence, Vue visibility, and recovery memory pass. Do not open kube-manager port `8100`, retrieval, vector store, MCP runtime, Tool execution, or Phase 2 NIM/HPC/Slurm/BCM work in this lane.
- M5.71-1 latest-technology note: Phase 1 remains top-tier by using Java/Spring as the governed control plane while introducing latest Agent technologies through explicit evidence contracts. Current mainline remains Spring Boot 3.5.x / Spring AI 1.1.x / Java 17 because it is buildable and verified; Java 21/25, Spring Boot 4, Spring AI 2, virtual threads, structured concurrency, full MCP runtime, A2A handoff/provenance, OpenTelemetry GenAI adapters, GraphRAG, rerankers, and vector stores remain Phase 1 targets or compatibility-matrix work gated by source-owned contracts, deterministic tests, reviewed traces, Vue operator visibility, and recovery checkpoints.
- M5.70-1 security invariant: Memory/RAG trace-set catalog entries now exist for `memory-rag-citation-fidelity`, `memory-rag-privacy-tenant`, and `memory-rag-lifecycle-policy`; all three bind to `memory-rag-release-gate`, keep `traceIds=[]`, and carry `catalogOnlyUntilReviewed=true`, `suiteRuntimeExecutionAllowed=false`, `runtimeRetrievalAllowed=false`, and `ciBlockingAllowed=false`. The eval-suite binding contract now reports `contractStatus=TRACE_SETS_DEFINED_REVIEWED_EVIDENCE_NOT_CURATED`, `memoryRagEvalSuiteBound=true`, `memoryRagTraceSetBound=false`, `mappedGateCheckCount=9`, `missingGateCheckCount=0`, and `availableTraceSetCount=7`, with trace-set policy fields derived from the actual catalog rows. Trace-set gates for these rows fail closed with `gateVerdict=SUITE_RUNTIME_DISABLED`, `pass=false`, `emptyInput=true`, `traceIds=[]`, `suiteGate=null`, `runtimeExecutionAllowed=false`, `retrievalRuntimeAllowed=false`, and `ciBlockingEnabled=false`. Curie review addendum: M5.70 now also has an independent trace-set policy latch; if a future suite runtime is enabled while a Memory/RAG row still has `suiteRuntimeExecutionAllowed=false` or `catalogOnlyUntilReviewed=true` with empty `traceIds`, the gate returns `TRACE_SET_RUNTIME_DISABLED` and still embeds no suite gate.
- M5.70-1 resume addendum: future Memory/RAG work should curate reviewed redacted trace ids for the three Memory/RAG trace sets through Git review, then generate advisory Memory/RAG gate-bundle evidence, then add Vue workbench visibility, then promote CI blocking only in a separate reviewed slice, then bind durable memory/retrieval runtime only after source digest, lifecycle, tenant/privacy, eval evidence, Vue visibility, and recovery memory pass. Do not run eval runtime before reviewed traces exist, mutate trace-set catalogs at runtime, enable CI blocking, execute retrieval, bind vector stores, call embedding/reranker/LLM/tools/kube-manager/MCP `tools/call`, write memory or audit, invoke HITL, issue durable receipts, upgrade dependencies, or touch NIM/HPC/Slurm/BCM Phase 2 scope without separate release evidence.
- M5.70-1 latest-technology note: Phase 1 remains top-tier by converting advanced Agent patterns into evidence contracts first. OpenAI Agents/Evals-style tools, handoffs, guardrails, tracing, and eval loops; Spring AI Memory/RAG/MCP/eval/observability; MCP tools/resources/prompts; OpenTelemetry GenAI telemetry; A2A handoff/provenance; GraphRAG, rerankers, vector stores, and future Java/Spring upgrades remain in scope, but runtime authority is still blocked until backend-owned contracts, deterministic tests, reviewed redacted traces, Vue operator visibility, and recovery memory prove the path.

- M5.73-1 security invariant: Memory/RAG reviewed trace-evidence manifest now exists at `/api/agent/observability/memory-rag/workbench/trace-set-curation/review-manifest`; it is admin-only, read-only, manifest-only, Vue-workbench-only, and fail-closed. It composes only Memory/RAG contracts/readiness: curation contract, source evidence digest contract, durable lifecycle contract, eval gate contract, eval-suite binding contract, and readiness. It reports `manifestStatus=WAITING_FOR_REVIEWED_REDACTED_TRACE_FIXTURES`, `requiredTraceSetCount=3`, `reviewedTraceSetCount=0`, `reviewedTraceAnchorCount=0`, `authoritativeFixtureCount=0`, `promotionReadyTraceSetCount=0`, and `runtimeControlAllowed=false`. It exposes required fixture schema, digest evidence, review workflow, advanced technology mappings, endpoint map, manifest policy, safety, and privacy for `memory-rag-citation-fidelity`, `memory-rag-privacy-tenant`, and `memory-rag-lifecycle-policy`.
- M5.73-1 resume addendum: future Memory/RAG work should use the M5.73 manifest as the checklist before adding any trace IDs to `src/main/resources/observability/eval-trace-sets.json`. Do not fake fixtures. Only authoritative reviewed redacted trace anchors with source digest, lifecycle, tenant/privacy, and human Git review evidence may be promoted. After reviewed fixtures exist, generate advisory Memory/RAG gate-bundle evidence in a separate reviewed slice. CI blocking, retrieval runtime, durable memory runtime, MCP `tools/call`, kube-manager write authority, port `8100` runtime calls, and Phase 2 NIM/HPC/Slurm/BCM remain closed.
- M5.73-1 latest-technology note: Phase 1 continues to introduce advanced Agent technologies through evidence-first Java/Spring control-plane contracts. Spring AI Memory/RAG/VectorStore, OpenAI Agents tracing/guardrails/evals, MCP tools/resources/prompts, OpenTelemetry GenAI, A2A Agent Card/task/artifact provenance, OWASP LLM safety, GraphRAG, rerankers, and vector stores are represented as manifest mappings and future gates, not as runtime authority. Java/Spring remains the governed backend mainline; Spring Boot 4, Spring AI 2, Java 21/25, full MCP runtime, A2A handoff, GraphRAG, rerankers, and vector stores remain compatibility-matrix or later gated runtime work.
- M5.74-1 security invariant: official version/protocol watch now exists at `/api/agent/observability/top-tier/official-version-protocol-watch`; it is admin-only, read-only, watch-only, and official-source-review-only. It reports `schemaVersion=agent-official-version-protocol-watch.v1`, `watchStatus=OFFICIAL_WATCH_DEFINED_NOT_RUNTIME_BOUND`, `sourceReviewDate=2026-06-09`, `officialSourceCount=7`, `technologyTrackCount=8`, `phase1TopTierGoalPreserved=true`, `javaSpringControlPlanePreserved=true`, `phase2NimHpcSlurmBcmPaused=true`, `runtimeUpgradePerformed=false`, `dependencyUpgradePerformed=false`, and `externalCallsPerformed=false`.
- M5.74-1 resume addendum: future latest-technology work should use the M5.74 watch as the required entry point before changing Java/Spring/Spring AI/OpenAI/MCP/A2A/OTel/OWASP/RAG integration. Do not blind-upgrade dependencies or open runtime authority because a protocol is new. First update official source evidence through Git review, then add compatibility-matrix tests, typed backend contracts, Vue read-only visibility, reviewed trace/eval evidence, and separate release-gated runtime binding. MCP `tools/call`, A2A handoff, retrieval runtime, vector store/reranker/GraphRAG, CI blocking, kube-manager write authority, port `8100` runtime calls, and Phase 2 NIM/HPC/Slurm/BCM remain closed.
- M5.74-1 latest-technology note: official sources tracked by the watch are Spring AI Reference, OpenAI Responses migration guide, OpenAI Agents SDK guide, MCP 2025-11-25, A2A latest specification, OpenTelemetry GenAI semantic conventions, and OWASP LLM Top 10. The technology tracks are Java/Spring governed control plane, Spring AI Memory/RAG/MCP, OpenAI Responses/Agents interop, MCP runtime call plane, A2A handoff provenance, OTel GenAI adapter, OWASP LLM risk controls, and advanced RAG/GraphRAG/rerankers/vector stores.
- M5.75-1 security invariant: official version/protocol watch dashboard now exists at `/api/agent/observability/top-tier/official-version-protocol-watch/dashboard`; it is admin-only, read-only, dashboard-only, Vue-workbench-only, and external-call-free at request time. It reports `schemaVersion=agent-official-version-protocol-watch-dashboard.v1`, `dashboardStatus=DASHBOARD_READY_TO_RENDER_OFFICIAL_WATCH`, `sourceCardCount=8`, `technologyTrackCardCount=8`, `adoptionGateCount=7`, `blockedRuntimeShortcutCount=6`, `runtimeControlAllowed=false`, `phase1TopTierGoalPreserved=true`, and `phase2NimHpcSlurmBcmPaused=true`.
- M5.75-1 resume addendum: future Vue work should bind `vue-kube-manager` to render `sourceCards`, `technologyTrackCards`, `adoptionGateRows`, `blockedRuntimeShortcutRows`, `disabledRuntimeActions`, and `renderSections` from the dashboard. Do not add dependency-upgrade buttons, MCP `tools/call` buttons, A2A handoff controls, retrieval/vector/reranker/GraphRAG controls, CI blocking switches, kube-manager write authority, HITL triggers, durable receipt buttons, audit/memory write controls, or Phase 2 NIM/HPC/Slurm/BCM reopening controls from this page.
- M5.75-1 latest-technology note: the official watch now includes `nsa-mcp-security-2026-06`, the 2026-06-02 NSA MCP Security Cybersecurity Information, so MCP security guidance is visible as a Phase 1 security gate. This does not open MCP runtime authority. Full MCP runtime, A2A, retrieval, vector stores, rerankers, GraphRAG, Java/Spring/Spring AI major upgrades, and CI blocking remain separate compatibility/release-gated slices after reviewed trace/eval/audit evidence.
- M5.76-1 security invariant: official version/protocol watch Vue binding spec now exists at `/api/agent/observability/top-tier/official-version-protocol-watch/vue-binding-spec`; it is admin-only, read-only, binding-spec-only, Vue-workbench-only, and external-call-free at request time. It reports `schemaVersion=agent-official-version-protocol-watch-vue-binding-spec.v1`, `bindingStatus=VUE_BINDING_SPEC_READY`, `componentSpecCount=7`, `fieldBindingCount=12`, `tableColumnGroupCount=4`, `disabledActionBindingCount=6`, `fixtureCount=4`, `runtimeControlAllowed=false`, and `sourceDashboardEmbedded=true`.
- M5.76-1 resume addendum: future Vue work should implement the official version/protocol watch page from the backend binding spec: components, field paths, table columns, state rules, disabled action bindings, and fixtures. Do not implement hidden runtime buttons or click handlers for dependency upgrades, MCP `tools/call`, A2A handoff, retrieval/vector/reranker/GraphRAG, CI blocking, kube-manager writes, audit/memory writes, HITL, durable receipt issuance, or Phase 2 NIM/HPC/Slurm/BCM reopening.
- M5.76-1 latest-technology note: official sources were rechecked on 2026-06-09. Spring AI stable remains 1.1.7 with Tool Calling, MCP, RAG, evaluation, vector stores, and observability; Spring Boot docs show 4.0.6 but the project keeps major upgrades in compatibility matrix; OpenAI docs expose Responses API, Agents SDK, orchestration, guardrails, observability, eval workflows, ChatKit, tools, MCP/connectors, retrieval, and safety; MCP latest resolves to 2025-11-25; A2A latest covers Agent Card, tasks, streaming, security, bindings, and interoperability. M5.76 turns these into binding/spec/fixture evidence, not direct runtime authority.
- M5.77-1 security invariant: advanced technology compatibility matrix now exists at `/api/agent/observability/top-tier/advanced-technology-compatibility-matrix`; it is admin-only, read-only, matrix-only, and external-call-free at request time. It reports `schemaVersion=agent-advanced-technology-compatibility-matrix.v1`, `matrixStatus=MATRIX_DEFINED_NOT_EXECUTED`, `sourceBaselineCount=8`, `matrixItemCount=10`, `migrationGateCount=8`, `blockedShortcutCount=7`, `testLaneCount=8`, `runtimeUpgradeAllowedNow=false`, `dependencyUpgradeAllowedNow=false`, and `runtimeControlAllowed=false`.
- M5.77-1 resume addendum: future latest-technology work must use the compatibility matrix before upgrading Java, Spring Boot, Spring AI, OpenAI integrations, MCP runtime, A2A, OTel GenAI adapters, GraphRAG/rerankers/vector stores, kube-manager writes, SBOM/dependency audit, or CI blocking. Do not change `pom.xml`, switch baselines, run MCP `tools/call`, enable retrieval prompt influence, enable A2A handoff, enable CI blocking, or open kube-manager writes from the matrix endpoint.
- M5.77-1 latest-technology note: Phase 1 still targets latest Agent technology, but the next safe form is compatibility branches and reviewed evidence, not runtime shortcuts. Current mainline remains Java 17 / Spring Boot 3.5.x / Spring AI 1.1.x until Java 21/25, Boot 4, Spring AI 2, MCP runtime, A2A, OTel GenAI, GraphRAG/reranker/vector-store, and CI/SBOM lanes pass source review, focused tests, security/privacy regression, Vue readonly evidence, recovery memory, and Git-reviewed release decision.
- M5.78-1 security invariant: advanced technology compatibility matrix Vue binding spec now exists at `/api/agent/observability/top-tier/advanced-technology-compatibility-matrix/vue-binding-spec`; it is admin-only, read-only, binding-spec-only, Vue-workbench-only, and external-call-free at request time. It reports `schemaVersion=agent-advanced-technology-compatibility-matrix-vue-binding-spec.v1`, `bindingStatus=VUE_BINDING_SPEC_READY`, `componentSpecCount=8`, `fieldBindingCount=14`, `tableColumnGroupCount=5`, `disabledActionBindingCount=7`, `fixtureCount=5`, `runtimeControlAllowed=false`, and `sourceMatrixEmbedded=true`.
- M5.78-1 resume addendum: future Vue work should implement the compatibility matrix page from the backend binding spec: components, fields, table columns, state rendering rules, disabled shortcut rows, mocked fixtures, and implementation checklist. Do not add runtime/dependency upgrade buttons, MCP `tools/call`, A2A handoff, retrieval/vector/reranker/GraphRAG controls, CI blocking switches, kube-manager write actions, audit/memory writes, HITL, durable receipt issuance, or Phase 2 NIM/HPC/Slurm/BCM reopening controls.
- M5.78-1 latest-technology note: Phase 1 latest-technology governance is now represented as official watch -> dashboard -> binding spec and compatibility matrix -> binding spec. The next safe step is `vue-kube-manager` rendering from these binding specs with mocked fixtures and absent runtime buttons, before any Java/Spring/Spring AI/OpenAI/MCP/A2A/OTel/RAG runtime or dependency migration.

- M5.79-1 security invariant: top-tier Vue workbench implementation package now exists at `/api/agent/observability/top-tier/vue-workbench-implementation-package`; it is admin-only, read-only, implementation-package-only, Vue-workbench-only, and external-call-free at request time. It composes only `AgentOfficialVersionProtocolWatchVueBindingSpecService.spec()` and `AgentAdvancedTechnologyCompatibilityMatrixVueBindingSpecService.spec()`. It reports `schemaVersion=agent-top-tier-vue-workbench-implementation-package.v1`, `packageStatus=IMPLEMENTATION_PACKAGE_READY`, `routeSpecCount=2`, `apiClientBindingCount=4`, `pageAssemblyCount=2`, `sharedComponentCount=7`, `acceptanceFixtureCount=6`, `sourceBindingSpecsEmbedded=true`, and `runtimeControlAllowed=false`. It publishes route specs, API client bindings, page assemblies, shared component contracts, acceptance fixtures, forbidden runtime controls, and implementation order for future `vue-kube-manager` workbench implementation. It does not modify `pom.xml`, upgrade dependencies, switch Java/Spring/Spring AI baselines, run LLMs, execute Tools, invoke `SafeToolExecutor`, invoke HITL, call kube-manager or port `8100`, expose MCP runtime `tools/call`, run A2A handoff, execute retrieval/vector/reranker/GraphRAG, write memory, write audit, issue durable receipts, mutate eval catalogs, enable CI blocking, or touch NIM / HPC / Slurm / BCM Phase 2 work.
- M5.79-1 multi-expert review memory: Newton / frontend-contract review recommended a cross-page workbench package because M5.76/M5.78 single-page specs still leave Vue guessing about route, tab, drilldown, disabled-action, and fixture behavior; M5.79 implements that recommendation. Faraday / backend-architecture review recommended a follow-up evidence-readiness endpoint mapping compatibility-matrix lanes to reviewed trace/eval/Memory-RAG evidence gaps; keep this as the likely M5.80 direction, not part of M5.79.
- M5.79-1 resume addendum: future Vue work should consume the M5.79 package to implement two read-only pages: `/agent/top-tier/official-version-protocol-watch` and `/agent/top-tier/advanced-technology-compatibility-matrix`. Add mocked HTTP fixture tests for `runtime-buttons-absent-in-both-pages`, admin auth, source JSON redaction, and cross-page read-only navigation. Do not add dependency-upgrade buttons, MCP `tools/call`, A2A handoff controls, retrieval/vector/reranker/GraphRAG controls, CI blocking switches, kube-manager write actions, audit/memory writes, HITL, durable receipt issuance, or Phase 2 NIM/HPC/Slurm/BCM reopening controls.
- M5.79-1 latest-technology note: Phase 1 remains top-tier by converting latest Agent technologies into visible, testable, teachable frontend implementation contracts before runtime authority. The current safest next backend slice is M5.80 evidence readiness for the compatibility matrix, followed by actual `vue-kube-manager` rendering when that repository is writable.
